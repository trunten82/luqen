---
phase: 29-service-mcp-tools
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - MCPT-01
  - MCPT-02
  - MCPI-05
  - MCPI-06
tags:
  - docs
  - traceability
  - requirements
  - roadmap

must_haves:
  truths:
    - "REQUIREMENTS.md traceability table reflects the Phase 29/Phase 30 split: MCPT-01 → Phase 30, MCPT-02 → Phase 29 (partial) + Phase 30 (brand score retrieval), MCPT-03 → Phase 29, MCPI-05 → Phase 30, MCPI-06 → Phase 30"
    - "ROADMAP.md Phase 29 Success Criteria reflect only what was actually delivered: 4 branding tools (guidelines + match) + 4 LLM tools (fixes, analysis, discover-branding, extract-requirements)"
    - "ROADMAP.md Phase 29 Requirements field matches the delivered set: MCPT-02 (partial), MCPT-03"
    - "ROADMAP.md Phase 30 Requirements field absorbs the rescoped items: MCPT-01, MCPT-02 (brand score retrieval half), MCPT-04, MCPT-05, MCPI-05, MCPI-06"
    - "ROADMAP.md Phase 30 Success Criteria include scan/report tools, brand score retrieval tools, MCP Resources, and MCP Prompts"
    - "ROADMAP.md Phase 29 Plans list reflects the 3 completed plans (29-01, 29-02, 29-03)"
    - "ROADMAP.md Phase 29 Plans count matches 3"
    - "Traceability coverage remains 20/20 — no requirement ID is orphaned after the rescope"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table with Phase 29/30 re-allocation"
      contains: "MCPT-01 | Phase 30"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 29 + Phase 30 scope wording reflecting D-14/D-15"
      contains: "Phase 29: Service MCP Tools"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 29 Requirements field cross-references IDs in the traceability table"
      pattern: "MCPT-0[123]|MCPI-0[56]"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/29-service-mcp-tools/29-CONTEXT.md"
      via: "D-14 decision is the rescope source of truth"
      pattern: "D-14|D-15"
---

<objective>
Update `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` to reflect the Phase 29 rescope decided in `29-CONTEXT.md` D-14/D-15: MCPT-01 (scan/report/issue tools), the brand score retrieval half of MCPT-02, MCPI-05 (MCP Resources), and MCPI-06 (MCP Prompts) all move to Phase 30 because their natural data lives in `packages/dashboard` — not in compliance/branding/llm. Phase 29's delivered set is: MCPT-02 (partial — list guidelines + match + discover), MCPT-03 (complete — fixes + analysis).

Purpose: Keep traceability accurate after scope narrowing. Prevent the Phase 30 orchestrator from being confused by stale Phase 29 wording. Document the rescope decisions in the source-of-truth files so future milestones inherit the correct context.
Output: Two modified docs files with updated traceability rows, updated Phase 29 / Phase 30 sections, and updated Plans lists. No code changes.

Run AFTER Plan 29-01 and Plan 29-02 land — this plan updates docs to match the as-shipped scope. Depending on both earlier plans avoids a stale-docs race if either earlier plan slips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-service-mcp-tools/29-CONTEXT.md
@.planning/phases/29-service-mcp-tools/29-PATTERNS.md
@.planning/phases/29-service-mcp-tools/29-01-PLAN.md
@.planning/phases/29-service-mcp-tools/29-02-PLAN.md

<interfaces>
<!-- Current REQUIREMENTS.md traceability table (the BEFORE state to update): -->
```
| MCPI-05 | Phase 29 | Pending |
| MCPI-06 | Phase 29 | Pending |
| MCPT-01 | Phase 29 | Pending |
| MCPT-02 | Phase 29 | Pending |
| MCPT-03 | Phase 29 | Pending |
| MCPT-04 | Phase 30 | Pending |
| MCPT-05 | Phase 30 | Pending |
```

<!-- Target AFTER state (D-14 from 29-CONTEXT.md): -->
```
| MCPI-05 | Phase 30 | Pending |
| MCPI-06 | Phase 30 | Pending |
| MCPT-01 | Phase 30 | Pending |
| MCPT-02 | Phase 29 (guidelines + match + discover), Phase 30 (brand score retrieval) | Pending |
| MCPT-03 | Phase 29 | Pending |
| MCPT-04 | Phase 30 | Pending |
| MCPT-05 | Phase 30 | Pending |
```

<!-- Current ROADMAP.md Phase 29 section (the BEFORE state to update — lines 42-53): -->
```
### Phase 29: Service MCP Tools
**Goal**: Users can perform all key compliance, branding, and LLM operations through MCP tools, and MCP clients have read-only resource context and workflow shortcut prompts
**Depends on**: Phase 28
**Requirements**: MCPT-01, MCPT-02, MCPT-03, MCPI-05, MCPI-06
**Success Criteria** (what must be TRUE):
  1. An MCP client can trigger a site scan, list reports, and query issues via compliance MCP tools and receive structured results
  2. An MCP client can list brand guidelines, retrieve brand scores, and invoke discover-branding via branding MCP tools
  3. An MCP client can request fix suggestions and report analysis via LLM MCP tools
  4. An MCP client can read scan reports and brand scores as MCP Resources (read-only, no side effects)
  5. An MCP client can invoke predefined prompt shortcuts (`/scan`, `/report`, `/fix`) that pre-fill tool arguments with workflow-appropriate defaults
**Plans**: TBD
**UI hint**: no
```

<!-- Current ROADMAP.md Phase 30 section (the BEFORE state — lines 55-62): -->
```
### Phase 30: Dashboard MCP + External Clients
**Goal**: Dashboard admin operations are accessible via MCP and external clients such as Claude Desktop can connect and operate with valid credentials
**Depends on**: Phase 28
**Requirements**: MCPT-04, MCPT-05
**Success Criteria** (what must be TRUE):
  1. An authenticated MCP client can manage users, orgs, and service connections via dashboard MCP tools, with RBAC-gated tool manifest (org-member callers see only their permitted subset)
  2. A developer can connect Claude Desktop or an IDE MCP extension to a Luqen service endpoint using standard OAuth2 credentials and successfully call tools — verified via MCP Inspector or Claude Desktop tool list
**Plans**: TBD
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md traceability table to reflect D-14 rescope</name>
  <read_first>
    - .planning/REQUIREMENTS.md (current file — know the full state before editing; lines 76-97 is the traceability table)
    - .planning/phases/29-service-mcp-tools/29-CONTEXT.md (D-14 — authoritative list of moves; D-15 — confirms delivered set)
    - .planning/phases/29-service-mcp-tools/29-01-PLAN.md (confirm MCPT-02 partial is delivered: 4 branding tools including discover on LLM MCP)
    - .planning/phases/29-service-mcp-tools/29-02-PLAN.md (confirm MCPT-03 delivered: 4 LLM tools)
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md (MODIFIED — edit traceability table rows for MCPT-01, MCPT-02, MCPI-05, MCPI-06)
  </files>
  <behavior>
    Update exactly 4 rows in the traceability table (lines 82-87 currently). Leave all other rows unchanged. Do NOT touch the requirements definitions (lines 12-40) — IDs remain the same, only the Phase assignment changes.

    **Changed rows (MCPT-01, MCPT-02, MCPI-05, MCPI-06):**
    - `| MCPT-01 | Phase 29 | Pending |` → `| MCPT-01 | Phase 30 | Pending |`
    - `| MCPT-02 | Phase 29 | Pending |` → `| MCPT-02 | Phase 29 (guidelines + match + discover), Phase 30 (brand score retrieval) | Pending |`
    - `| MCPI-05 | Phase 29 | Pending |` → `| MCPI-05 | Phase 30 | Pending |`
    - `| MCPI-06 | Phase 29 | Pending |` → `| MCPI-06 | Phase 30 | Pending |`

    **Unchanged rows:** MCPT-03, MCPT-04, MCPT-05, all MCPI-01..MCPI-04 (Phase 28 complete), all AGENT-* and APER-* rows.

    **Add a footer note above `---` to document the rescope origin:**
    ```
    ## Phase 29 Scope Rescope

    Per `.planning/phases/29-service-mcp-tools/29-CONTEXT.md` D-14, the following
    requirements moved from Phase 29 to Phase 30 because their natural data lives
    in `packages/dashboard`, not in compliance/branding/llm:
    - **MCPT-01** (scan/report/issue tools) — dashboard owns ScanRepository + scan orchestrator
    - **MCPT-02** partial — "retrieve brand scores" half; dashboard owns BrandScoreRepository
    - **MCPI-05** (MCP Resources) — resources expose dashboard-owned scan reports + brand scores
    - **MCPI-06** (MCP Prompts) — prompts orchestrate cross-service workflows

    Phase 29 delivered: MCPT-02 guidelines + match + discover-branding (via LLM MCP per D-08), MCPT-03 complete (4 LLM tools).
    ```

    Also update the "Last updated" footer date from `2026-04-16 — traceability populated after roadmap creation` to `2026-04-17 — Phase 29 rescope (D-14): MCPT-01, MCPT-02 brand-score half, MCPI-05, MCPI-06 moved to Phase 30`.
  </behavior>
  <action>
    Use the Edit tool to perform these exact string replacements in `.planning/REQUIREMENTS.md`:

    1) Replace `| MCPT-01 | Phase 29 | Pending |` with `| MCPT-01 | Phase 30 | Pending |`

    2) Replace `| MCPT-02 | Phase 29 | Pending |` with `| MCPT-02 | Phase 29 (guidelines + match + discover), Phase 30 (brand score retrieval) | Pending |`

    3) Replace `| MCPI-05 | Phase 29 | Pending |` with `| MCPI-05 | Phase 30 | Pending |`

    4) Replace `| MCPI-06 | Phase 29 | Pending |` with `| MCPI-06 | Phase 30 | Pending |`

    5) Insert the `## Phase 29 Scope Rescope` section (shown in behavior block above) immediately BEFORE the `---` separator at the bottom of the file (the horizontal rule that precedes `*Requirements defined: 2026-04-16*`).

    6) Update the final-line footer timestamp as described in behavior.

    7) Verify coverage is still 20/20 — count all `| ... | Phase NN |` rows in the traceability table and assert the total is 20.
  </action>
  <verify>
    <automated>grep -c "^| MCP" .planning/REQUIREMENTS.md &amp;&amp; grep -n "MCPT-02 | Phase 29 (guidelines + match + discover), Phase 30 (brand score retrieval)" .planning/REQUIREMENTS.md &amp;&amp; grep -n "MCPT-01 | Phase 30" .planning/REQUIREMENTS.md &amp;&amp; grep -n "MCPI-05 | Phase 30" .planning/REQUIREMENTS.md &amp;&amp; grep -n "MCPI-06 | Phase 30" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "| MCPT-01 | Phase 30 | Pending |" .planning/REQUIREMENTS.md` returns a match
    - `grep -n "MCPT-02 | Phase 29 (guidelines + match + discover), Phase 30 (brand score retrieval)" .planning/REQUIREMENTS.md` returns a match
    - `grep -n "| MCPT-03 | Phase 29 | Pending |" .planning/REQUIREMENTS.md` returns a match (UNCHANGED — confirm still Phase 29)
    - `grep -n "| MCPI-05 | Phase 30 | Pending |" .planning/REQUIREMENTS.md` returns a match
    - `grep -n "| MCPI-06 | Phase 30 | Pending |" .planning/REQUIREMENTS.md` returns a match
    - `grep -n "| MCPT-04 | Phase 30 | Pending |" .planning/REQUIREMENTS.md` returns a match (UNCHANGED)
    - `grep -n "| MCPT-05 | Phase 30 | Pending |" .planning/REQUIREMENTS.md` returns a match (UNCHANGED)
    - `grep -c "| MCPI-0[1-4] | Phase 28 | Complete |" .planning/REQUIREMENTS.md` returns exactly 4 (Phase 28 rows UNCHANGED)
    - `grep -n "## Phase 29 Scope Rescope" .planning/REQUIREMENTS.md` returns a match (new section added)
    - `grep -n "D-14" .planning/REQUIREMENTS.md` returns a match (rescope origin documented)
    - `grep -c "| Phase" .planning/REQUIREMENTS.md | head -1` reports 20 (total traceability rows preserved — no IDs lost)
    - `grep -n "2026-04-17 — Phase 29 rescope" .planning/REQUIREMENTS.md` returns a match (timestamp updated)
    - Diff is scoped: no changes outside the traceability table, the new rescope section, or the footer timestamp — verify via `git diff .planning/REQUIREMENTS.md` and inspect visually (or grep-check: `grep -n "^- \\[x\\] \\*\\*MCPI-01" .planning/REQUIREMENTS.md` returns a match confirming definitions untouched)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md traceability table correctly assigns MCPT-01, MCPI-05, MCPI-06 to Phase 30; MCPT-02 is split-annotated; MCPT-03 remains Phase 29. New "Phase 29 Scope Rescope" section documents the move rationale. Footer timestamp updated. Coverage remains 20/20.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 29 and Phase 30 sections to reflect delivered scope</name>
  <read_first>
    - .planning/ROADMAP.md (current file — lines 42-62 contain the Phase 29 and Phase 30 sections; lines 100-110 contain the Progress table)
    - .planning/phases/29-service-mcp-tools/29-CONTEXT.md (D-14 moves, D-15 delivered set)
    - .planning/phases/29-service-mcp-tools/29-01-PLAN.md (confirm Plan 01 scope wording)
    - .planning/phases/29-service-mcp-tools/29-02-PLAN.md (confirm Plan 02 scope wording)
    - .planning/phases/29-service-mcp-tools/29-CONTEXT.md D-12 (Phase 30 Prompt shape — chat-message templates, NOT tool-call pre-fills — must be reflected in Phase 30 success criteria)
  </read_first>
  <files>
    - .planning/ROADMAP.md (MODIFIED — edit Phase 29 section, Phase 30 section, and Progress table rows)
  </files>
  <behavior>
    Rewrite both the Phase 29 and Phase 30 sections as follows. All other sections (milestones header, Phase 28 section, Phases 31-33, Progress table for phases other than 29/30) remain UNTOUCHED.

    **New Phase 29 section** (replaces current lines 42-53):
    ```markdown
    ### Phase 29: Service MCP Tools
    **Goal**: Users can list and match brand guidelines via branding MCP tools, and can request fix suggestions, report analysis, brand discovery, and regulation-requirement extraction via LLM MCP tools
    **Depends on**: Phase 28
    **Requirements**: MCPT-02 (partial — guidelines + match + discover), MCPT-03
    **Success Criteria** (what must be TRUE):
      1. An MCP client can list brand guidelines, get a single guideline, list site assignments, and match pa11y issues against a brand guideline via branding MCP tools (4 tools, all branding.view, all org-scoped with cross-org guards)
      2. An MCP client can generate WCAG fix suggestions, generate executive summaries for scan reports, auto-detect brand signals from a URL, and extract requirements from regulation text via LLM MCP tools (4 tools, all llm.view, all global)
      3. No tool accepts `orgId` in its inputSchema — every handler sources orgId from the ToolContext populated by the OAuth2 JWT at request time (D-13 invariant, enforced by runtime iteration test per service)
    **Plans**: 3 plans
    Plans:
    - [ ] 29-01-PLAN.md — Branding MCP tools (4 tools: list_guidelines, get_guideline, list_sites, match)
    - [ ] 29-02-PLAN.md — LLM MCP tools (4 tools: generate_fix, analyse_report, discover_branding, extract_requirements)
    - [ ] 29-03-PLAN.md — REQUIREMENTS.md + ROADMAP.md rescope (move MCPT-01, MCPT-02 brand-score half, MCPI-05, MCPI-06 to Phase 30)
    **UI hint**: no
    **Rescope note**: The original Phase 29 wording included scan/report/issue tools (MCPT-01), brand score retrieval (MCPT-02 second half), MCP Resources (MCPI-05), and MCP Prompts (MCPI-06). These moved to Phase 30 during context gathering (see `.planning/phases/29-service-mcp-tools/29-CONTEXT.md` D-14) because their natural data lives in `packages/dashboard`, not in compliance/branding/llm.
    ```

    **New Phase 30 section** (replaces current lines 55-62):
    ```markdown
    ### Phase 30: Dashboard MCP + External Clients
    **Goal**: Dashboard admin operations AND dashboard-owned read data (scans, reports, brand scores) are accessible via MCP as tools, resources, and prompts; external clients such as Claude Desktop can connect with valid credentials
    **Depends on**: Phase 28, Phase 29
    **Requirements**: MCPT-01, MCPT-02 (brand score retrieval half), MCPT-04, MCPT-05, MCPI-05, MCPI-06
    **Success Criteria** (what must be TRUE):
      1. An MCP client can trigger a site scan, list reports, and query issues via dashboard MCP tools (absorbed from Phase 29 — MCPT-01)
      2. An MCP client can retrieve brand scores via dashboard MCP tools (absorbed from Phase 29 — MCPT-02 brand-score half)
      3. An authenticated MCP client can manage users, orgs, and service connections via dashboard MCP tools, with RBAC-gated tool manifest (org-member callers see only their permitted subset) — MCPT-04
      4. A developer can connect Claude Desktop or an IDE MCP extension to a Luqen service endpoint using standard OAuth2 credentials and successfully call tools — verified via MCP Inspector or Claude Desktop tool list — MCPT-05
      5. An MCP client can read scan reports and brand scores as MCP Resources (read-only URIs like `scan://report/{id}` and `brand://score/{siteUrl}`) — MCPI-05 (absorbed from Phase 29)
      6. An MCP client can invoke predefined MCP Prompts (`/scan`, `/report`, `/fix`) that return **chat-message templates** (system+user messages with placeholders) that the client feeds to its own LLM, which then chooses which cross-service tools to invoke — MCPI-06 (absorbed from Phase 29; shape locked in 29-CONTEXT.md D-12 — NOT tool-call pre-fills)
    **Plans**: TBD
    ```

    **Progress table update** — change Phase 29 row from `0/?` / `Not started` / `-` to `3/3` / `Complete` / `<today's date>` IF Plans 29-01 and 29-02 have actually landed their SUMMARY files at execution time (they should, since this plan is wave 2 depending on both). If 29-01 and 29-02 have NOT finished at the time this task runs, leave the Progress row unchanged — do not fabricate status. The plan executor (a Claude instance) should check `ls .planning/phases/29-service-mcp-tools/29-01-SUMMARY.md .planning/phases/29-service-mcp-tools/29-02-SUMMARY.md` before updating the row.

    All other rows in the Progress table remain UNCHANGED. In particular: Phase 28 stays at `3/3 Complete 2026-04-17`; Phases 30-33 stay at `0/? Not started -`.
  </behavior>
  <action>
    Use the Edit tool to perform these replacements in `.planning/ROADMAP.md`:

    1) Replace the entire Phase 29 block (the old 12 lines from `### Phase 29: Service MCP Tools` through `**UI hint**: no`) with the new Phase 29 block (shown in behavior above — includes Goal, Depends on, Requirements, Success Criteria, Plans, UI hint, and Rescope note).

    2) Replace the entire Phase 30 block (old 8 lines from `### Phase 30: Dashboard MCP + External Clients` through `**Plans**: TBD`) with the new Phase 30 block (shown in behavior).

    3) Update the Progress table row for Phase 29 only IF prerequisites were met. Check first:
       ```
       ls .planning/phases/29-service-mcp-tools/29-01-SUMMARY.md .planning/phases/29-service-mcp-tools/29-02-SUMMARY.md
       ```
       If BOTH files exist (exit code 0):
         Replace `| 29. Service MCP Tools | 0/? | Not started | - |` with `| 29. Service MCP Tools | 3/3 | Complete   | <today's date in YYYY-MM-DD> |`
       If either is missing: leave the Progress row unchanged — log the skip in the summary.

    4) Do NOT touch anything else in ROADMAP.md — milestones block, Phase 28, Phases 31-33, other progress rows.

    5) Verify the edits with `git diff .planning/ROADMAP.md` — only Phase 29 block, Phase 30 block, and (conditionally) one row of the Progress table should have changes.
  </action>
  <verify>
    <automated>grep -n "MCPT-02 (partial — guidelines + match + discover), MCPT-03" .planning/ROADMAP.md &amp;&amp; grep -n "29-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -n "29-02-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -n "29-03-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -n "chat-message templates" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MCPT-02 (partial — guidelines + match + discover), MCPT-03" .planning/ROADMAP.md` returns a match (Phase 29 Requirements line updated)
    - `grep -n "29-01-PLAN.md" .planning/ROADMAP.md` returns a match (Plans list rewritten)
    - `grep -n "29-02-PLAN.md" .planning/ROADMAP.md` returns a match
    - `grep -n "29-03-PLAN.md" .planning/ROADMAP.md` returns a match
    - `grep -n "branding_list_guidelines, get_guideline, list_sites, match\\|list_guidelines, get_guideline, list_sites, match" .planning/ROADMAP.md` returns a match (Plan 01 objective text present)
    - `grep -n "generate_fix, analyse_report, discover_branding, extract_requirements" .planning/ROADMAP.md` returns a match (Plan 02 objective text)
    - `grep -n "D-13 invariant" .planning/ROADMAP.md` returns a match (new Phase 29 success criterion 3)
    - `grep -n "MCPT-01, MCPT-02 (brand score retrieval half), MCPT-04, MCPT-05, MCPI-05, MCPI-06" .planning/ROADMAP.md` returns a match (Phase 30 Requirements line absorbs rescoped items)
    - `grep -n "Depends on\\**: Phase 28, Phase 29" .planning/ROADMAP.md` returns a match (Phase 30 gains Phase 29 dependency for absorbed tools)
    - `grep -n "chat-message templates" .planning/ROADMAP.md` returns a match (MCPI-06 shape from D-12 reflected)
    - `grep -n "Rescope note" .planning/ROADMAP.md` returns a match (Phase 29 carries rescope note)
    - `grep -c "### Phase 29: Service MCP Tools" .planning/ROADMAP.md` returns exactly 1 (no duplicate sections)
    - `grep -c "### Phase 30: Dashboard MCP + External Clients" .planning/ROADMAP.md` returns exactly 1
    - `grep -n "### Phase 28: MCP Foundation" .planning/ROADMAP.md` returns a match (Phase 28 section UNTOUCHED)
    - `grep -n "### Phase 31: Conversation Persistence" .planning/ROADMAP.md` returns a match (Phase 31 UNTOUCHED)
    - `grep -n "| 28. MCP Foundation | 3/3 | Complete   | 2026-04-17 |" .planning/ROADMAP.md` returns a match (Phase 28 progress row UNCHANGED)
    - If Plans 29-01 + 29-02 SUMMARYs existed at task time: `grep -n "| 29. Service MCP Tools | 3/3 | Complete" .planning/ROADMAP.md` returns a match; otherwise `grep -n "| 29. Service MCP Tools | 0/? | Not started" .planning/ROADMAP.md` returns a match
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 29 section reflects the as-delivered scope (MCPT-02 partial + MCPT-03, 3 plans, rescope note). Phase 30 section absorbs the rescoped items (MCPT-01, MCPT-02 brand-score half, MCPI-05 resources, MCPI-06 chat-message prompts per D-12). Phase 28 and Phases 31-33 untouched. Progress table row for Phase 29 updated only if prerequisites met. Diff is scoped and reviewable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git repo → planning consumers | Incorrect requirements-to-phase mapping could cause future phase planners (Phase 30 planner) to skip items or double-implement them |
| ROADMAP.md success criteria → phase verifiers | UAT and verification compare shipped behavior to success criteria — stale wording causes false failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-18 | Tampering | Docs edit silently drops a requirement ID from traceability (e.g. MCPT-02 becomes un-mapped) | mitigate | Task 1 acceptance asserts MCPT-02 row has the split annotation (not deleted). Total row count grep enforces all 20 IDs still present. MCPT-02 split-row is the only non-standard "phase" value — documented in the new Rescope section. |
| T-29-19 | Information Disclosure | Secrets or internal URLs accidentally committed to ROADMAP.md via copy-paste | accept | Docs-only plan; no new data introduced — only ID/phase reassignments. Copy-paste source is 29-CONTEXT.md (already committed). Low risk. |
| T-29-20 | Repudiation | Rescope happens without written rationale — future contributors can't audit the decision | mitigate | New "Phase 29 Scope Rescope" section in REQUIREMENTS.md (Task 1) points to 29-CONTEXT.md D-14 as origin. ROADMAP.md Phase 29 block carries a Rescope note with the same pointer. Both are grep-verified in acceptance. |
| T-29-21 | Tampering | Task 2 overwrites unrelated ROADMAP.md sections (e.g. Phase 31) through a careless sed / multiline Edit | mitigate | Acceptance asserts Phase 28, 31, 32, 33 sections remain present and untouched. Progress table asserts Phase 28 row's exact text is preserved. git diff review is the final gate. |
| T-29-22 | Elevation of Privilege | N/A — docs-only plan; no runtime permission changes | accept | No threat in this category; noted for completeness. |
| T-29-23 | Denial of Service | N/A — docs-only plan; no runtime impact | accept | No threat in this category. |
</threat_model>

<verification>
- `grep -n "MCPT-01 | Phase 30" .planning/REQUIREMENTS.md` returns a match
- `grep -n "MCPT-02 | Phase 29 (guidelines + match + discover), Phase 30 (brand score retrieval)" .planning/REQUIREMENTS.md` returns a match
- `grep -n "MCPI-05 | Phase 30" .planning/REQUIREMENTS.md` returns a match
- `grep -n "MCPI-06 | Phase 30" .planning/REQUIREMENTS.md` returns a match
- `grep -n "MCPT-03 | Phase 29 | Pending" .planning/REQUIREMENTS.md` returns a match (unchanged — confirms scope discipline)
- `grep -n "D-14" .planning/REQUIREMENTS.md` returns a match
- `grep -n "MCPT-02 (partial — guidelines + match + discover), MCPT-03" .planning/ROADMAP.md` returns a match
- `grep -n "29-01-PLAN.md" .planning/ROADMAP.md` returns a match
- `grep -n "29-02-PLAN.md" .planning/ROADMAP.md` returns a match
- `grep -n "29-03-PLAN.md" .planning/ROADMAP.md` returns a match
- `grep -n "chat-message templates" .planning/ROADMAP.md` returns a match (D-12 preserved)
- `grep -c "### Phase " .planning/ROADMAP.md` returns 6 (Phases 28-33 all still present)
- No untouched sections changed: `git diff --stat .planning/ROADMAP.md` shows only the Phase 29, Phase 30, and Progress-row-29 lines changed
- No untouched sections changed: `git diff --stat .planning/REQUIREMENTS.md` shows only traceability rows + new rescope section + footer timestamp
</verification>

<success_criteria>
- REQUIREMENTS.md traceability correctly assigns all 20 v3.0.0 IDs to a phase (coverage 20/20 preserved)
- MCPT-01, MCPI-05, MCPI-06 reassigned to Phase 30; MCPT-02 annotated with split Phase 29/Phase 30 coverage; MCPT-03 unchanged (Phase 29)
- REQUIREMENTS.md footer notes the rescope with reference to 29-CONTEXT.md D-14
- ROADMAP.md Phase 29 section matches delivered scope (branding guidelines/match tools + 4 LLM tools)
- ROADMAP.md Phase 30 section absorbs the rescoped requirements (MCPT-01 scan/report/issue tools, MCPT-02 brand-score half, MCPI-05 resources, MCPI-06 chat-message prompts)
- ROADMAP.md Phase 30 `Depends on` now includes Phase 29 (absorbed tools require Phase 29 MCP plumbing)
- ROADMAP.md Phases 28, 31, 32, 33 and their progress rows are UNTOUCHED
- ROADMAP.md Plans list for Phase 29 has 3 entries matching 29-01, 29-02, 29-03
- Git diff is scoped — no collateral edits
</success_criteria>

<output>
After completion, create `.planning/phases/29-service-mcp-tools/29-03-SUMMARY.md` documenting:
- Before/after diffs for the 4 traceability rows (exact strings)
- Confirmation the Phase 29 + Phase 30 blocks in ROADMAP.md were replaced in-place with no collateral edits
- Whether the Progress row for Phase 29 was updated to 3/3 Complete (requires 29-01 and 29-02 SUMMARYs to exist at run time) — report YES or NO with the timestamp used
- Confirmation coverage is still 20/20 in REQUIREMENTS.md (20 `| Phase NN |` rows present)
- Any unexpected formatting drift in either file (markdown table column alignment, trailing whitespace) and how resolved
</output>
</content>
</invoke>