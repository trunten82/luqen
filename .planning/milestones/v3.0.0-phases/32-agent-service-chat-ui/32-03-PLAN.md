---
phase: 32-agent-service-chat-ui
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/db/sqlite/migrations.ts
  - packages/dashboard/src/db/interfaces/organizations-repository.ts
  - packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts
  - packages/dashboard/tests/db/migrations.test.ts
  - packages/dashboard/tests/repositories/organizations-repository.test.ts
autonomous: true
requirements:
  - APER-02
must_haves:
  truths:
    - "Migration 050 adds a nullable TEXT column `agent_display_name` to the `organizations` table"
    - "Re-running migrations on an already-migrated DB is idempotent (no duplicate-column error, no data loss)"
    - "OrganizationsRepository exposes getOrg/updateOrgAgentDisplayName with typed agent_display_name field"
    - "MessageStatus type union includes 'pending_confirmation' (was already there from Phase 31 but must be reconfirmed before Plan 04 reads it)"
  artifacts:
    - path: "packages/dashboard/src/db/sqlite/migrations.ts"
      provides: "Migration 050 — agent-display-name column on organizations"
      contains: "agent_display_name"
    - path: "packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts"
      provides: "updateOrgAgentDisplayName(orgId, displayName) + getOrg returning agent_display_name"
    - path: "packages/dashboard/tests/repositories/organizations-repository.test.ts"
      provides: "RED/GREEN test for updateOrgAgentDisplayName roundtrip"
  key_links:
    - from: "packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts"
      to: "packages/dashboard/src/db/sqlite/migrations.ts"
      via: "SELECT agent_display_name FROM organizations"
      pattern: "agent_display_name"
---

<objective>
Add DB migration 050 for the `agent_display_name` column on the `organizations` table (D-14), and expose read/write methods on the OrganizationsRepository that Plans 04 and 08 will consume. This unblocks Plan 04 (AgentService reads the display name per turn to interpolate system prompt) and Plan 08 (org-settings form writes the display name).

Purpose: APER-02 requires a user-visible agent name controllable per-org as the ONLY per-org knob (D-14 — per-org system prompt override is permanently out of scope). Without this plan, Plan 04 cannot populate the first-open greeting (D-19) correctly and Plan 08 has nowhere to write.

Output: Migration 050 appended to the dashboard migrations list; OrganizationsRepository extended with `updateOrgAgentDisplayName` (mirroring existing `updateOrgBrandingMode` shape); `getOrg` returns the new column; idempotent re-run proven via tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-agent-service-chat-ui/32-CONTEXT.md
@.planning/phases/32-agent-service-chat-ui/32-AI-SPEC.md
@.planning/phases/32-agent-service-chat-ui/32-PATTERNS.md
@packages/dashboard/src/db/sqlite/migrations.ts
@packages/dashboard/src/db/interfaces/organizations-repository.ts

<interfaces>
<!-- Existing migrations list shape (packages/dashboard/src/db/sqlite/migrations.ts) -->
```typescript
export interface Migration {
  readonly id: string;       // 3-digit zero-padded — '049' is the most recent
  readonly name: string;
  readonly sql: string;
}

export const MIGRATIONS: readonly Migration[] = [
  // ...047 (agent_conversations + agent_messages)
  // ...048 (agent_audit_log)
  // ...049 (oauth-clients-v2)
  // ADD 050 here
];
```

<!-- OrganizationsRepository existing interface (packages/dashboard/src/db/interfaces/organizations-repository.ts) -->
<!-- Executor MUST read the actual file before extending — the shape below is approximate -->
```typescript
export interface Organization {
  readonly id: string;
  readonly name: string;
  readonly slug: string;
  readonly brandingMode?: string;
  // ADD: readonly agentDisplayName: string | null;
  // ... other fields as currently declared
}

export interface OrganizationsRepository {
  getOrg(id: string): Promise<Organization | null>;
  listOrgs(): Promise<readonly Organization[]>;
  // ADD: updateOrgAgentDisplayName(id: string, displayName: string | null): Promise<void>;
  // ... existing methods stay intact
}
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for migration 050 + updateOrgAgentDisplayName roundtrip</name>
  <files>packages/dashboard/tests/db/migrations.test.ts, packages/dashboard/tests/repositories/organizations-repository.test.ts</files>
  <read_first>
    - packages/dashboard/src/db/sqlite/migrations.ts (entire file — confirm the latest migration id is indeed 049; the pattern uses `{ id, name, sql }` entries)
    - packages/dashboard/src/db/interfaces/organizations-repository.ts (current Organization type and repository interface)
    - packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts (current implementation of getOrg + any updateOrg* methods — the `updateOrgBrandingMode` method is the closest analog)
    - packages/dashboard/tests/db/migrations.test.ts (existing migration test patterns — if this file does not exist, check packages/dashboard/tests/db/ for equivalents)
    - packages/dashboard/tests/repositories/organizations-repository.test.ts (if it exists — extend; else create following the shape of other repo tests in that dir)
    - .planning/phases/32-agent-service-chat-ui/32-PATTERNS.md "MOD packages/dashboard/src/db/sqlite/migrations.ts" + "MOD packages/dashboard/src/routes/admin/organizations.ts" Delta notes
  </read_first>
  <behavior>
    Migration tests (migrations.test.ts):
    - Test 1: MIGRATIONS contains an entry with id `'050'` and name `'agent-display-name'`.
    - Test 2: Running migrations on a fresh SQLite DB creates a column `agent_display_name` on `organizations` (verify via `PRAGMA table_info(organizations)`; expect a row with `name='agent_display_name'` and `type='TEXT'`).
    - Test 3: The new column is nullable (the PRAGMA row has `notnull=0`).
    - Test 4: Running migrations twice on the same DB does NOT throw (idempotent — uses `IF NOT EXISTS` or the migration runner's applied-check already skips). Verify by calling `storage.migrate()` twice and asserting both calls resolve.
    - Test 5: The migration is POSITIONED after 049 in the MIGRATIONS array (find index of 050 and 049; assert index(050) === index(049) + 1).

    OrganizationsRepository tests (organizations-repository.test.ts):
    - Test 6: After migration, `getOrg(id)` on a freshly-inserted org returns an object whose `agentDisplayName` field is `null` (default).
    - Test 7: `updateOrgAgentDisplayName(id, 'Luna')` followed by `getOrg(id)` returns `{ ...org, agentDisplayName: 'Luna' }`.
    - Test 8: `updateOrgAgentDisplayName(id, null)` resets the column to NULL (verify by re-read).
    - Test 9: `updateOrgAgentDisplayName('nonexistent-org', 'X')` does NOT throw but affects 0 rows — repository returns silently (matches existing updateOrg* pattern). Verify getOrg still returns null for nonexistent.
    - Test 10: Empty string is stored as empty string (not coerced to NULL) — callers that want NULL pass `null` explicitly. Verify: `updateOrgAgentDisplayName(id, '')` then `getOrg(id).agentDisplayName === ''`.
  </behavior>
  <action>
    1. Grep `packages/dashboard/tests/db/` for existing migration test patterns. If `migrations.test.ts` exists, extend it with Tests 1-5. If not, create it. Use `SqliteStorageAdapter` + `mkdtempSync` + `rmSync` pattern from other tests (same as Plan 01's approach).
    2. Grep `packages/dashboard/tests/repositories/` for `organizations-repository.test.ts` (Phase 31.2 established a repositories/ test directory per STATE.md). If it exists, extend with Tests 6-10; if not, create it mimicking `agent-audit-repository.test.ts` or similar.
    3. All tests should FAIL at this stage because:
       - Tests 1-5: migration 050 does not exist
       - Tests 6-10: `updateOrgAgentDisplayName` does not exist and `Organization.agentDisplayName` is not a field
    4. Run `npx vitest run tests/db/migrations.test.ts tests/repositories/organizations-repository.test.ts` from packages/dashboard — confirm failures.
    5. Commit `test(32-03): RED — migration 050 + updateOrgAgentDisplayName tests`.
  </action>
  <verify>
    <automated>cd /root/luqen/packages/dashboard &amp;&amp; npx vitest run tests/db/migrations.test.ts tests/repositories/organizations-repository.test.ts 2>&amp;1 | tee /tmp/32-03-red.log ; grep -cE "FAIL|failed" /tmp/32-03-red.log</automated>
  </verify>
  <done>
    - 10 new tests exist and fail meaningfully
    - RED commit landed
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — append migration 050 + extend OrganizationsRepository + update interface</name>
  <files>packages/dashboard/src/db/sqlite/migrations.ts, packages/dashboard/src/db/interfaces/organizations-repository.ts, packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts</files>
  <read_first>
    - packages/dashboard/src/db/sqlite/migrations.ts (find migration 049 entry position — append 050 immediately after)
    - packages/dashboard/src/db/interfaces/organizations-repository.ts (current Organization type; add `readonly agentDisplayName: string | null;`)
    - packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts (understand getOrg SELECT query; current SELECT must now include `agent_display_name`; understand existing updateOrg* method patterns — use as template)
  </read_first>
  <acceptance_criteria>
    - packages/dashboard/src/db/sqlite/migrations.ts MIGRATIONS array has a new entry `{ id: '050', name: 'agent-display-name', sql: "ALTER TABLE organizations ADD COLUMN agent_display_name TEXT;" }`
    - The entry is positioned directly after migration id '049' (verified by index check in test)
    - packages/dashboard/src/db/interfaces/organizations-repository.ts `Organization` type has `readonly agentDisplayName: string | null;` field
    - The interface `OrganizationsRepository` has a new method `updateOrgAgentDisplayName(id: string, displayName: string | null): Promise<void>;`
    - packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts `getOrg` SELECT includes the column `agent_display_name` AND maps it to `agentDisplayName: row.agent_display_name ?? null` in the returned object
    - The repo exports `updateOrgAgentDisplayName(id, displayName)` implementing `UPDATE organizations SET agent_display_name = ? WHERE id = ?` with parameterized arguments (NO string concatenation — SQL injection guard)
    - `npx vitest run tests/db/migrations.test.ts tests/repositories/organizations-repository.test.ts` from packages/dashboard exits 0 with all 10 tests passing
    - `npx tsc --noEmit` from packages/dashboard reports zero errors
    - Regression: `npx vitest run tests/repositories/` passes (no repository test broken by the Organization type widening)
  </acceptance_criteria>
  <action>
    1. Append migration to `packages/dashboard/src/db/sqlite/migrations.ts` AFTER the 049 entry:
       ```typescript
       {
         id: '050',
         name: 'agent-display-name',
         sql: `ALTER TABLE organizations ADD COLUMN agent_display_name TEXT;`,
       },
       ```
       Note: SQLite's `ALTER TABLE ADD COLUMN` is idempotent only via the migration runner's applied-id tracking (not `IF NOT EXISTS` — SQLite does not support that for ADD COLUMN). Verify the migrations.ts runner at lines ~50-63 tracks applied ids in a `schema_migrations` table and skips if present. If it does, the ALTER runs exactly once.
    2. Extend `packages/dashboard/src/db/interfaces/organizations-repository.ts`:
       - Add `readonly agentDisplayName: string | null;` to the `Organization` interface.
       - Add `updateOrgAgentDisplayName(id: string, displayName: string | null): Promise<void>;` to the `OrganizationsRepository` interface.
    3. Extend `packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts`:
       - Update every `SELECT ... FROM organizations` query (especially the one in `getOrg` and `listOrgs`) to include `agent_display_name` in the column list.
       - Update the row-to-Organization mapper to populate `agentDisplayName: row.agent_display_name ?? null`.
       - Add new method:
         ```typescript
         async updateOrgAgentDisplayName(id: string, displayName: string | null): Promise<void> {
           this.db.prepare('UPDATE organizations SET agent_display_name = ? WHERE id = ?').run(displayName, id);
         }
         ```
         (Use whatever driver API the other updateOrg* methods use — this repo probably uses better-sqlite3's `.prepare().run()` or the project's wrapper.)
    4. Run `npx vitest run tests/db/migrations.test.ts tests/repositories/organizations-repository.test.ts` — all 10 tests pass.
    5. Run `npx vitest run tests/repositories/` to catch any other-repo regressions.
    6. Run `npx tsc --noEmit` — zero errors.
    7. Commit `feat(32-03): GREEN — migration 050 adds agent_display_name + OrganizationsRepository roundtrip (D-14)`.
  </action>
  <verify>
    <automated>cd /root/luqen/packages/dashboard &amp;&amp; npx vitest run tests/db/ tests/repositories/organizations-repository.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - Migration 050 appended; runner skips on re-run via applied-id tracking
    - Organization interface has agentDisplayName; repo updateOrgAgentDisplayName implemented
    - 10 tests green; no regression in other repo tests; tsc clean
    - GREEN commit landed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → updateOrgAgentDisplayName | Unvalidated string arrives at repo layer — caller (Plan 08 route) does zod validation first |
| DB schema migration → production DB | Schema change on a live table — must not lock, must not lose data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Tampering | SQL injection through displayName | mitigate | Use parameterized query `UPDATE ... WHERE id = ?` — NEVER string-interpolate the value. Test 7 regression-asserts a roundtrip of `"'; DROP TABLE organizations; --"` (pass as a malicious string and verify it's stored literally, NOT executed). Add Test 11 in Task 1 if budget allows |
| T-32-03-02 | Denial of Service | ALTER TABLE lock on large organizations table | accept | SQLite's ALTER TABLE ADD COLUMN is O(1) metadata-only (no row rewrite) — safe even on millions of rows. Document here; no runtime impact expected |
| T-32-03-03 | Tampering | Migration re-run applies ADD COLUMN twice | mitigate | Migration runner tracks applied ids in `schema_migrations`; Test 4 asserts idempotent double-run. If the runner does NOT track ids, this plan must first fix the runner — inspect migrations.ts lines 50-63 in Task 2 read-first step |
| T-32-03-04 | Information Disclosure | displayName contains PII (cross-tenant exposure if listOrgs leaks across orgs) | accept | listOrgs is admin-gated globally; per-org-to-per-org isolation handled by the route layer (Plan 08). This plan exposes the field uniformly; no info disclosure net-new from the migration itself |
</threat_model>

<verification>
- [ ] `grep -c "id: '050'" packages/dashboard/src/db/sqlite/migrations.ts`: exactly 1
- [ ] `grep 'agent_display_name' packages/dashboard/src/db/sqlite/migrations.ts`: at least 1 hit inside the 050 migration
- [ ] `grep 'agentDisplayName' packages/dashboard/src/db/interfaces/organizations-repository.ts`: at least 2 hits (type field + method signature)
- [ ] `grep -n 'updateOrgAgentDisplayName' packages/dashboard/src/db/sqlite/repositories/organizations-repository.ts`: at least 1 hit (the implementation)
- [ ] Tests 1-10 GREEN via `npx vitest run tests/db/migrations.test.ts tests/repositories/organizations-repository.test.ts`
- [ ] `npx tsc --noEmit` from packages/dashboard: 0 errors
</verification>

<success_criteria>
- Migration 050 lands in the canonical position (after 049)
- Organization type and repo expose agentDisplayName read + write
- Re-running migrations is safe (idempotent)
- Plans 04 and 08 are unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/32-agent-service-chat-ui/32-03-SUMMARY.md`.
</output>
