---
phase: 31.2-mcp-access-control-refinement
plan: 04
status: complete
started: 2026-04-19T16:30:00Z
completed: 2026-04-19T20:40:00Z
---

# Plan 31.2-04 Summary — Admin UI + Revoke Defense-in-Depth

## Scope delivered

Closed smoke gap **G1** from Phase 31.1 (org admins blocked from `/admin/clients`) and delivered the full D-20 revoke posture including bullet 3 (verifier-side revoked-client check) so nothing carries over to Phase 31.3.

Two tasks:

- **Task 1** — org-scoped DCR visibility in `GET /admin/clients` + template columns (Revoked + Org) + registrant-org-name resolution via SQL JOIN
- **Task 2** — cross-org revoke defense with audit trail + soft-revoke cascade (DELETE → UPDATE `revoked_at` + refresh-token rotation) + D-20 bullet 3 mcp/middleware client-status check

## Execution notes

Wave 3 was originally dispatched as a single `gsd-executor` worktree agent, but that agent hit its per-session usage limit after committing **only Task 1 RED tests** (commit `caca339`). The orchestrator resumed inline in the main session, implementing the GREEN pass + Task 2 tests + remaining code across 3 additional commits:

| Commit | Scope |
|--------|-------|
| `caca339` | Task 1 RED — 13 failing tests for visibility + Revoked/Org columns + cross-org revoke defense + audit + Repo Test 7 (committed by partial worktree agent) |
| `8b67da3` | Task 1 GREEN + Task 2 router/repo — `findByOrg` JOIN projection, `revoke()` soft-delete + refresh-chain cascade, `/admin/clients` preHandler relaxed to `admin.system OR admin.org`, cross-org revoke defense + audit-log, template Revoked + Org columns |
| `c64cc3e` | Task 2 step 3 — `mcp/middleware.ts` D-20 bullet 3 post-JWT revoked-client check + back-compat for pre-31.2 tokens lacking `client_id` + 4 new middleware-revoke tests |
| `1ccfb55` | Update two pre-existing Phase 31.1 DCR tests for the D-19 preHandler change + D-20 soft-revoke semantic (called out in T-31.2-04-05) |

## Files modified

### Source

- `packages/dashboard/src/db/interfaces/oauth-client-repository.ts` — add `OauthClient.registrantOrgName?: string` (D-24); update `revoke()` docstring to reflect soft-delete semantic
- `packages/dashboard/src/db/sqlite/repositories/oauth-client-repository.ts` — `findByOrg` JOIN projects `organizations.name` as `registrant_org_name`; `revoke()` rewritten from `DELETE` to transactional `UPDATE revoked_at IS NULL` guard + cascade `UPDATE oauth_refresh_tokens SET rotated = 1`
- `packages/dashboard/src/routes/admin/clients.ts` — GET `/admin/clients` preHandler relaxed to `admin.system OR admin.org` (dropped `compliance.view`); admin.system uses `listAll`, admin.org uses `findByOrg(currentOrgId)`; `listAll` rows get org name via `getUserOrgs` fallback, `'—'` for NULL-registrant orphans; `canRevoke` gates on `revoked_at IS NULL`; new `showOrgColumn: isGlobalAdmin` passed to view; POST revoke handler rewritten with cross-org check + `agent_audit_log` append on denial
- `packages/dashboard/src/views/admin/clients.hbs` — new `Revoked` column (shows date badge or `—`); conditional `Org` column visible only to admin.system; revoke button hidden when row is revoked; `hx-confirm` copy updated to "MCP sessions are blocked immediately on the next tool call" (D-20)
- `packages/dashboard/src/mcp/middleware.ts` — `McpTokenPayload.client_id?` added; `McpAuthPreHandlerOptions.storage.oauthClients` added; post-`verifyToken`, pre-RBAC check rejects tokens whose owning client has `revokedAt !== null` with 401 + `WWW-Authenticate: Bearer ..., error="invalid_token", error_description="client_revoked"`; silent-skip when `client_id` absent (D-13 back-compat) or when client row not found

### Tests

- `packages/dashboard/tests/routes/admin/clients.test.ts` — NEW (550 lines, 13 tests from RED commit): Tests 1-6 (GET visibility + Revoked/Org columns + hide-revoke-when-revoked) + Tests A-E (revoke defense + cross_org_revoke_attempt audit entries)
- `packages/dashboard/tests/repositories/oauth-client-repository.test.ts` — RED: Test 7 (findByOrg `registrantOrgName` from JOIN); updated pre-existing `revoke` test to assert soft-revoke semantic (row persists with `revokedAt !== null`); new idempotency test for double-revoke
- `packages/dashboard/tests/mcp/middleware-revoke.test.ts` — NEW: Tests H, I, J, K (revoked reject; active pass; no-client_id back-compat; null-row back-compat)
- `packages/dashboard/tests/mcp/middleware.test.ts` — extend `makeStubStorage` helper to default an `oauthClients.findByClientId` stub that returns null (existing tests never set `client_id` so the revoke branch is silent-skipped)
- `packages/dashboard/tests/routes/admin/clients-dcr.test.ts` — update Phase 31.1 Test 7 (soft-revoke semantic) and Test 8 (viewer 403 per D-19)

## Key decisions honoured

| Decision | Implementation |
|----------|---------------|
| **D-19** org-scoped visibility | preHandler `admin.system OR admin.org`; `findByOrg` for admin.org branch; NULL-registrant rows admin.system-only |
| **D-20 bullet 1** soft revoke | `UPDATE revoked_at` with `IS NULL` idempotency guard |
| **D-20 bullet 2** cascade | Same transaction rotates all live refresh tokens for the client |
| **D-20 bullet 3** verifier check | `mcp/middleware.ts` post-JWT lookup; immediate 401 on revoked client — delivered in-plan, NO carry-over to 31.3 |
| **D-21** cross-org audit | `agent_audit_log` row `tool_name='admin.clients.cross_org_revoke_attempt'`, no de-dup |
| **D-24** Revoked + Org columns | HBS template; Org column conditional on `showOrgColumn` (admin.system only); registrant org name from SQL JOIN, userId fallback only for listAll rows |
| **D-13** back-compat | Pre-31.2 tokens lacking `client_id` claim pass middleware silently; null client row also passes (treated as stale/back-compat) |

## Verification

### Scoped tests — all green

- `tests/routes/admin/clients.test.ts` — 13 tests (Tests 1-6 + A-E) all pass
- `tests/repositories/oauth-client-repository.test.ts` — 29 tests (findByOrg + recordRegistrationUser + soft-revoke + idempotency + Test 7) all pass
- `tests/mcp/middleware-revoke.test.ts` — 4 tests (H, I, J, K) all pass
- `tests/mcp/middleware.test.ts` — existing tests unchanged, all pass after helper update
- `tests/routes/admin/clients-dcr.test.ts` — 5 tests (Tests 6-9) all pass after D-19/D-20 update

### Grep guards from plan

All plan `<done>` grep guards pass:

- `findByOrg` in `routes/admin/clients.ts` ✓
- `registrantOrgName` in both interfaces + sqlite impl ✓
- `registrant_org_name` in sqlite impl ✓
- `showOrgColumn` in route + hbs ✓
- `Revoked` + `revokedAtDisplay` in hbs ✓
- `cross_org_revoke_attempt` in route ✓
- `storage.agentAudit.append` in route ✓
- `UPDATE oauth_clients_v2 SET revoked_at` + `UPDATE oauth_refresh_tokens` in sqlite impl ✓
- `client_id`, `client_revoked`, `findByClientId(payload.client_id)` in middleware ✓

### Pre-existing test updates (T-31.2-04-05)

Two Phase 31.1 tests in `tests/routes/admin/clients-dcr.test.ts` relied on the now-removed behaviours:

- Test 7 expected `findByClientId` to return `null` post-revoke → updated to assert `revokedAt !== null`
- Test 8 expected viewer with `compliance.view` to load `/admin/clients` (200) → updated to assert 403 per D-19 preHandler change

Both updates are additive-at-intent (the test intent is preserved; only the assertion target changes to match the new D-19/D-20 semantics).

## Success criteria traceability

| SC | Met by |
|----|--------|
| SC1 — user lacking `mcp.use` sees gate | Plan 02 |
| SC2 — users with `mcp.use` in one org see only that org's tools after switching | Plan 02 + 03 |
| SC3 — Org admin can revoke their org's DCR clients; system admin can revoke any | **Plan 04** (this) — D-19 + D-21 |
| SC4 — services emit `WWW-Authenticate` header on 401 | Plan 05 |
| SC5 — tool visibility follows real RBAC | Plan 03 |
| SC6 — Phase 31.1 smoke still passes | Non-regression protected by plan T-31.2-04-05 test updates + middleware back-compat (D-13) |

## SMOKE-CHECKLIST.md delta

- **Step 9 (admin revoke kills session)** — previously described as "within ~1h access-token TTL". New timing: **immediately on next tool call** via D-20 bullet 3 middleware check. (Copy update to SMOKE-CHECKLIST.md deferred — not load-bearing; noted here.)
- **Step 10 (rotate + overlap)** — unchanged. Middleware revoke check runs orthogonally to JWKS verification; rotated-key tokens for non-revoked clients still pass.

## Deviations

1. **Task 1 step 3 `listAll` fallback** — plan suggested an inline `resolveRegistrantOrgName` helper. Implemented inline in the `dcrClients.map` closure instead of as a separate function — less abstraction for a single-callsite helper.
2. **Test K added** — not in the plan's Tests H/I/J list, but added to lock in the "null client row pass-through" back-compat path that the middleware explicitly handles.
3. **`clients-dcr.test.ts` Test 8 rewrite** — plan didn't specify exact updates for the viewer-path tests; rewrote to match the D-19 preHandler semantic (viewer-with-only-`compliance.view` → 403) rather than preserving the original "viewer sees own" test (which is no longer achievable under D-19).
