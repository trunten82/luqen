---
phase: 32.1-agent-chat-fixes
plan: all (01-09 delivered inline as a rolling session)
subsystem: agent/uat-fixes
tags: [uat, hotfixes, ci-green, multimodal, global-admin, mcp-bridge, markdown, mermaid]

# Dependency graph
requires:
  - phase: 32 (AgentService + chat UI is the surface this phase patches)
provides:
  - Global-admin chat (synthetic __admin__:<userId> namespace)
  - In-process MCP tool dispatcher wired via mcp-bridge
  - Toolcatalog surfacing real descriptions + inputSchemas to the LLM
  - Markdown + GFM + mermaid multimodal rendering in the drawer
  - "New chat" control + localStorage conversationId persistence across reloads
  - Reactive display-name updates via HTMX Trigger
  - Audit-friendly persisted assistant(tool_calls) row before dispatch
  - CI green across dashboard, llm, compliance, core
affects: [packages/dashboard/src/routes/agent.ts, packages/dashboard/src/server.ts, packages/dashboard/src/agent/*, packages/core/src/mcp/*, packages/llm/src/prompts/*, packages/llm/src/providers/ollama.ts, packages/compliance/src/engine/proposals.ts]

# Tech tracking
tech-stack:
  added:
    - "marked + DOMPurify (vendored UMD — safe GFM rendering)"
    - "mermaid (vendored UMD — strict securityLevel, SVG via DOMParser)"
  patterns:
    - "Core/mcp runInToolContext helper — in-process ALS re-entry for agent dispatch"
    - "Synthetic admin orgId unwrap (__admin__:<userId> → '') for cross-org tool reads"
    - "HX-Trigger-driven drawer updates (no full-page refresh on settings save)"
    - "assistant(tool_calls) persisted BEFORE dispatch — matches Ollama/OpenAI/Anthropic message order"

# Files created/modified
key-files:
  created:
    - packages/dashboard/src/agent/mcp-bridge.ts
  modified:
    - packages/dashboard/src/routes/agent.ts
    - packages/dashboard/src/agent/agent-service.ts
    - packages/dashboard/src/server.ts
    - packages/dashboard/src/static/agent.js
    - packages/dashboard/src/static/style.css
    - packages/dashboard/src/views/partials/agent-drawer.hbs
    - packages/dashboard/src/views/layouts/main.hbs
    - packages/core/src/mcp/http-plugin.ts
    - packages/core/src/mcp/index.ts
    - packages/llm/src/providers/ollama.ts
    - packages/llm/src/api/routes/capabilities-exec.ts
    - packages/llm/src/prompts/agent-system.ts
    - packages/compliance/src/engine/proposals.ts (acknowledge metadata skip; surfaced by same UAT session)
    - packages/dashboard/src/routes/admin/organizations.ts (HX-Trigger for display name)
    - packages/dashboard/src/routes/admin/proposals.ts + view (pending/resolved split)
    - 6× locale JSON (new i18n keys for new-chat, proposals headings, nav labels)
---

# Phase 32.1 Summary — Agent Chat UAT Gap Closure

10 UAT gaps found during the Phase 32 live UAT session on lxc-luqen were closed across 9 plans (08 was quality-only, executed last). The chat stack went from "drawer renders" to "agent answers user-org questions with tool data and renders markdown + diagrams" end-to-end. CI cleared 8 pre-existing stale test failures on the same branch.

## Plans Delivered

| Plan | Gap | Result |
|------|-----|--------|
| 09 | CI red on master for weeks | 4 test files updated — scope fixtures + returnTo assertion; CI green |
| 01 | Global admin 400 `no_org_context` | Synthetic `__admin__:<userId>` namespace in /agent/* routes |
| 02 | Tool dispatcher had `tools:[]` — every call returned `unknown_tool` | mcp-bridge extracts `_registeredTools` into ToolDispatcher manifest + AgentService toolCatalog; runInToolContext ALS re-entry |
| 03 | No way to reset a conversation | "New chat" button + localStorage clear |
| 04 | Display-name change needed hard refresh | HX-Trigger + agent.js listener updates `#agent-display-name` in place |
| 05 | Badges overflowed on capabilities tab | CSS `flex-wrap: wrap` + `overflow-wrap: anywhere` |
| 06 | Markdown rendered as literal `**bold**` / tables collapsed | marked + DOMPurify + mermaid vendored; `.agent-msg__body` table CSS overrides the mobile `.data-table` card-stack rule |
| 07 | Org settings page not in nav | "Agent settings" link on org members page |
| 08 | Assistant tool_calls row synthesised from tool rows (lossy) | Persist proper `role='assistant'` row with `toolCallJson` BEFORE dispatch |

## Verification Results

- Dashboard suite: 3008 passing + 40 skipped + 3 todo (zero failures after Plan 09).
- Compliance suite: 571 passing.
- LLM suite: 327 passing.
- Core suite: 251 passing.
- CI on master: green since commit `e0e533f`.
- Live UAT on lxc-luqen: drawer works for global admin AND org admin, tool calls return real data, markdown + mermaid render correctly, reset/reload flows all honour conversationId persistence.

## Deferred / Out of Scope

- Multiple-conversation listing/switching UI (future phase).
- tiktoken-accurate token counting (phase 33-03 uses char/4 heuristic).
- Larger agent model assignment is a config change, not a code change — current `ministral-3:3b-cloud` is intentional for dev.

---

*Phase 32.1 completed 2026-04-24.*
