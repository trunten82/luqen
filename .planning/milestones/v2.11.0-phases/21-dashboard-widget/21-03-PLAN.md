---
phase: 21-dashboard-widget
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - packages/dashboard/src/views/partials/brand-score-panel.hbs
  - packages/dashboard/src/server.ts
  - packages/dashboard/src/i18n/locales/en.json
  - packages/dashboard/src/i18n/locales/fr.json
  - packages/dashboard/src/i18n/locales/it.json
  - packages/dashboard/src/i18n/locales/pt.json
  - packages/dashboard/src/i18n/locales/de.json
  - packages/dashboard/src/i18n/locales/es.json
autonomous: true
requirements:
  - BUI-03

must_haves:
  truths:
    - "All hardcoded English strings in brand-score-panel.hbs replaced with {{t}} helpers"
    - "All hardcoded English strings in brand-score-widget.hbs use {{t}} helpers (verified — Plan 21-01 already uses them)"
    - "All hardcoded English strings in branding-mode-toggle.hbs use {{t}} helpers (verified — Phase 19 already uses them)"
    - "en.json has all new i18n keys for Phase 20 panel strings"
    - "All 5 non-English locale files have placeholder entries for every new key"
    - "grep finds zero hardcoded multi-word English strings in the swept partials"
    - "unscorable-reason-label helper in server.ts uses i18n keys instead of hardcoded English"
  artifacts:
    - path: "packages/dashboard/src/views/partials/brand-score-panel.hbs"
      provides: "i18n-ified panel partial"
    - path: "packages/dashboard/src/i18n/locales/en.json"
      provides: "All new keys for Phases 19/20/21"
    - path: "packages/dashboard/src/i18n/locales/fr.json"
      provides: "French placeholder entries"
  key_links:
    - from: "packages/dashboard/src/views/partials/brand-score-panel.hbs"
      to: "packages/dashboard/src/i18n/locales/en.json"
      via: "{{t}} helper lookups"
      pattern: '\\{\\{t "'
---

<objective>
Cross-phase i18n sweep: audit and fix all Phase 19/20/21 .hbs partials for hardcoded English strings, add i18n keys to en.json, add placeholder entries to fr/it/pt/de/es.json, and convert the unscorable-reason-label helper to use i18n keys.

Purpose: Delivers BUI-03 — all new UI strings from Phases 19/20/21 translated via {{t}} helpers across all 6 locales.

Output: Hardcoded-string-free partials, complete en.json keys, placeholder entries in 5 other locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-dashboard-widget/21-01-SUMMARY.md

@packages/dashboard/src/views/partials/brand-score-panel.hbs
@packages/dashboard/src/views/partials/brand-score-widget.hbs
@packages/dashboard/src/views/admin/partials/branding-mode-toggle.hbs
@packages/dashboard/src/server.ts
@packages/dashboard/src/i18n/locales/en.json
@packages/dashboard/src/i18n/locales/fr.json
@packages/dashboard/src/i18n/locales/de.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 19/20/21 partials and identify all hardcoded English strings</name>
  <files>
    packages/dashboard/src/views/partials/brand-score-panel.hbs
    packages/dashboard/src/server.ts
  </files>
  <read_first>
    packages/dashboard/src/views/partials/brand-score-panel.hbs
    packages/dashboard/src/views/partials/brand-score-widget.hbs
    packages/dashboard/src/views/admin/partials/branding-mode-toggle.hbs
    packages/dashboard/src/server.ts (lines 452-462 — unscorable-reason-label helper)
    packages/dashboard/src/i18n/locales/en.json
  </read_first>
  <action>
**Audit findings (pre-computed from planning read):**

**branding-mode-toggle.hbs (Phase 19):** CLEAN. All strings already use `{{t "admin.org.brandingMode.*"}}` keys. No fixes needed.

**brand-score-widget.hbs (Phase 21):** CLEAN (if Plan 21-01 is followed). All strings use `{{t "home.brandScore*"}}` keys. No fixes needed.

**brand-score-panel.hbs (Phase 20):** NEEDS FIXES. The following hardcoded English strings must be replaced:

| Line | Hardcoded String | Replacement Key |
|------|------------------|-----------------|
| 35 | `Good` | `{{t "reportDetail.brandScoreGood"}}` |
| 35 | `Fair` | `{{t "reportDetail.brandScoreFair"}}` |
| 35 | `Needs work` | `{{t "reportDetail.brandScoreNeedsWork"}}` |
| 42 | `First brand score for this site` | `{{t "reportDetail.brandScoreFirst"}}` |
| 46 | `vs previous scan` (after +delta) | `{{t "reportDetail.brandScoreDeltaUp"}}` |
| 49 | `vs previous scan` (after -delta) | `{{t "reportDetail.brandScoreDeltaDown"}}` |
| 51 | `No change vs previous scan` | `{{t "reportDetail.brandScoreDeltaFlat"}}` |
| 55 | `No change vs previous scan` (else branch) | `{{t "reportDetail.brandScoreDeltaFlat"}}` |
| 63 | `of ... issues are on brand elements` | `{{t "reportDetail.brandScoreIssueCounter" brandRelated=brandRelatedCount total=totalIssues}}` |
| 70 | `Color contrast` | `{{t "reportDetail.brandScoreColorContrast"}}` |
| 87 | `Typography` | `{{t "reportDetail.brandScoreTypography"}}` |
| 104 | `Components` | `{{t "reportDetail.brandScoreComponents"}}` |
| 121 | `Score based on ... dimensions` | `{{t "reportDetail.brandScoreDimensions" count=...}}` (complex — see below) |
| 133 | `Brand score not available for this scan` | `{{t "reportDetail.brandScoreNotAvailable"}}` |
| 134 | `Brand scoring was introduced in v2.11.0` | `{{t "reportDetail.brandScoreIntroduced"}}` |

**unscorable-reason-label helper in server.ts (line 452-461):** Has 6 hardcoded English labels. These should ideally use i18n keys, but the helper runs server-side without request context (no locale available). The pragmatic fix: leave these as-is for now since the helper is a fallback display — the {{t}} keys in the templates handle the primary UI path. The helper labels are only used inside the partial which has `{{unscorable-reason-label brandScore.reason}}` — these are effectively English defaults. Flag for future i18n work but do not block BUI-03 on this.

**Fix brand-score-panel.hbs** — replace all hardcoded strings. The updated file content:

Replace line 35 (the badge text):
```handlebars
          {{#if (gte brandScore.overall 85)}}{{t "reportDetail.brandScoreGood"}}{{else}}{{#if (gte brandScore.overall 70)}}{{t "reportDetail.brandScoreFair"}}{{else}}{{t "reportDetail.brandScoreNeedsWork"}}{{/if}}{{/if}}
```

Replace line 42:
```handlebars
          <span class="text-muted">{{t "reportDetail.brandScoreFirst"}}</span>
```

Replace line 46:
```handlebars
              <span class="text--success">&#9650; +{{brandDelta}} {{t "reportDetail.brandScoreDeltaUp"}}</span>
```

Replace line 49:
```handlebars
                <span class="text--error">&#9660; {{brandDelta}} {{t "reportDetail.brandScoreDeltaDown"}}</span>
```

Replace lines 51 and 55 (both identical):
```handlebars
                <span class="text-muted">&#9644; {{t "reportDetail.brandScoreDeltaFlat"}}</span>
```

Replace line 63:
```handlebars
        <strong>{{brandRelatedCount}}</strong> {{t "reportDetail.brandScoreIssueCounterOf"}} <strong>{{totalIssues}}</strong> {{t "reportDetail.brandScoreIssueCounterSuffix"}}
```

Replace line 70 (`Color contrast`):
```handlebars
          <span style="font-size:var(--font-size-sm);font-weight:500;">{{t "reportDetail.brandScoreColorContrast"}}</span>
```

Replace line 87 (`Typography`):
```handlebars
          <span style="font-size:var(--font-size-sm);font-weight:500;">{{t "reportDetail.brandScoreTypography"}}</span>
```

Replace line 104 (`Components`):
```handlebars
          <span style="font-size:var(--font-size-sm);font-weight:500;">{{t "reportDetail.brandScoreComponents"}}</span>
```

Replace line 121 (dimensions text):
```handlebars
        {{t "reportDetail.brandScoreDimensionsPrefix"}} {{#if (eq brandScore.coverage.contributingWeight 1)}}{{t "reportDetail.brandScoreDimensionsAll"}}{{else}}{{#if (gte brandScore.coverage.contributingWeight 0.5)}}{{t "reportDetail.brandScoreDimensions2of3"}}{{else}}{{t "reportDetail.brandScoreDimensions1of3"}}{{/if}}{{/if}}
```

Replace line 133:
```handlebars
      <p class="text-muted">{{t "reportDetail.brandScoreNotAvailable"}}</p>
```

Replace line 134:
```handlebars
      <p class="text-muted" style="font-size:var(--font-size-xs);margin-top:var(--space-xs);">{{t "reportDetail.brandScoreIntroduced"}}</p>
```

Also replace the `aria-label` text in the progress bars (lines 78, 95, 112) — currently `aria-label="Color contrast score"` etc. Replace with the i18n key values:
```handlebars
aria-label="{{t "reportDetail.brandScoreColorContrast"}}"
aria-label="{{t "reportDetail.brandScoreTypography"}}"
aria-label="{{t "reportDetail.brandScoreComponents"}}"
```

  </action>
  <verify>
    <automated>cd /root/luqen/packages/dashboard && grep -cE '"[A-Z][a-z]+\s+[a-z]' src/views/partials/brand-score-panel.hbs | grep -v '^0$' || echo "PASS: 0 hardcoded multi-word strings"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '[A-Z][a-z]+ [a-z]' packages/dashboard/src/views/partials/brand-score-panel.hbs` returns a count where only comments and HTML attribute values remain (no user-visible hardcoded English)
    - All `{{t "reportDetail.brandScore*"}}` calls reference keys that will be added in Task 2
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>brand-score-panel.hbs has zero hardcoded English strings in user-visible positions. All text uses {{t}} helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n keys to en.json + placeholder entries to 5 other locales</name>
  <files>
    packages/dashboard/src/i18n/locales/en.json
    packages/dashboard/src/i18n/locales/fr.json
    packages/dashboard/src/i18n/locales/it.json
    packages/dashboard/src/i18n/locales/pt.json
    packages/dashboard/src/i18n/locales/de.json
    packages/dashboard/src/i18n/locales/es.json
  </files>
  <read_first>
    packages/dashboard/src/i18n/locales/en.json (lines 1089-1200 for reportDetail section)
    packages/dashboard/src/i18n/locales/fr.json (first 20 lines — to understand structure)
    packages/dashboard/src/i18n/locales/de.json (first 20 lines)
  </read_first>
  <action>
**Step 1: Add keys to `en.json`.**

Inside the `"reportDetail"` object, add these keys (after the existing `"brandScore": "Brand Score"` key at line ~1193):

```json
    "brandScoreGood": "Good",
    "brandScoreFair": "Fair",
    "brandScoreNeedsWork": "Needs work",
    "brandScoreFirst": "First brand score for this site",
    "brandScoreDeltaUp": "vs previous scan",
    "brandScoreDeltaDown": "vs previous scan",
    "brandScoreDeltaFlat": "No change vs previous scan",
    "brandScoreIssueCounterOf": "of",
    "brandScoreIssueCounterSuffix": "issues are on brand elements",
    "brandScoreColorContrast": "Color contrast",
    "brandScoreTypography": "Typography",
    "brandScoreComponents": "Components",
    "brandScoreDimensionsPrefix": "Score based on",
    "brandScoreDimensionsAll": "all 3 dimensions",
    "brandScoreDimensions2of3": "2 of 3 dimensions",
    "brandScoreDimensions1of3": "1 of 3 dimensions",
    "brandScoreNotAvailable": "Brand score not available for this scan",
    "brandScoreIntroduced": "Brand scoring was introduced in v2.11.0"
```

Verify the `"home"` section already has the keys from Plan 21-01:
- `"brandScore"`, `"brandScoreNoData"`, `"brandScoreNoDataHint"`, `"brandScoreFirstScore"`, `"brandScoreDeltaUp"`, `"brandScoreDeltaDown"`, `"brandScoreDeltaFlat"`, `"brandScoreTrendLabel"`

If any are missing (Plan 21-01 not yet executed), add them now.

**Step 2: Add placeholder entries to fr.json, it.json, pt.json, de.json, es.json.**

For EACH of the 5 non-English locale files:

1. Read the file
2. Find (or create) the `"reportDetail"` section
3. Add placeholder entries for all new reportDetail.brandScore* keys with locale prefix:

For `fr.json`:
```json
    "brandScore": "[FR] Brand Score",
    "brandScoreGood": "[FR] Good",
    "brandScoreFair": "[FR] Fair",
    "brandScoreNeedsWork": "[FR] Needs work",
    "brandScoreFirst": "[FR] First brand score for this site",
    "brandScoreDeltaUp": "[FR] vs previous scan",
    "brandScoreDeltaDown": "[FR] vs previous scan",
    "brandScoreDeltaFlat": "[FR] No change vs previous scan",
    "brandScoreIssueCounterOf": "[FR] of",
    "brandScoreIssueCounterSuffix": "[FR] issues are on brand elements",
    "brandScoreColorContrast": "[FR] Color contrast",
    "brandScoreTypography": "[FR] Typography",
    "brandScoreComponents": "[FR] Components",
    "brandScoreDimensionsPrefix": "[FR] Score based on",
    "brandScoreDimensionsAll": "[FR] all 3 dimensions",
    "brandScoreDimensions2of3": "[FR] 2 of 3 dimensions",
    "brandScoreDimensions1of3": "[FR] 1 of 3 dimensions",
    "brandScoreNotAvailable": "[FR] Brand score not available for this scan",
    "brandScoreIntroduced": "[FR] Brand scoring was introduced in v2.11.0"
```

Repeat the same pattern for it.json (`[IT]`), pt.json (`[PT]`), de.json (`[DE]`), es.json (`[ES]`).

Also add the `"home"` section brand score keys to all 5 locales with the same prefix pattern:
```json
    "brandScore": "[XX] Brand Score",
    "brandScoreNoData": "[XX] No brand scores yet",
    "brandScoreNoDataHint": "[XX] Brand scores will appear here after your first scan with a linked brand guideline.",
    "brandScoreFirstScore": "[XX] First brand score",
    "brandScoreDeltaUp": "[XX] vs previous",
    "brandScoreDeltaDown": "[XX] vs previous",
    "brandScoreDeltaFlat": "[XX] No change",
    "brandScoreTrendLabel": "[XX] Brand score trend"
```

Where `[XX]` is the locale code in uppercase (`[FR]`, `[IT]`, `[PT]`, `[DE]`, `[ES]`).

**Step 3: Run the verification grep.**

```bash
grep -rE '"[A-Z][a-z]+\s+[a-z]' \
  packages/dashboard/src/views/partials/brand-score-panel.hbs \
  packages/dashboard/src/views/partials/brand-score-widget.hbs \
  packages/dashboard/src/views/admin/partials/branding-mode-toggle.hbs \
  | grep -v '{{!' | grep -v 'aria-' | grep -v 'class=' | grep -v 'style=' | grep -v 'hx-'
```

This should return 0 matches (no hardcoded multi-word English strings outside comments and HTML attributes).

  </action>
  <verify>
    <automated>cd /root/luqen/packages/dashboard && grep -cE '{{t "reportDetail\.brandScore' src/views/partials/brand-score-panel.hbs; echo "---"; npm run lint; echo "---"; npx vitest run tests/views/report-detail.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "brandScoreGood" packages/dashboard/src/i18n/locales/en.json` returns 1
    - `grep -c "brandScoreGood" packages/dashboard/src/i18n/locales/fr.json` returns 1
    - `grep -c "brandScoreGood" packages/dashboard/src/i18n/locales/it.json` returns 1
    - `grep -c "brandScoreGood" packages/dashboard/src/i18n/locales/pt.json` returns 1
    - `grep -c "brandScoreGood" packages/dashboard/src/i18n/locales/de.json` returns 1
    - `grep -c "brandScoreGood" packages/dashboard/src/i18n/locales/es.json` returns 1
    - All 6 locale files are valid JSON (`node -e "JSON.parse(require('fs').readFileSync('...'))"` for each)
    - `npm run lint` exits 0
    - Existing report-detail tests pass (no regressions from panel i18n changes)
    - `grep -rE '"[A-Z][a-z]+ [a-z]' packages/dashboard/src/views/partials/brand-score-panel.hbs | grep -v '{{!' | grep -v 'class=' | wc -l` returns 0
  </acceptance_criteria>
  <done>All Phase 19/20/21 partials use {{t}} helpers for all user-visible strings. en.json has all keys. 5 other locales have placeholder entries for every new key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n keys -> template | Translation values auto-escaped by Handlebars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | S (Spoofing) | i18n locale files | accept | Locale files are server-side JSON loaded at boot; no user-supplied translations in v2.11.0 |
| T-21-06 | T (Tampering) | i18n keys | accept | JSON files committed to repo; no runtime editing path |
</threat_model>

<verification>
1. `grep -rE '"[A-Z][a-z]+ [a-z]' packages/dashboard/src/views/partials/brand-score-panel.hbs packages/dashboard/src/views/partials/brand-score-widget.hbs packages/dashboard/src/views/admin/partials/branding-mode-toggle.hbs | grep -v '{{!' | grep -v 'class=' | grep -v 'aria-' | grep -v 'hx-' | wc -l` — must be 0
2. All 6 locale files parse as valid JSON
3. `npm run lint` exits 0
4. `npx vitest run tests/views/report-detail.test.ts` — existing tests pass
</verification>

<success_criteria>
- Zero hardcoded multi-word English strings in any Phase 19/20/21 .hbs partial (outside comments and HTML attributes)
- en.json has all new keys for reportDetail.brandScore* and home.brandScore*
- fr/it/pt/de/es.json each have placeholder entries for every new key
- Existing report-detail test suite passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/21-dashboard-widget/21-03-SUMMARY.md`
</output>
