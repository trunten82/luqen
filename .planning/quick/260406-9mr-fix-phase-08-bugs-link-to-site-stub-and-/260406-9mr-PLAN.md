---
quick_id: 260406-9mr
description: "Fix phase-08 bugs: link-to-site stub and clone missing image_path"
task_count: 2
wave: 1
autonomous: true
---

# Quick Task: Fix Phase-08 Bugs

## Objective

Fix two bugs shipped in phase 08:
1. "Link to site" button on system-library-row.hbs hits a non-existent endpoint — replace with inline form that POSTs to existing `/admin/branding-guidelines/:id/sites`
2. `cloneSystemGuideline()` INSERT omits `image_path` — cloned guidelines lose logo

## Tasks

### Task 1: Fix clone missing image_path

**Files:**
- packages/dashboard/src/db/sqlite/repositories/branding-repository.ts
- packages/dashboard/tests/db/branding-repository-system.test.ts

**Action:**
- Add `image_path` to the `insertGuideline` prepared statement in `cloneSystemGuideline()`
- Pass `source.imagePath` into the run params
- Add test asserting cloned guideline preserves imagePath

**Verify:** `npx vitest run tests/db/branding-repository-system.test.ts`

### Task 2: Fix link-to-site button

**Files:**
- packages/dashboard/src/views/admin/partials/system-library-row.hbs
- packages/dashboard/tests/routes/admin-branding-guidelines-system-library.test.ts

**Action:**
- Replace the `hx-get="/admin/branding-guidelines/{{id}}/link-to-site"` button with an inline form containing a site URL input and a submit button that POSTs to `/admin/branding-guidelines/{{id}}/sites` with `{ siteUrl }`
- Use existing design system classes (btn, form-control)
- Add test for the link-to-site form rendering

**Verify:** `npx vitest run tests/routes/admin-branding-guidelines-system-library.test.ts`
