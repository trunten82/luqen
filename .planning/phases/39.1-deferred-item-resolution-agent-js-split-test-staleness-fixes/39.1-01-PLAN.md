---
phase: 39.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/tests/e2e/auth-flow-e2e.test.ts
  - packages/dashboard/tests/mcp/data-tools.test.ts
  - packages/dashboard/tests/mcp/admin-tools.test.ts
  - packages/dashboard/tests/mcp/http.test.ts
  - packages/dashboard/tests/e2e/agent-multi-step.e2e.test.ts
  - packages/dashboard/tests/db/migration-058-059.test.ts
  - packages/dashboard/tests/static/agent-actions-handlers.test.ts
autonomous: true
requirements:
  - DI-31.2-01
  - DI-32-01
  - DI-32-02
  - DI-32-03
  - DI-37-01
  - DI-38-01
  - DI-38-02b

must_haves:
  truths:
    - "tests/e2e/auth-flow-e2e.test.ts passes with returnTo query expectations"
    - "tests/mcp/{data-tools,admin-tools,http}.test.ts pass with corrected scope tier expectations"
    - "tests/e2e/agent-multi-step.e2e.test.ts E3 case passes with fetch passed through harness IIFE"
    - "tests/db/migration-058-059.test.ts passes with column-list reflecting migration 060 expires_at"
    - "tests/static/agent-actions-handlers.test.ts shareAssistant case passes covering ClipboardItem(Promise) + writeText fallback"
    - "Full dashboard test suite green (no regressions)"
  artifacts:
    - path: "packages/dashboard/tests/e2e/auth-flow-e2e.test.ts"
      provides: "Updated returnTo assertions per DI-31.2-01"
    - path: "packages/dashboard/tests/mcp/data-tools.test.ts"
      provides: "Scope expectation aligned to write-tier rule per DI-32-01"
    - path: "packages/dashboard/tests/mcp/admin-tools.test.ts"
      provides: "Scope expectation aligned to admin-tier rule per DI-32-02"
    - path: "packages/dashboard/tests/mcp/http.test.ts"
      provides: "Scope expectation aligned to write-tier rule per DI-32-03"
    - path: "packages/dashboard/tests/e2e/agent-multi-step.e2e.test.ts"
      provides: "Harness IIFE loader signature includes fetch (DI-37-01)"
    - path: "packages/dashboard/tests/db/migration-058-059.test.ts"
      provides: "Column-list assertion includes expires_at where appropriate (DI-38-01)"
    - path: "packages/dashboard/tests/static/agent-actions-handlers.test.ts"
      provides: "shareAssistant test exercises ClipboardItem(Promise) + writeText fallback (DI-38-02b)"
  key_links:
    - from: "tests/mcp/data-tools.test.ts"
      to: "packages/dashboard/src/mcp/middleware.ts"
      via: "scope tier enforcement comment reference"
      pattern: "scopes:\\s*\\[.*write.*\\]|scopes:\\s*\\[.*admin.*\\]"
    - from: "tests/e2e/agent-multi-step.e2e.test.ts"
      to: "tests/e2e/agent-history.e2e.test.ts"
      via: "shared IIFE loader signature pattern"
      pattern: "['\"]fetch['\"]"
---

<objective>
Bundle 5 test-staleness fixes promoted from Phase 39 TRIAGE.md into a single TDD pass with one commit per DI item. All fixes are in test files only; no production source code changes. Goal: full dashboard test suite green with no regressions.

Purpose: Close 7 deferred items (DI-31.2-01, DI-32-01, DI-32-02, DI-32-03, DI-37-01, DI-38-01, DI-38-02b) that block CI green and impede further agent-drawer work.
Output: 7 commits (one per DI), all test files updated, dashboard `npm test` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-deferred-item-resolution-agent-js-split-test-staleness-fixes/39.1-CONTEXT.md
@.planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md

@packages/dashboard/src/mcp/middleware.ts
@packages/dashboard/src/routes/auth.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix auth-flow-e2e returnTo assertions (DI-31.2-01)</name>
  <files>packages/dashboard/tests/e2e/auth-flow-e2e.test.ts</files>
  <read_first>
    - packages/dashboard/tests/e2e/auth-flow-e2e.test.ts (full file)
    - packages/dashboard/src/routes/auth.ts (login redirect emit logic)
    - .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md L98-103 (DI-31.2-01 scope)
  </read_first>
  <behavior>
    - Test "GET /home without auth redirects to /login": redirect target matches `/login?returnTo=%2Fhome`
    - Test "session is invalid after logout": redirect target matches `/login?returnTo=...` (or bare `/login` if logout flow does not preserve returnTo — verify against route source)
    - Both assertions tolerate query-string suffix without coupling to exact encoding edge cases
  </behavior>
  <action>
    Run failing tests first: `npx vitest run tests/e2e/auth-flow-e2e.test.ts` to confirm RED state. Update both assertions to expect `/login?returnTo=...` per the Phase 31.1 commit `4337c8d` middleware behaviour. Prefer regex match (e.g. `expect(redirect).toMatch(/^\/login(\?returnTo=.+)?$/)`) so future query-string additions don't re-break the test. Run again to confirm GREEN. Commit per DI-31.2-01.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/e2e/auth-flow-e2e.test.ts</automated>
  </verify>
  <done>auth-flow-e2e.test.ts: both returnTo cases pass; commit `fix(test): align auth-flow returnTo assertions to /login?returnTo (DI-31.2-01)` exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix mcp scope-filter test expectations (DI-32-01..03)</name>
  <files>packages/dashboard/tests/mcp/data-tools.test.ts, packages/dashboard/tests/mcp/admin-tools.test.ts, packages/dashboard/tests/mcp/http.test.ts</files>
  <read_first>
    - packages/dashboard/src/mcp/middleware.ts (scope tier enforcement — read carefully to determine which tier each failing tool requires)
    - packages/dashboard/tests/mcp/data-tools.test.ts (failing 2 cases)
    - packages/dashboard/tests/mcp/admin-tools.test.ts (failing 3 cases)
    - packages/dashboard/tests/mcp/http.test.ts (failing 1 case)
    - .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md L106-110 (DI-32-01..03 scope)
  </read_first>
  <behavior>
    - data-tools.test.ts cases that exercise `.create` (or write-tier) tools: scopes include `write` tier
    - admin-tools.test.ts cases for admin tools: scopes include `admin` tier
    - http.test.ts case for write-tier tool: scopes include `write` tier
    - Tests where read-tier is correct stay unchanged
    - Each updated scope array has an inline comment pointing to `src/mcp/middleware.ts` rule that requires that tier
  </behavior>
  <action>
    Run all three suites first: `npx vitest run tests/mcp/data-tools.test.ts tests/mcp/admin-tools.test.ts tests/mcp/http.test.ts` to confirm RED on the 6 cases. For each failing case, read the production rule in `src/mcp/middleware.ts` (or wherever scope tiers are defined for the tool) and update only the test fixture scope array to match the required tier. Add a one-line comment above each updated `scopes: [...]` line: `// requires write tier per src/mcp/middleware.ts:LINE`. DO NOT modify production code — tests are stale, rules are correct (per CONTEXT D-MCP-Scope decision). Run again to confirm GREEN. Commit per DI: one commit covering all three suites since they share root cause: `fix(test): align mcp scope expectations to write/admin tier (DI-32-01..03)`.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/mcp/data-tools.test.ts tests/mcp/admin-tools.test.ts tests/mcp/http.test.ts</automated>
  </verify>
  <done>All 6 previously-failing MCP scope cases pass; production middleware.ts unchanged (verify with git diff); commit exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Promote agent-multi-step.e2e fetch loader patch to permanent (DI-37-01)</name>
  <files>packages/dashboard/tests/e2e/agent-multi-step.e2e.test.ts</files>
  <read_first>
    - packages/dashboard/tests/e2e/agent-multi-step.e2e.test.ts (current state — CI patch may already be in place per CONTEXT)
    - packages/dashboard/tests/e2e/agent-history.e2e.test.ts:262 (canonical IIFE loader pattern with fetch)
    - .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md L112-117 (DI-37-01 scope)
  </read_first>
  <behavior>
    - IIFE loader call site signature includes `'fetch'` parameter and passes `(win as any).fetch` as argument
    - E3 case ("multi-step tool dispatch with chip strip") completes without `ReferenceError: fetch is not defined`
    - E1, E2, E4 cases continue to pass (no regressions)
    - Inline comment near the loader references Phase 37 cross-cutting (history panel reload after stream done) so future readers understand why fetch is needed
  </behavior>
  <action>
    Inspect current loader signature. If the CI patch already aligns with `agent-history.e2e.test.ts:262` shape, verify the comment is present and add it if missing. If only a beforeEach stub was added (rather than threading fetch through the IIFE), refactor to use the canonical IIFE-arg pattern from agent-history.e2e.test.ts so the fix matches the established pattern and survives future refactors. Run `npx vitest run tests/e2e/agent-multi-step.e2e.test.ts` to confirm green. Commit `fix(test): thread fetch through agent-multi-step IIFE loader (DI-37-01)`.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/e2e/agent-multi-step.e2e.test.ts</automated>
  </verify>
  <done>All E1–E4 cases pass; loader signature matches agent-history.e2e.test.ts pattern; commit exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Promote migration-058-059 column-list patch to permanent (DI-38-01)</name>
  <files>packages/dashboard/tests/db/migration-058-059.test.ts</files>
  <read_first>
    - packages/dashboard/tests/db/migration-058-059.test.ts (current assertion state)
    - packages/dashboard/db/migrations/059*.ts (or .sql — agent_share_links creation)
    - packages/dashboard/db/migrations/060*.ts (adds expires_at)
    - packages/dashboard/tests/db/migration-060.test.ts (if exists — confirm no overlap)
    - .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md L126-131 (DI-38-01 scope)
  </read_first>
  <behavior>
    - Test correctly asserts the column set as it exists AT the migration boundary it claims to verify
    - If the test description says "migration 059 creates table with expected columns", the assertion lists migration-059 columns only (no expires_at)
    - If the test runs migrations through 060 before asserting, it lists migration-060 columns (includes expires_at)
    - Restructure so it's unambiguous which migration's state is under test (per CONTEXT D-Mig-058-059 decision)
  </behavior>
  <action>
    Read both migrations 059 and 060 to determine the column set at each boundary. Read the current test assertion to determine which migration boundary it intends to verify. Two valid resolutions:
    (a) If the test runs migrations up through 060 before asserting (CI patch shape), keep `expires_at` in the expected list and rename the `it()` description to "creates agent_share_links table with the expected post-migration-060 columns" for clarity.
    (b) If the test stops at migration 059, expected columns must NOT include `expires_at` and the CI patch was wrong; revert to the 059-state column list.
    Choose (a) or (b) based on how the test arranges (which migrations it runs). Add an inline comment naming the migration boundary. Run `npx vitest run tests/db/migration-058-059.test.ts tests/db/migration-060.test.ts` to confirm both green. Commit `fix(test): pin migration-058-059 column assertion to its actual migration boundary (DI-38-01)`.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/db/migration-058-059.test.ts tests/db/migration-060.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>migration-058-059 test passes; migration-060 test (if exists) passes; assertion's migration boundary is explicit in code/comment; commit exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Verify shareAssistant test covers ClipboardItem(Promise) + writeText fallback (DI-38-02b)</name>
  <files>packages/dashboard/tests/static/agent-actions-handlers.test.ts</files>
  <read_first>
    - packages/dashboard/tests/static/agent-actions-handlers.test.ts (focus: case "12. shareAssistant" near L387-400)
    - packages/dashboard/src/static/agent.js (locate share handler — calls navigator.clipboard.write with new ClipboardItem(Promise) and falls back to writeToClipboard / clipboard.writeText in JSDOM)
    - .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md L133-138 (DI-38-02b scope)
  </read_first>
  <behavior>
    - shareAssistant test exercises the ClipboardItem(Promise) path AND the writeText fallback path explicitly (split into 2 test cases if needed per CONTEXT decision)
    - Mock provides `globalThis.ClipboardItem` constructor accepting `{ [mime]: Promise<Blob> }`
    - Mock provides `navigator.clipboard.write([items])` resolving Promise
    - Fallback path mocks only `navigator.clipboard.writeText` (no ClipboardItem) and asserts handler degrades to writeText
    - Each case has clear arrange/act/assert and a one-line comment naming which clipboard path it covers
  </behavior>
  <action>
    Run `npx vitest run tests/static/agent-actions-handlers.test.ts -t "shareAssistant"` to confirm current state. If the CI patch made it pass via writeText only and the ClipboardItem path is silently dead, split into two cases:
      - "shareAssistant — uses ClipboardItem(Promise) when supported"
      - "shareAssistant — falls back to writeText when ClipboardItem unavailable"
    Each case sets up its own clipboard mock and asserts the right path was taken. If the existing patch already covers both, add a comment naming the paths and verify the assertions actually distinguish the two paths (not just both happen to succeed). Run all `tests/static/` to confirm no regressions. Commit `fix(test): cover both clipboard paths in shareAssistant case (DI-38-02b)`.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/static/agent-actions-handlers.test.ts</automated>
  </verify>
  <done>shareAssistant case(s) pass; both ClipboardItem(Promise) and writeText fallback paths are explicitly covered; commit exists.</done>
</task>

<task type="auto">
  <name>Task 6: Full dashboard test-suite regression check</name>
  <files></files>
  <read_first>
    - Output from prior tasks' verify commands
  </read_first>
  <action>
    Run the full dashboard suite to confirm no regressions: `cd packages/dashboard && npm test`. If anything fails that wasn't failing before this plan, fix it (or revert the change that introduced the regression). Capture exit code and a short summary in the SUMMARY.md.
  </action>
  <verify>
    <automated>cd packages/dashboard && npm test 2>&1 | tail -30</automated>
  </verify>
  <done>npm test exits 0; previously-failing 6 cases (auth-flow x2, mcp x6, agent-multi-step E3, migration-058-059, shareAssistant) all pass.</done>
</task>

</tasks>

<verification>
- All 7 DI items resolved with passing CI
- 7 commits exist, one per DI scope (Tasks 1, 2, 3, 4, 5; Task 6 may be a no-op or a single regression-fix commit)
- `cd packages/dashboard && npm test` exits 0
- Production code untouched in this plan: `git diff master -- packages/dashboard/src/` shows no changes
</verification>

<success_criteria>
- All 6 promoted DI items in Plan 01 scope are closed (DI-31.2-01, DI-32-01, DI-32-02, DI-32-03, DI-37-01, DI-38-01, DI-38-02b)
- Dashboard test suite green
- No production source files modified by this plan
- Each fix has a commit with a clear `fix(test): … (DI-…)` message
</success_criteria>

<output>
After completion, create `.planning/phases/39.1-deferred-item-resolution-agent-js-split-test-staleness-fixes/39.1-01-SUMMARY.md`
</output>
