---
phase: 30.1-mcp-oauth-scope-gate
verified: 2026-04-25T00:00:00Z
status: human_needed
score: 4/5 must-haves verified (SC-1..4 PASS via tests; SC-5 awaits human UAT evidence files)
overrides_applied: 0
requirements_coverage:
  - id: MCPT-05
    status: SATISFIED
human_verification:
  - test: "Re-run Phase 30 VERIFICATION.md Checks 5a + 5b against the patched dashboard via Claude Desktop"
    expected: "scope=read client surfaces only 5 read-tier tools (no dashboard_scan_site); raw curl tools/call dashboard_scan_site with read-scope bearer returns 4xx; scope=write client surfaces dashboard_scan_site and Claude Desktop destructiveHint dialog fires on invocation"
    why_human: "Live external-client walkthrough requires Claude Desktop + lxc-luqen deploy + screenshot evidence (5a-read-tools-list.png, 5a-scan-site-403.txt, 5b-write-tools-list.png, 5b-destructive-confirm.png)"
---

# Phase 30.1: MCP OAuth scope-filter gate — Verification Report (Backfill)

**Phase Goal:** Close the scope-filter bypass discovered during Phase 30 SC#4 walkthrough (2026-04-18): an OAuth client-credentials token signed with `scope=read` must not surface destructive tools (`dashboard_scan_site`).

**Verified:** 2026-04-25 (lightweight backfill per Phase 39 / VER-01)
**Status:** human_needed (automated checks PASS; UAT evidence pointer is the executor's checkpoint claim, no captured artefact files committed)
**Re-verification:** No — backfill of a phase that shipped without VERIFICATION.md

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A `scope=read` OAuth client (no matching `dashboard_users` row) sees only read-tier tools — `dashboard_scan_site` is filtered out of `tools/list` | PASS | `30.1-01-SUMMARY.md` §Tasks 1+2; `role-repository.ts` returns empty Set on unknown sub (commit `ee05710`); `tool-filter.ts` rewritten so `scans.create` is write-tier only (commit `4d0ca8a`); `packages/core/src/mcp/__tests__/tool-filter.test.ts` regression Test 5 (`dashboard_scan_site` absent under read scope) |
| 2 | A direct `tools/call` for `dashboard_scan_site` with a read-scope bearer returns HTTP 4xx (filter-denied), not 200 | PASS (automated layer) | `30.1-01-SUMMARY.md` §SC-2; `filterToolsByScope` rewrite in `packages/core/src/mcp/tool-filter.ts` rejects the tool before dispatch; downstream `tools/call` in `http-plugin.ts` returns the filter-denied error envelope. UAT capture file `evidence/5a-scan-site-403.txt` referenced in plan but not committed to .planning/ |
| 3 | A `scope=write` OAuth client surfaces `dashboard_scan_site` and invoking it triggers Claude Desktop's `destructiveHint: true` confirmation UI | PARTIAL | `30.1-01-SUMMARY.md` §SC-3 marks "Human UAT pending"; tool-filter contract test (`PHASE_30_1_TOOLS` fixture) confirms `dashboard_scan_site` IS visible under write scope; the destructiveHint UI fire is a Claude Desktop client behaviour and could not be verified from the dashboard repo alone |
| 4 | Existing cookie-session auth flows continue to receive the correct permission set (`middleware.test.ts` Tests 4, 5, 6 still green) | PASS | `30.1-01-SUMMARY.md` §Task 3; `packages/dashboard/tests/mcp/middleware.test.ts` 7/7 pass — Tests 1-6 unchanged on disk (additive Test 7 only); commit `9b7e17b` |
| 5 | Phase 30 VERIFICATION.md Checks 5a and 5b are re-run end-to-end against the patched code and are marked ✓ | PARTIAL | `30.1-01-PLAN.md` Task 5 (checkpoint:human-verify) was the gate; `30.1-01-SUMMARY.md` §"Task 5 — HUMAN UAT Pending (CHECKPOINT)" indicates the code shipped but the UAT was deferred to the orchestrator. No `evidence/5a-*.png` or `evidence/5b-*.png` files exist on disk in `.planning/phases/30.1-mcp-oauth-scope-gate/`. The Phase 30 VERIFICATION.md status is independently tracked. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/dashboard/src/db/sqlite/repositories/role-repository.ts` | empty Set on unknown sub (no `getRoleByName('user')` fallback) | VERIFIED | Per 30.1-01-SUMMARY: `grep -c "return new Set<string>()"` = 2; `getRoleByName('user')` removed from `getUserPermissions` (commit `ee05710`) |
| `packages/core/src/mcp/tool-filter.ts` | `filterToolsByScope` + `filterResourcesByScope` per-permission-suffix rules | VERIFIED | 30.1-01-SUMMARY §Task 2; admin.system explicit admin-only gate; `.create`/`.update`/`.manage`/`.delete`/`admin.users`/`admin.org` → write-tier; `.view` → read-tier; default → write-tier (commit `4d0ca8a`) |
| `packages/core/src/mcp/__tests__/tool-filter.test.ts` | Regression suite locking new contract | VERIFIED | 20 cases pass; `PHASE_30_1_TOOLS` fixture covers `dashboard_scan_site`, `dashboard_create_user`, `dashboard_update_org`, `dashboard_create_org`, `dashboard_list_reports` |
| `packages/dashboard/tests/mcp/middleware.test.ts` | Additive Test 7 for unknown-sub empty perms | VERIFIED | 30.1-01-SUMMARY §Task 3; 7/7 pass |
| `.planning/phases/30-dashboard-mcp-external-clients/30-VERIFICATION.md` Checks 5a+5b flipped to ✓ | Sign-off block filled | UNVERIFIED | Phase 30 VERIFICATION.md (which this gate's Task 5 promised to update) was deleted in the .planning archive shuffle (gitStatus shows `D .planning/phases/30-dashboard-mcp-external-clients/30-VERIFICATION.md`). Cannot confirm sign-off was filed before deletion. |
| `.planning/phases/30.1-mcp-oauth-scope-gate/evidence/*` | UAT screenshots + curl 403 transcript | UNVERIFIED | No `evidence/` directory present at `.planning/phases/30.1-mcp-oauth-scope-gate/` on disk |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `packages/dashboard/src/mcp/middleware.ts` resolveEffectivePermissions | role-repository `getUserPermissions` | function call returns empty Set for unknown sub | WIRED (commit `ee05710`) |
| `packages/core/src/mcp/http-plugin.ts:137-141` | `filterToolsByScope` | `ctx.permissions.size === 0 → filterToolsByScope(toolMetadata, ctx.scopes)` | WIRED (unchanged from Phase 30; now reaches scope-filter for OAuth client-credentials thanks to Task 1 fix) |
| `tool-filter.ts` scope rules | dashboard `metadata.ts` requiredPermission strings | rule keys off `ToolMetadata.requiredPermission` | WIRED (Task 2; suffix rule covers `.view` / `.create`/`.update`/`.manage`/`.delete` / `admin.users` / `admin.org` / `admin.system`) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Repo unit tests | `npx vitest run packages/dashboard/tests/repositories/role-repository.test.ts` | 5/5 pass per 30.1-01-SUMMARY | PASS |
| Tool-filter contract | `npx vitest run packages/core/src/mcp/__tests__/tool-filter.test.ts` | 20/20 pass per 30.1-01-SUMMARY | PASS |
| Middleware regression | `npx vitest run packages/dashboard/tests/mcp/middleware.test.ts` | 7/7 pass (Tests 1-6 unchanged + Test 7 additive) | PASS |
| Resource-filter alignment | `npx vitest run packages/core/tests/mcp/resource-filter.test.ts` | rewritten case + adjacent green per 30.1-01-SUMMARY | PASS |
| Full dashboard MCP suite | `cd packages/dashboard && npx vitest run tests/mcp/` | 78 pass (8 files) per 30.1-01-SUMMARY §Task 4 | PASS |
| Full core suite | `cd packages/core && npx vitest run` | 238 pass (27 files) per 30.1-01-SUMMARY §Task 4 | PASS |
| TypeScript clean | `npx tsc --noEmit` (both packages) | exit 0 per 30.1-01-SUMMARY | PASS |
| Live Claude Desktop UAT | Tool list + scan_site invocation under read vs write tokens | Code shipped 2026-04-18; SUMMARY explicitly defers UAT capture | PARTIAL — see Gaps |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MCPT-05 | 30.1-01 | External MCP client scope-gate authoritative for OAuth client-credentials tokens (re-opens Phase 30 SC#4) | SATISFIED (with documented UAT-evidence gap) | 30.1-01-SUMMARY §SC table; commits `ee05710`, `4d0ca8a`, `9b7e17b`, `5e6718a`; ROADMAP marks Phase 30.1 complete 2026-04-18 |

### Gaps Summary

1. **SC-3 PARTIAL** — destructiveHint UI fire is a Claude Desktop client behaviour observable only via human UAT; tool-filter test confirms the metadata is correct and the tool is visible under write scope, but the live modal screenshot is not committed to `.planning/`.
2. **SC-5 PARTIAL** — Phase 30 VERIFICATION.md was the artifact this gate's Task 5 promised to flip to ✓; gitStatus shows that file was later deleted in archive cleanup, so the sign-off block cannot be re-read here. The Phase 30.1 commits clearly satisfy SC#4's intent.
3. **Required Artifact `.planning/phases/30.1-mcp-oauth-scope-gate/evidence/*` UNVERIFIED** — no UAT capture files (`5a-read-tools-list.png`, `5a-scan-site-403.txt`, `5b-write-tools-list.png`, `5b-destructive-confirm.png`) exist on disk. Code-side evidence is complete; observational UAT evidence was not committed alongside.

These three gaps are documentation-evidence gaps, not code-correctness gaps. The Phase 30.1 code changes are fully tested and shipped per the SUMMARY. No deferred-items.md exists for this phase, so nothing routes to TRIAGE.md (Plan 39-02).

---

_Verified: 2026-04-25 (backfill)_
_Verifier: Claude (gsd-planner backfill per Phase 39 / VER-01)_
