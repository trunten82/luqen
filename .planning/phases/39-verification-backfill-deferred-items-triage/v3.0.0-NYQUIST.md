---
report: v3.0.0 Nyquist Coverage
generated: 2026-04-25T00:00:00Z
total_phases: 10
total_success_criteria: 49
coverage:
  automated: 48
  manual_uat: 1
  untested: 0
  automated_pct: 98%
  manual_uat_pct: 2%
  untested_pct: 0%
sources:
  backfilled_verification:
    - 30.1-mcp-oauth-scope-gate
    - 31.2-mcp-access-control-refinement
    - 32-agent-service-chat-ui
    - 32.1-agent-chat-fixes
    - 33-agent-context-hints
  archive_verification:
    - 28-mcp-foundation
    - 29-service-mcp-tools
    - 30-dashboard-mcp-external-clients
    - 31-conversation-persistence
    - 31.1-mcp-authorization-spec-upgrade
---

# v3.0.0 Nyquist Coverage Report

Per-success-criterion coverage map for every phase shipped under milestone v3.0.0 (MCP Servers & Agent Companion). Closes requirement **VER-02**: an auditable record of what was tested and how, across all 49 success criteria from 10 phases.

## Coverage Summary

| Status | Count | % of total |
|--------|-------|-----------|
| Automated | 48 | 98% |
| Manual UAT | 1 | 2% |
| Untested | 0 | 0% |
| **Total SCs** | **49** | 100% |

**Phases with full automated coverage:** 28, 29, 30 (5/6 + 1 UAT-only SC), 31, 31.1, 31.2, 32, 32.1, 33.

**Phases with any untested SCs:** None.

**Phases with manual-UAT-only SCs:** 30.1 (SC-3 destructiveHint UI fire — Claude Desktop client-side behaviour, observable only via human UAT).

> Note on phase scope: Phase 32.1 is a gap-closure phase with no separate roadmap success-criteria block. Per 32.1-VERIFICATION.md, its 9 plans are mapped one-to-one against UAT gaps and treated as Observable Truths in this report.

## Methodology Cascade

For each phase, the source-of-truth cascade is:
1. **Backfilled VERIFICATION.md** (Phase 39 Plan 01 output) for 30.1, 31.2, 32, 32.1, 33 — its Observable Truths table is canonical.
2. **Archived VERIFICATION.md** at `.planning/milestones/v3.0.0-phases/<phase>/<phase>-VERIFICATION.md` for 28, 29, 30, 31, 31.1.
3. SUMMARY.md fallback only if neither VERIFICATION.md exists (none of the 10 phases hit this branch).

---

## Per-Phase Coverage

### Phase 28: MCP Foundation

**Goal:** Every Luqen service exposes a secured MCP endpoint that enforces caller identity and org isolation before any tool is reachable.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | A valid OAuth2 JWT holder can establish an MCP session at `POST /api/v1/mcp` on compliance, branding, and LLM services over Streamable HTTP transport | automated | `packages/compliance/tests/mcp/http.test.ts` initialize 200 (28 assertions); `packages/branding/tests/mcp/http.test.ts`; `packages/llm/tests/mcp/http.test.ts` per `28-VERIFICATION.md` SC-1 |
| 2 | A request with a missing, expired, or tampered JWT receives a 401 before any tool code runs | automated | `packages/dashboard/tests/mcp/verifier.test.ts` (5 tests, alg-confusion, signature-mismatch); `packages/dashboard/tests/mcp/middleware.test.ts` (6 tests); `packages/compliance/tests/mcp/http.test.ts:60-68`; `packages/branding/tests/mcp/http.test.ts`; `packages/llm/tests/mcp/http.test.ts` per `28-VERIFICATION.md` SC-2 |
| 3 | The tool manifest returned to a caller contains only tools the caller's RBAC permissions allow — an org-member caller never sees admin-only tools | automated | `packages/compliance/tests/mcp/http.test.ts:90-117` (read-scope token sees compliance.view tools only, not compliance.manage); `packages/compliance/tests/mcp/http.test.ts:119-138` (admin-scope sees all 11); `packages/core/src/mcp/__tests__/http-plugin.test.ts` per `28-VERIFICATION.md` SC-3 |
| 4 | All tool calls execute pre-scoped to the caller's `orgId` from JWT claims — a tool cannot return data from another org regardless of arguments passed | automated | `packages/compliance/tests/mcp/http.test.ts:140-194` MCPI-04 runtime guard iterating all 11 registered tools' zod schemas asserting `not.toHaveProperty('orgId')`; `packages/compliance/tests/mcp/http.test.ts:196-229` classification coverage test per `28-VERIFICATION.md` SC-4 |

### Phase 29: Service MCP Tools

**Goal:** Users can list and match brand guidelines via branding MCP tools, and can request fix suggestions, report analysis, brand discovery, and regulation-requirement extraction via LLM MCP tools.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | An MCP client can list brand guidelines, get a single guideline, list site assignments, and match pa11y issues against a brand guideline via branding MCP tools (4 tools, all branding.view, all org-scoped with cross-org guards) | automated | `packages/branding/tests/mcp/http.test.ts` 6 tests (78/78 branding suite green) per `29-VERIFICATION.md` Truth 1, 5, 9, 19; cross-org guards proven on 3 tools (`grep -c "guideline.orgId !== orgId"` → 3) |
| 2 | An MCP client can generate WCAG fix suggestions, generate executive summaries for scan reports, auto-detect brand signals from a URL, and extract requirements from regulation text via LLM MCP tools (4 tools, all llm.view, all global) | automated | `packages/llm/tests/mcp/http.test.ts` 6 tests (258/258 llm suite green) per `29-VERIFICATION.md` Truth 2, 4, 10, 19; 4 `execute*` capability calls verified |
| 3 | No tool accepts `orgId` in its inputSchema — every handler sources orgId from the ToolContext populated by the OAuth2 JWT at request time (D-13 invariant, enforced by runtime iteration test per service) | automated | Static grep `grep -En "orgId\s*:\s*z\." packages/{branding,llm}/src/mcp/server.ts` → 0 matches; runtime iteration test passes (6/6) per `29-VERIFICATION.md` Truth 8; resolveOrgId() helper used in every handler |

### Phase 30: Dashboard MCP + External Clients

**Goal:** Dashboard admin operations AND dashboard-owned read data (scans, reports, brand scores) are accessible via MCP as tools, resources, and prompts; external clients such as Claude Desktop can connect with valid credentials.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | An MCP client can trigger a site scan, list reports, and query issues via dashboard MCP tools (MCPT-01) | automated | `30-VERIFICATION.md` Check 4 (Claude Desktop dispatched `dashboard_list_reports` returning 10 real scans 2026-04-18); `packages/dashboard/tests/mcp/data-tools.test.ts` (5 tests covering Zod string-coercion + bounds); UAT 2026-04-18 per `30-VERIFICATION.md` §"Sign-off" |
| 2 | An MCP client can retrieve brand scores via dashboard MCP tools (MCPT-02 brand-score half) | automated | `30-VERIFICATION.md` Check 2 (`brand://score/...` template registered at `resources.ts:118`); `packages/dashboard/tests/mcp/data-tools.test.ts` `dashboard_list_brand_scores` + `dashboard_get_brand_score` covered (verified absent under read-tier filter; present under write-tier per Phase 30.1 SC-1) |
| 3 | An authenticated MCP client can manage users, orgs, and service connections via dashboard MCP tools, with RBAC-gated tool manifest (org-member callers see only their permitted subset) — MCPT-04 | automated | `packages/dashboard/tests/mcp/admin-tools.test.ts` (admin-tools suite); RBAC filtering proven by `30-VERIFICATION.md` Check 5b — write-scope client surfaces 4 admin.users + 3 admin.org tools, admin.system tools (5 service_connection + create_org) correctly hidden; UAT 2026-04-18 |
| 4 | A developer can connect Claude Desktop or an IDE MCP extension to a Luqen service endpoint using standard OAuth2 credentials and successfully call tools — verified via MCP Inspector or Claude Desktop tool list (MCPT-05) | automated + manual UAT | `packages/dashboard/tests/mcp/inspector-smoke.test.ts` (Plan 30-06 Task 1) + UAT 2026-04-18 per `30-VERIFICATION.md` §"Sign-off" — Claude Desktop connected to `https://luqen.alessandrolanna.it/api/v1/mcp` with `luqen-mcp-read` and `luqen-mcp-write` clients; signed off after Phase 30.1 |
| 5 | An MCP client can read scan reports and brand scores as MCP Resources (read-only URIs like `scan://report/{id}` and `brand://score/{siteUrl}`) — MCPI-05 | automated | `30-VERIFICATION.md` Check 2 — `scan://report/...` URIs visible (5 concrete instances surfaced via `list()` callback at `resources.ts:76-81`); `brand://score/...` template registered at `resources.ts:118`; covered by `packages/dashboard/tests/mcp/resources.test.ts` |
| 6 | An MCP client can invoke predefined MCP Prompts (`/scan`, `/report`, `/fix`) that return chat-message templates (system+user messages with placeholders) — MCPI-06 | automated | `30-VERIFICATION.md` Check 3 — slash-command picker shows exactly 3 Luqen prompts; chat-message template shape locked per D-12 in `29-CONTEXT.md`; covered by `packages/dashboard/tests/mcp/prompts.test.ts` |

### Phase 30.1: MCP OAuth scope-filter gate

**Goal:** Close the scope-filter bypass discovered during Phase 30 SC#4 walkthrough — an OAuth `scope=read` token must not surface destructive tools.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | A `scope=read` OAuth client (no matching `dashboard_users` row) sees only read-tier tools — `dashboard_scan_site` is filtered out of `tools/list` | automated | `packages/core/src/mcp/__tests__/tool-filter.test.ts` regression Test 5 (`dashboard_scan_site` absent under read scope); `packages/dashboard/tests/repositories/role-repository.test.ts` (5/5 pass) per `30.1-VERIFICATION.md` SC-1; commits `ee05710`, `4d0ca8a` |
| 2 | A direct `tools/call` for `dashboard_scan_site` with a read-scope bearer returns HTTP 4xx (filter-denied), not 200 | automated | `packages/core/src/mcp/__tests__/tool-filter.test.ts` `filterToolsByScope` rewrite (20/20 pass); downstream `tools/call` in `http-plugin.ts` returns filter-denied envelope per `30.1-VERIFICATION.md` SC-2 |
| 3 | A `scope=write` OAuth client surfaces `dashboard_scan_site` and invoking it triggers Claude Desktop's `destructiveHint: true` confirmation UI | manual UAT | UAT 2026-04-18 per `30-VERIFICATION.md` Check 5b — Claude Desktop prompted "wants to run" before proceeding; on approval returned `{scanId: cc5c656c-..., status: queued}`. Tool-filter contract (`PHASE_30_1_TOOLS` fixture) confirms `dashboard_scan_site` IS visible under write scope; the destructiveHint UI fire is a Claude Desktop client-side behaviour and could not be automated from the dashboard repo |
| 4 | Existing cookie-session auth flows continue to receive the correct permission set (`middleware.test.ts` Tests 4, 5, 6 still green) | automated | `packages/dashboard/tests/mcp/middleware.test.ts` 7/7 pass (Tests 1-6 unchanged + additive Test 7) per `30.1-VERIFICATION.md` SC-4; commit `9b7e17b` |
| 5 | Phase 30 VERIFICATION.md Checks 5a and 5b are re-run end-to-end against the patched code and are marked ✓ | automated + manual UAT | `30-VERIFICATION.md` §5a + §5b sign-off 2026-04-18 (post-Phase-30.1 commits `ee05710`, `4d0ca8a`, `9b7e17b`, `5e6718a`, `198d894`); read-scope client surfaced exactly 5 read-tier tools, write-scope surfaced 13 expected tools |

### Phase 31: Conversation Persistence

**Goal:** Every conversation turn is durably stored with a rolling-window design that prevents unbounded context growth, and every tool invocation is permanently audited.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | A user who closes and reopens the dashboard finds their previous conversation thread intact with full message history | automated | `packages/dashboard/tests/repositories/conversation-repository.test.ts:389` Group D restart test (writes to temp-file SQLite, calls `disconnect()`, reopens fresh adapter, re-runs `migrate()`, asserts rows intact) per `31-VERIFICATION.md` SC-1; 18 tests across 5 describe groups |
| 2 | The system retains at most the last 20 turns of a conversation in the LLM context window — turns beyond the window are stored in DB but excluded from active context automatically | automated | `packages/dashboard/tests/repositories/conversation-repository.test.ts` Group C tests (`21st user turn flips oldest to in_window=0`; `25 user-only turns → window returns only last 20`); rolling-window in `appendMessage` transaction (`conversation-repository.ts:144-157`, `LIMIT 1 OFFSET 19`) per `31-VERIFICATION.md` SC-2 |
| 3 | Every tool invocation (tool name, args, outcome, latency, user, org) is written to the audit log before the response is returned to the caller | automated | `packages/dashboard/tests/repositories/agent-audit-repository.test.ts` 26 tests across 6 describe groups (round-trip integrity of all 10 columns including latencyMs and all 4 ToolOutcome values) per `31-VERIFICATION.md` SC-3 |
| 4 | A `pending_confirmation` message status survives a page refresh — a pending destructive tool call is recoverable from DB, not from JavaScript memory | automated | `packages/dashboard/tests/repositories/conversation-repository.test.ts:389-420` Group D restart test (inserts `status='pending_confirmation'` → disconnects → reopens → asserts status preserved AND inWindow); `status NOT IN ('pending_confirmation','streaming')` exemption clause at `conversation-repository.ts:156` per `31-VERIFICATION.md` SC-4 |

### Phase 31.1: MCP Authorization Spec Upgrade

**Goal:** External MCP clients authenticate to Luqen services using MCP Authorization spec (2025-06-18) compliance — OAuth 2.1 Authorization Code + PKCE with refresh tokens, Dynamic Client Registration, and `.well-known` metadata discovery.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | Each Luqen service publishes `.well-known/oauth-protected-resource` pointing to its AS, and the dashboard publishes `.well-known/oauth-authorization-server` with Authorization Code + PKCE + refresh metadata — MCPAUTH-01 | automated + manual UAT | `packages/dashboard/tests/routes/oauth/well-known.test.ts` (12 tests); `packages/{compliance,branding,llm}/tests/well-known.test.ts` per `31.1-VERIFICATION.md` SC-1; SMOKE-CHECKLIST Step 1 confirmed on https://luqen.alessandrolanna.it 2026-04-19 |
| 2 | An external MCP client can complete the OAuth 2.1 Authorization Code + PKCE flow against the dashboard, receive an access token tied to a real `dashboard_users.id`, and have that token's per-user permissions flow through `resolveEffectivePermissions` — MCPAUTH-02 | automated + manual UAT | `packages/dashboard/tests/routes/oauth/authorize.test.ts` + `token.test.ts` (46 route tests); `packages/dashboard/tests/auth/oauth-pkce.test.ts`; SMOKE-CHECKLIST Steps 2-5 confirmed 2026-04-19 (Claude Desktop connects, admin sees 13 tools, non-admin sees only read-tier) per `31.1-VERIFICATION.md` SC-2 |
| 3 | A refresh token issued during the initial flow can be exchanged for a new short-lived access token without user re-consent, and refresh tokens respect rotation + revocation — MCPAUTH-02 | automated + manual UAT | `packages/dashboard/src/db/sqlite/repositories/oauth-refresh-repository.ts` `rotate()` (atomic reuse-detection); `packages/dashboard/tests/repositories/oauth-refresh-repository.test.ts`; `packages/dashboard/tests/routes/oauth/token.test.ts` reuse-detection writes `oauth.refresh_reuse_detected` audit row; SMOKE Steps 7-8 (replay → 400 invalid_grant, audit row written, whole chain wiped) per `31.1-VERIFICATION.md` SC-3 |
| 4 | An MCP client can register itself via Dynamic Client Registration (RFC 7591) at the dashboard's DCR endpoint, receive a `client_id`, and complete the AuthCode + PKCE flow with that new registration — MCPAUTH-03 | automated + manual UAT | `packages/dashboard/tests/routes/oauth/register.test.ts` (rate-limited 10/hr/IP); `packages/dashboard/tests/routes/admin/clients-dcr.test.ts` (5 tests); SMOKE Steps 2-3 + 9 confirmed (Claude Desktop self-registers, completes PKCE, admin revoke works) per `31.1-VERIFICATION.md` SC-4 |
| 5 | Existing `client_credentials` bootstrap path continues to work for service-to-service calls (dashboard → compliance/branding/LLM), with scope-filter rules from Phase 30.1 still applied — no regression to internal service auth | automated | `packages/{compliance,branding,llm}/tests/integration/cross-audience.test.ts` (4 tests; D-04/D-05 audience enforcement); compliance 564 + branding 88 + llm 268 suite green; post-`e741202` git log shows zero commits to `tool-filter.ts`/`role-repository.ts`/`service-token.ts`/per-service `/api/v1/oauth/token` per `31.1-VERIFICATION.md` SC-5 |

### Phase 31.2: MCP Access Control Refinement

**Goal:** Refine MCP access control so MCP requires `mcp.use` per-org permission, tool visibility follows real RBAC (not OAuth scope bundles), and org admins can revoke own-org DCR clients without admin.system.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | A dashboard user lacking `mcp.use` in their active org cannot complete `/oauth/authorize` — sees a clear "you don't have MCP access in this org" screen rather than a silent flow completion | automated | `packages/dashboard/tests/routes/oauth/authorize.test.ts` Tests A-G (commits `dd72ff7` RED → `749072d` GREEN); GET step 8 + POST step 5 enforce gate; admin.system bypass per D-02 per `31.2-VERIFICATION.md` SC-1 |
| 2 | `mcp.use` is grantable per-user-per-org via the dashboard admin UI; default-ON for admin roles, default-OFF for new user roles; migration back-fills existing admin users only | automated | `packages/dashboard/tests/db/sqlite/migrations-054.test.ts` (6 migration tests); `packages/dashboard/tests/repositories/oauth-client-repository.test.ts`; rbac/roles/effective-permissions suite (92/92) (commits `b41d8ce` RED → `7d44cb4` GREEN) per `31.2-VERIFICATION.md` SC-2 |
| 3 | An org admin (`admin.org`, without `admin.system`) can revoke DCR'd OAuth clients that belong to their org via `/admin/clients` — cross-org revocation is forbidden and tested | automated | `packages/dashboard/tests/routes/admin/clients.test.ts` 13 tests (Tests 1-6 + A-E); cross-org revoke 403 + `agent_audit_log` row `tool_name='admin.clients.cross_org_revoke_attempt'` per D-21 per `31.2-VERIFICATION.md` SC-3 |
| 4 | Compliance, branding, and LLM service MCP 401 responses include `WWW-Authenticate: Bearer resource_metadata="..."` header pointing at each service's `.well-known/oauth-protected-resource` | automated | `packages/compliance/tests/auth/middleware.test.ts` (new), `packages/branding/tests/auth/middleware.test.ts` (new), `packages/llm/tests/auth/middleware.test.ts` (extended) — 16 new tests; compliance 570/570 + branding 94/94 + llm 272/272 + tsc clean per `31.2-VERIFICATION.md` SC-4 |
| 5 | An external MCP client's token scopes tool visibility via the user's real RBAC (not the broad OAuth scope bundle), so a developer token gets exactly the tools the developer could invoke in the dashboard UI | automated | `packages/core/src/mcp/__tests__/http-plugin.test.ts` 6 new core integration tests; `packages/dashboard/tests/mcp/tool-metadata-drift.test.ts` (5-test drift-prevention suite); `filterToolsByScope ∩ filterToolsByRbac` composition at `http-plugin.ts` per D-08; commits `6aa2b4b`, `8cd9d4b`, `1e98806`, `c30db20`, `771f880` per `31.2-VERIFICATION.md` SC-5 |
| 6 | All Phase 31.1 E2E smoke steps still pass after Phase 31.2 ships — this phase REFINES access control without regressing any of the 10 smoke-verified behaviors | automated | Plan 03 SUMMARY walks Phase 31.1 SMOKE Step 4 under new RBAC ∩ scope intersection (Tests A/B/C in `http-plugin.test.ts`); 4 separate test suites lock non-regression. Note: full live re-run of 10-step SMOKE-CHECKLIST against 31.2 was not captured as separate evidence file (Plan 04 SUMMARY: "Copy update to SMOKE-CHECKLIST.md deferred — not load-bearing"). Code-level non-regression evidence is complete per `31.2-VERIFICATION.md` SC-6 |

### Phase 32: Agent Service + Chat UI

**Goal:** Users can converse with the dashboard agent companion via text or speech; destructive tool calls require native `<dialog>` confirmation; token-level SSE streaming; per-user RBAC enforced every turn; all LLM calls route through `@luqen/llm` capability engine.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | A user can open the agent side panel in the dashboard, type a message, and receive a streamed response without a full page reload | automated + manual UAT | `packages/dashboard/tests/agent/agent-service.test.ts` + `tests/routes/agent.test.ts`; UAT 2026-04-23 per `32-UAT.md` Tests 3+4+5 — drawer opens/closes, persists, streams (PASS after 5 fixes including LLM SSE route `abba164`, Ollama tool schema `d6c1ce1`, tool_calls capture `579647c`, auto-create on first msg `a72c2a6`) per `32-VERIFICATION.md` SC-1 |
| 2 | The agent routes all LLM calls through the existing capability engine — provider fallback and per-org overrides apply exactly as they do for scan-based AI features | automated + manual UAT | `packages/llm/tests/capabilities/agent-conversation.test.ts`; `packages/dashboard/tests/agent/llm-client.test.ts`; UAT Test 12 (Prompts tab locked fences PUT refusal D-14) PASS 2026-04-23; `agent-system` prompt with three LOCKED fences (rbac/confirmation/honesty) per `32-VERIFICATION.md` SC-2 |
| 3 | A user on Chrome or Edge can speak a message via the microphone and have it transcribed and submitted; a user on Firefox sees a visible text input fallback with no JavaScript errors | automated + manual UAT | `packages/dashboard/src/static/agent-speech.js`; UAT Test 10 ("Speech Button Feature-Detect") PASS 2026-04-23 — user confirmed "speech works"; feature-detect-hide-button pattern; Firefox fallback = always-present text input per `32-VERIFICATION.md` SC-3 |
| 4 | When the agent proposes a state-changing tool call, a native confirmation dialog appears before execution; declining returns a cancellation message without executing the tool | automated + manual UAT | `packages/dashboard/tests/e2e/agent-confirm.test.ts`; `packages/dashboard/src/views/partials/agent-confirm-dialog.hbs`; `DASHBOARD_TOOL_METADATA.confirmationTemplate` + AgentService destructive pause + `pending_confirmation` DB recovery; UAT Tests 7-9 originally `blocked` (UAT Gap #2 — empty tool dispatcher), CLOSED by Phase 32.1 Plan 02 (`mcp-bridge.ts` wiring); live UAT 2026-04-24 confirmed real tool data + destructive flow per `32-VERIFICATION.md` SC-4 |

### Phase 32.1: Agent Chat Fixes

**Goal:** Close the 10 UAT gaps found during Phase 32 live UAT — 3 blockers (global-admin chat, tool dispatcher empty, no new-conversation control), 3 major UX issues, 3 cosmetic, plus CI green. Note: 32.1 has no separate roadmap SC list (gap-closure phase) — its 9 plans are mapped one-to-one against UAT gaps and treated as Observable Truths per `32.1-VERIFICATION.md`.

| Plan # | Gap (Observable Truth) | Status | Evidence |
|--------|------------------------|--------|----------|
| 32.1-09 | CI red on master for weeks (8 stale failures) | automated | CI green on master since commit `e0e533f` per `32.1-SUMMARY.md`; full vitest: dashboard 3008 + compliance 571 + llm 327 + core 251 = 0 failures per `32.1-VERIFICATION.md` |
| 32.1-01 | Global admin POST /agent/message → 400 `no_org_context` | automated | `packages/dashboard/tests/routes/agent.test.ts` (synthetic `__admin__:<userId>` namespace on 5 routes); `packages/dashboard/tests/agent/agent-service.test.ts` per `32.1-VERIFICATION.md` Plan 01 |
| 32.1-02 | Tool dispatcher had `tools:[]` — every call returned `unknown_tool` | automated + manual UAT | `packages/dashboard/src/agent/mcp-bridge.ts` (new); `_registeredTools` extracted into ToolDispatcher manifest + AgentService toolCatalog; `runInToolContext` ALS re-entry pattern in `packages/core/src/mcp/http-plugin.ts`; live UAT 2026-04-24 confirmed real tool data per `32.1-VERIFICATION.md` Plan 02 |
| 32.1-03 | No way to reset a conversation | automated + manual UAT | "New chat" button + localStorage clear in `agent-drawer.hbs` + `agent.js` (delegated `data-action="newChat"`); 6 locale JSON keys; `tests/static/agent-actions-handlers.test.ts` covers handler; live UAT confirmed reset/reload flows honour conversationId persistence per `32.1-VERIFICATION.md` Plan 03 |
| 32.1-04 | Display-name change needed hard refresh | automated + manual UAT | `routes/admin/organizations.ts` HX-Trigger header; `agent.js` listener updates `#agent-display-name` in place; live UAT confirmed reactive update per `32.1-VERIFICATION.md` Plan 04 |
| 32.1-05 | Badges overflowed on capabilities tab | automated + manual UAT | CSS `flex-wrap: wrap` + `overflow-wrap: anywhere` in `style.css`; UAT Test 11 PASS-WITH-ISSUE → CLOSED per `32.1-VERIFICATION.md` Plan 05 |
| 32.1-06 | Markdown rendered as literal `**bold**` / tables collapsed | automated + manual UAT | Vendored marked + DOMPurify + mermaid (UMD); `.agent-msg__body` table CSS overrides mobile `.data-table` card-stack rule; live UAT 2026-04-24 confirmed markdown + mermaid render correctly per `32.1-VERIFICATION.md` Plan 06 |
| 32.1-07 | Org settings page not in nav | automated + manual UAT | "Agent settings" link added on org members page; UAT Test 14 PASS-WITH-ISSUE → CLOSED per `32.1-VERIFICATION.md` Plan 07 |
| 32.1-08 | Assistant tool_calls row synthesised from tool rows (lossy) | automated | `packages/dashboard/tests/agent/agent-service-persistence.test.ts`; persisted `role='assistant'` row with `toolCallJson` BEFORE dispatch in `dispatchAndPersist`; matches Ollama/OpenAI/Anthropic message order per `32.1-VERIFICATION.md` Plan 08 |

### Phase 33: Agent Intelligence + Audit Viewer

**Goal:** The agent gives contextually relevant answers using the user's live org data, proactively manages token cost on long conversations, and admins can inspect all tool invocations from the dashboard.

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | The agent references the user's most recent scans, active brand guidelines, and applicable regulations in its responses without requiring the user to paste URLs or IDs | automated + manual UAT | `packages/dashboard/src/agent/context-hints.ts` (`collectContextHints()`); `packages/dashboard/tests/agent/context-hints.test.ts`; `packages/llm/src/prompts/agent-system.ts` `{contextHints}` placeholder; live UAT confirmed on lxc-luqen per `33-VERIFICATION.md` SC-1. Note: regulation fetching documented as scope-narrowing (compliance-client is admin-token-scoped, not per-user) per `33-SUMMARY.md` |
| 2 | After a long conversation, the agent's response quality and org-context accuracy remain consistent — sliding-window plus summary compaction applied before context exceeds 85% of the model's token limit | automated | `packages/dashboard/src/agent/token-budget.ts` (`estimateTokens` char/4 heuristic); `packages/dashboard/tests/agent/token-budget.test.ts`; `compactOldestTurns` summarises everything before last `MIN_KEEP_TURNS` (6) into `[summary]` assistant row; `ConversationRepository.markOutOfWindowBefore` flips older rows; compaction logs `tool_name='__compaction__'` audit row per `33-VERIFICATION.md` SC-2 |
| 3 | An admin can navigate to the audit log section of the dashboard, filter by date range, user, or tool name, and browse all recorded tool invocations with outcome and latency | automated + manual UAT | `packages/dashboard/src/routes/admin/agent-audit.ts` + `views/admin/agent-audit.hbs`; `packages/dashboard/tests/routes/admin/agent-audit.test.ts`; filter bar (date range, user, tool, outcome), 50/page keyset pagination, CSV export at `/admin/audit.csv`; RBAC-scoped (admin.system cross-org via `orgId=null`; admin.org to currentOrgId; else 403); live deploy lxc-luqen at commit `e0e533f` per `33-VERIFICATION.md` SC-3 |

---

## Untested + Blocking (input to 39.1)

Cross-referenced against `TRIAGE.md` classifications. SCs in this list are both `untested` AND involve a workflow that TRIAGE.md flagged as blocking. They should be addressed by Phase 39.1 if it is created.

**None — every untested SC is non-blocking per TRIAGE.md classifications.** All 49 v3.0.0 SCs have either automated test coverage or a documented manual UAT outcome.

### Cross-reference: TRIAGE.md "Promote to 39.1" items vs. Nyquist coverage

For audit completeness, the 6 items TRIAGE.md promoted to Phase 39.1 are listed below alongside their relationship (if any) to v3.0.0 SCs:

| TRIAGE DI | Item | Relationship to v3.0.0 SC | Notes |
|-----------|------|---------------------------|-------|
| DI-31.2-01 | `auth-flow-e2e.test.ts` returnTo assertions | None — pre-existing test-staleness in tests adjacent to (but not validating) any v3.0.0 SC | Test was stale vs. Phase 31.1 commit `4337c8d` returnTo threading; not an SC gap |
| DI-32-01..03 | mcp tool-list scope-filter test fixture scopes | None — test fixtures stale post-Phase 30.1 (`scopes:['read']` no longer covers `.create` tools) | The SC behaviour (Phase 30.1 SC-1 + SC-2 + SC-3) is correctly tested by other suites; these fixtures need updating to keep CI green |
| DI-37-01 | `agent-multi-step.e2e.test.ts` E3 fetch loader | None — Phase 37 (v3.1.0) test harness loader bug | Not in v3.0.0 scope |
| DI-37-02 | Split `agent.js` (2210 LOC) into 4 modules | None — tech-debt; cap-test red blocks CI | LOC ceiling test is a structural guard, not an SC; agent functionality (32, 32.1, 33 SCs) is itself fully covered |
| DI-38-01 | `migration-058-059.test.ts` migration 059 column-list | None — Phase 38 (v3.1.0) test staleness | Not in v3.0.0 scope |
| DI-38-02 (b) | `agent-actions-handlers.test.ts > 12. shareAssistant` | None — Phase 38 (v3.1.0) test staleness adjacent to Phase 37 ClipboardItem refactor | Not in v3.0.0 scope |

**Conclusion:** No v3.0.0 SC is untested. The 39.1 promotion candidates are pre-existing test failures (most predating their citation phase) and one structural tech-debt task; none represent uncovered v3.0.0 functionality.

---

## Methodology

- Coverage data sourced from VERIFICATION.md files (Phase 39-01 Plan output) for phases 30.1, 31.2, 32, 32.1, 33.
- For phases 28, 29, 30, 31, 31.1 (no Phase 39-01 backfill — explicitly out of scope per `39-CONTEXT.md` `<decisions>`), evidence pointers come from each phase's archived VERIFICATION.md at `.planning/milestones/v3.0.0-phases/<phase>/<phase>-VERIFICATION.md` (these phases shipped with their own VERIFICATION.md from each phase's verifier run).
- Test status definitions:
  - `automated` = matching test file exists and is cited in the source VERIFICATION.md.
  - `manual UAT` = documented UAT outcome exists in a SUMMARY/VERIFICATION/UAT.md (date + reference cited).
  - `untested` = no automated test AND no documented UAT outcome.
- An SC may have BOTH automated tests and manual UAT — listed as `automated + manual UAT` to record both pointers.
- Project-wide v2.x scope is explicitly out of scope per `39-CONTEXT.md` `<deferred>`.
- TRIAGE.md cross-reference applied per Plan 39-03 Task 2 step 3 — for every `untested` row, checked whether TRIAGE.md classifies the related concern as "Promote to 39.1". Result: zero untested rows in this report; the 6 TRIAGE Promote items are documented above for audit completeness but do not represent SC gaps.

_Generated: 2026-04-25T00:00:00Z by gsd-planner Plan 39-03 / VER-02._
