---
phase: 39-verification-backfill-deferred-items-triage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "TRIAGE.md exists and lists every deferred-items entry sourced from v3.0.0 (31.2, 32) AND v3.1.0 (35, 36, 37, 38)"
    - "Every entry has exactly one classification: 'Promote to 39.1', 'Defer to v3.2.0', or 'Won't-fix'"
    - "Every entry has a one-sentence rationale citing the severity rule applied (per 39-CONTEXT.md D-Triage policy)"
    - "Each entry cites its source (file path + section/line) so reviewers can audit the decision"
    - "If at least one item is classified 'Promote to 39.1', a follow-up action note records that /gsd-insert-phase 39.1 should be invoked"
  artifacts:
    - path: ".planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md"
      provides: "Single consolidated triage decision log for all v3.0.0 + v3.1.0 deferred items"
      contains: "Promoted to 39.1"
      min_lines: 50
  key_links:
    - from: "TRIAGE.md entry"
      to: "Source deferred-items.md or SUMMARY 'Deferred Issues' section"
      via: "Source column"
      pattern: "(deferred-items|Deferred|SUMMARY)"
    - from: "TRIAGE.md 'Promote to 39.1' section"
      to: "Future 39.1 phase tasks"
      via: "Task seed list at end of section"
      pattern: "39.1"
---

<objective>
Walk every phase directory in v3.0.0 and v3.1.0 for deferred-items entries (both standalone `deferred-items.md` files and inline "Deferred Issues" sections inside SUMMARY.md files), classify each entry per the severity policy locked in 39-CONTEXT.md, and produce a single consolidated TRIAGE.md decision log.

Purpose: Close requirement VER-03. Drive the decision on whether a follow-up phase 39.1 is needed.
Output: `.planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md` plus an explicit recommendation at the end of the SUMMARY about invoking `/gsd-insert-phase 39.1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/39-verification-backfill-deferred-items-triage/39-CONTEXT.md

<triage_policy>
Locked by 39-CONTEXT.md `<decisions>` block (D-Triage policy). Apply VERBATIM:

- **Promote to v3.1.0 (39.1)** if:
  - BLOCKING bug (breaks user workflow, data loss, security regression)
  - Security issue
  - Materially impedes user workflows
  - Tech-debt or UX nicety that fits the current cycle's scope (e.g. agent.js LOC split — file is at 2009 lines and exceeds the in-test cap)

- **Defer to v3.2.0** if:
  - Tech-debt or UX nicety that's low-impact, speculative, or would expand the milestone

- **Won't-fix** if:
  - Documented duplicate of another item
  - No-longer-relevant after a later phase shipped
  - Explicitly contradicted by a v3.1.0 decision

Known-going-in items (per 39-CONTEXT.md `<canonical_refs>`):
- agent.js LOC split (2009 lines, e2e cap test red) -> "Promote to 39.1" (matches "tech-debt fits current cycle" rule)
- agent-multi-step.e2e.test.ts E3 harness loader bug -> "Promote to 39.1" (test is broken in current cycle)
- Pre-existing test failures discovered in 38 SUMMARYs (4 noted) -> per-item triage required
</triage_policy>

<source_locations>
Walk these locations exhaustively:

v3.0.0 (archived):
- .planning/milestones/v3.0.0-phases/31.2-mcp-access-control-refinement/deferred-items.md
- .planning/milestones/v3.0.0-phases/32-agent-service-chat-ui/deferred-items.md
- .planning/milestones/v3.0.0-phases/32-agent-service-chat-ui/32-*-SUMMARY.md (8 summaries — grep for "Deferred"/"Issues"/"TODO"/"Follow-up")

v3.1.0 (live):
- .planning/phases/35-agent-conversation-history/35-*-SUMMARY.md (6 summaries)
- .planning/phases/36-multi-step-tool-use/36-*-SUMMARY.md (6 summaries)
- .planning/phases/37-streaming-ux-polish/deferred-items.md
- .planning/phases/37-streaming-ux-polish/37-*-SUMMARY.md (5 summaries)
- .planning/phases/38-multi-org-context-switching/38-*-SUMMARY.md (4 summaries)

Plus any 35-VERIFICATION.md / 36-VERIFICATION.md / 37-VERIFICATION.md "Anti-Patterns" or "Gaps" entries flagged for follow-up.
</source_locations>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk all sources and produce raw inventory</name>
  <files>
    .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md
  </files>
  <action>
Phase 1 of TRIAGE.md generation — produce the raw inventory section first, then classify in Task 2.

1. Read every file under `<source_locations>`. For each, extract every entry that looks like a deferred item:
   - Items in a `deferred-items.md` file (every bullet/row counts)
   - SUMMARY.md sections titled "Deferred Issues", "Items deferred to formal UAT", "Follow-up", "Known limitations", "Tech debt", or any inline "TODO: defer to ..." note
   - VERIFICATION.md "Anti-Patterns" or "Gaps" entries that flag a follow-up

2. For each entry capture:
   - **id**: stable id `DI-<source-phase>-<NN>` (e.g. `DI-32-01`, `DI-37-03`); number sequentially per source phase
   - **source**: file path + line/section reference (e.g. `phases/37/deferred-items.md L12-15` or `phases/35/35-05-SUMMARY.md §"Deferred Issues"`)
   - **summary**: the entry text condensed to one sentence
   - **severity hint**: read the entry — does it self-describe as blocker/security/UX nicety/tech-debt/duplicate?

3. Write TRIAGE.md with this structure (DO NOT classify yet — that happens in Task 2):

```markdown
---
phase: 39-verification-backfill-deferred-items-triage
plan: 02
created: <ISO timestamp>
total_items: <N>
classifications:
  promote_to_39_1: <N>     # filled in Task 2
  defer_to_v3_2_0: <N>     # filled in Task 2
  wont_fix: <N>            # filled in Task 2
---

# TRIAGE.md — Deferred-Items Decision Log

## Inventory (raw, pre-classification)

| ID | Source | Summary | Severity hint |
|----|--------|---------|----------------|
| DI-31.2-01 | phases/31.2/deferred-items.md L<n> | <one-sentence> | <hint> |
| ... | ... | ... | ... |

## Classifications

(Filled by Task 2)

## Promoted to 39.1

(Filled by Task 2)

## Deferred to v3.2.0

(Filled by Task 2)

## Won't-Fix

(Filled by Task 2)

## Follow-up Action

(Filled by Task 2)
```

4. Inventory must be exhaustive — no source file from `<source_locations>` left unread. If a phase contributes zero entries, note "(no deferred items found)" inline so reviewers can confirm coverage.
  </action>
  <verify>
    <automated>test -f .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md &amp;&amp; grep -q "## Inventory" .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md &amp;&amp; grep -qE "DI-(31\.2|32|35|36|37|38)-" .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md</automated>
  </verify>
  <done>TRIAGE.md exists with full inventory table covering every source location. Each row has stable ID, source pointer, one-sentence summary, severity hint. Empty source phases explicitly noted.</done>
</task>

<task type="auto">
  <name>Task 2: Classify every entry and produce final TRIAGE.md sections</name>
  <files>
    .planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md
  </files>
  <action>
Read TRIAGE.md (your Task 1 output). For every row in the Inventory table, apply the `<triage_policy>` from this plan's `<context>` block to produce a final classification.

1. Build the **Classifications** section as a per-item decision table:

```markdown
| ID | Classification | Rationale |
|----|---------------|-----------|
| DI-31.2-01 | Promote to 39.1 | Blocking — breaks org isolation when admin switches scope |
| DI-32-03 | Defer to v3.2.0 | Low-impact UX nicety (drawer animation polish); speculative |
| DI-35-02 | Won't-fix | Duplicate of DI-37-01; resolved by Phase 37 share-link work |
```

Rationale rules:
- Cite the specific severity-policy bullet that applies (e.g. "Blocking — security regression per D-Triage Promote bullet 2").
- For Won't-fix duplicates, cite the canonical entry id.
- For Defer-to-v3.2.0, explain why it's low-impact AND would expand current milestone.

2. Build the three classified sections (`## Promoted to 39.1`, `## Deferred to v3.2.0`, `## Won't-Fix`). Each section is a list of items with full context (id, source, summary, rationale) so 39.1 task seeds can be lifted directly:

```markdown
## Promoted to 39.1

These items will become tasks in Phase 39.1 (created via `/gsd-insert-phase 39.1` after this plan completes).

### DI-X-NN: <short title>
- **Source:** <file:line>
- **Summary:** <one paragraph>
- **Rationale:** <severity-policy citation>
- **Suggested 39.1 task scope:** <one sentence describing what closing this would entail>
```

3. Update the frontmatter `classifications:` counts to reflect the actuals.

4. Write the **Follow-up Action** section at the bottom:
   - If `promote_to_39_1 > 0`: state explicitly "Run `/gsd-insert-phase 39.1` to create the resolution phase. Seed tasks from the 'Promoted to 39.1' section above."
   - If `promote_to_39_1 == 0`: state "No items promoted; phase 39.1 is not required. Phase 39 stands alone."

5. Verify pre-known items received the expected classification:
   - agent.js LOC split (2009 lines): MUST be "Promote to 39.1"
   - agent-multi-step.e2e.test.ts E3 harness loader bug: MUST be "Promote to 39.1"
   If your sources don't surface these explicitly, add them as DI entries with sources cited from 38-SUMMARY.md / 36-VERIFICATION.md / agent.js header comment.

DO NOT invoke `/gsd-insert-phase` from this plan — that's a follow-up the user (or orchestrator) runs after reviewing TRIAGE.md.
  </action>
  <verify>
    <automated>F=.planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md; grep -q "## Classifications" "$F" &amp;&amp; grep -q "## Promoted to 39.1" "$F" &amp;&amp; grep -q "## Deferred to v3.2.0" "$F" &amp;&amp; grep -q "## Won't-Fix" "$F" &amp;&amp; grep -q "## Follow-up Action" "$F" &amp;&amp; grep -qE "promote_to_39_1: [0-9]+" "$F" &amp;&amp; ! grep -qE "(\(Filled by Task 2\))" "$F"</automated>
  </verify>
  <done>TRIAGE.md is fully classified. Frontmatter counts match the section contents. Every inventory row has exactly one classification with rationale. Follow-up Action section explicitly states whether `/gsd-insert-phase 39.1` is required. Pre-known items (agent.js LOC, e2e harness bug) appear in "Promote to 39.1".</done>
</task>

</tasks>

<verification>
Phase-level checks for this plan:
- TRIAGE.md exists and parses as valid markdown with YAML frontmatter.
- Every source location in `<source_locations>` was visited (cross-check by looking for at least one DI-<phase>- entry per phase, OR an explicit "no deferred items found" note).
- Sum of items in the three classified sections equals the inventory total.
- No "(Filled by Task 2)" placeholder text remains.
</verification>

<success_criteria>
- VER-03 satisfied: every deferred-items entry across v3.0.0 (31.2, 32) and v3.1.0 (35, 36, 37, 38) is classified with rationale.
- TRIAGE.md is consumable as a 39.1 task-seed source.
- Follow-up Action section gives the user a clear next step.
</success_criteria>

<output>
After completion, create `.planning/phases/39-verification-backfill-deferred-items-triage/39-02-SUMMARY.md` listing:
- Total items inventoried
- Counts per classification
- Whether 39.1 is recommended
- The full list of items promoted to 39.1 with one-line scope each (so the user can review before invoking `/gsd-insert-phase 39.1`)
</output>
</content>
</invoke>