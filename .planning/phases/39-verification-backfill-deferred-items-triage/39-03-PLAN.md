---
phase: 39-verification-backfill-deferred-items-triage
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/phases/39-verification-backfill-deferred-items-triage/v3.0.0-NYQUIST.md
autonomous: true
requirements: [VER-02]
must_haves:
  truths:
    - "v3.0.0-NYQUIST.md exists and lists every success criterion from every v3.0.0 phase (28, 29, 30, 30.1, 31, 31.1, 31.2, 32, 32.1, 33)"
    - "Every SC row has a test status: 'automated' | 'manual UAT' | 'untested'"
    - "Every SC row has an evidence pointer (test file path, UAT date, or 'no evidence found')"
    - "Aggregate coverage stats are reported (counts and percentages per status)"
    - "Any 'untested' row that 39-02's TRIAGE.md classified as blocking is cross-referenced into the 'Promote to 39.1' note at the bottom"
  artifacts:
    - path: ".planning/phases/39-verification-backfill-deferred-items-triage/v3.0.0-NYQUIST.md"
      provides: "Coverage report for every v3.0.0 success criterion"
      contains: "Coverage Summary"
      min_lines: 80
  key_links:
    - from: "Each NYQUIST row"
      to: "Source VERIFICATION.md (from Plan 39-01) or v3.0.0 SUMMARY"
      via: "Evidence column"
      pattern: "(VERIFICATION|SUMMARY|tests/)"
    - from: "Untested-blocking rows"
      to: "TRIAGE.md 'Promote to 39.1' section"
      via: "Cross-reference note"
      pattern: "TRIAGE"
---

<objective>
Produce the v3.0.0 Nyquist coverage report: a single document listing every success criterion from every v3.0.0 phase with its test status (automated / manual UAT / untested) and an evidence pointer. Builds on the VERIFICATION.md files written by Plan 39-01 and the triage decisions made in Plan 39-02.

Purpose: Close requirement VER-02. Provide auditable proof of what is and isn't covered in v3.0.0.
Output: `.planning/phases/39-verification-backfill-deferred-items-triage/v3.0.0-NYQUIST.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-verification-backfill-deferred-items-triage/39-CONTEXT.md
@.planning/milestones/v3.0.0-ROADMAP.md
@.planning/milestones/v3.0.0-REQUIREMENTS.md
@.planning/phases/39-verification-backfill-deferred-items-triage/39-01-SUMMARY.md
@.planning/phases/39-verification-backfill-deferred-items-triage/39-02-SUMMARY.md
@.planning/phases/30.1-mcp-oauth-scope-gate/30.1-VERIFICATION.md
@.planning/phases/31.2-mcp-access-control-refinement/31.2-VERIFICATION.md
@.planning/phases/32-agent-service-chat-ui/32-VERIFICATION.md
@.planning/phases/32.1-agent-chat-fixes/32.1-VERIFICATION.md
@.planning/phases/33-agent-context-hints/33-VERIFICATION.md
@.planning/phases/39-verification-backfill-deferred-items-triage/TRIAGE.md

<test_status_definitions>
Per 39-CONTEXT.md `<decisions>`:
- **automated**: SC has at least one matching unit/integration/e2e test file. Cite path.
- **manual UAT**: SC has a documented UAT outcome in a SUMMARY/VERIFICATION/UAT.md. Cite date + file.
- **untested**: No automated test AND no documented UAT outcome. Cite "no evidence found in <searched-files>".

A SC may have BOTH automated tests and manual UAT — list status as `automated` and add the UAT pointer in the evidence cell.
</test_status_definitions>

<v3_0_0_phase_list>
Per .planning/milestones/v3.0.0-ROADMAP.md, the v3.0.0 milestone shipped these phases:
- 28: MCP Foundation
- 29: Service MCP Tools
- 30: Dashboard MCP & External Clients
- 30.1: MCP OAuth Scope Gate
- 31: Conversation Persistence
- 31.1: MCP Authorization Spec Upgrade
- 31.2: MCP Access Control Refinement
- 32: Agent Service Chat UI
- 32.1: Agent Chat Fixes
- 33: Agent Context Hints (archive dir name: 33-agent-intelligence-audit-viewer)

For phases without a backfilled VERIFICATION.md (28, 29, 30, 31, 31.1), pull SC list from v3.0.0-ROADMAP.md and pull evidence pointers from SUMMARY files in `.planning/milestones/v3.0.0-phases/<phase-dir>/`. For phases WITH a backfilled VERIFICATION.md (30.1, 31.2, 32, 32.1, 33 — produced by Plan 39-01), use that as the primary source.
</v3_0_0_phase_list>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the per-phase NYQUIST coverage tables</name>
  <files>
    .planning/phases/39-verification-backfill-deferred-items-triage/v3.0.0-NYQUIST.md
  </files>
  <action>
1. Read v3.0.0-ROADMAP.md and extract the Goal + Success Criteria list for every phase listed in `<v3_0_0_phase_list>`.

2. For each phase, build a per-phase table:

```markdown
### Phase <N>: <name>

**Goal:** <one line from ROADMAP>

| SC # | Success Criterion | Status | Evidence |
|------|-------------------|--------|----------|
| 1 | <SC text> | automated | tests/path/to/file.test.ts (<n> tests pass per 30.1-VERIFICATION.md) |
| 2 | <SC text> | manual UAT | UAT 2026-04-22 per 32-UAT.md §"Drawer flow" |
| 3 | <SC text> | untested | no evidence found in 32-*-SUMMARY.md or tests/agent/ |
```

3. Source-of-truth cascade per row:
   - First check the phase's VERIFICATION.md (if Plan 39-01 produced one) — its Observable Truths table is canonical.
   - For phases without VERIFICATION.md (28, 29, 30, 31, 31.1), grep SUMMARY.md files for test-file references and UAT outcomes. Cite the SUMMARY pointer.
   - If no evidence is found anywhere, status is `untested` with explicit "no evidence found in <files searched>".

4. Begin v3.0.0-NYQUIST.md with frontmatter:

```yaml
---
report: v3.0.0 Nyquist Coverage
generated: <ISO timestamp>
total_phases: 10
total_success_criteria: <N>
coverage:
  automated: <N>
  manual_uat: <N>
  untested: <N>
  automated_pct: <X>%
  manual_uat_pct: <X>%
  untested_pct: <X>%
---
```

5. Document body order:
   - Coverage Summary (top-of-file, computed in Task 2)
   - Per-phase tables (in numerical phase order)
   - Untested + Blocking section (cross-ref TRIAGE.md, Task 2)
   - Notes / Methodology footer
  </action>
  <verify>
    <automated>F=.planning/phases/39-verification-backfill-deferred-items-triage/v3.0.0-NYQUIST.md; test -f "$F" &amp;&amp; for p in "Phase 28" "Phase 29" "Phase 30:" "Phase 30.1" "Phase 31:" "Phase 31.1" "Phase 31.2" "Phase 32:" "Phase 32.1" "Phase 33"; do grep -q "$p" "$F" || (echo "FAIL: missing $p"; exit 1); done</automated>
  </verify>
  <done>v3.0.0-NYQUIST.md exists with a per-phase table for every one of the 10 v3.0.0 phases. Every SC has a row with status (automated/manual UAT/untested) and an evidence pointer.</done>
</task>

<task type="auto">
  <name>Task 2: Compute aggregate coverage and cross-ref TRIAGE.md for blocking gaps</name>
  <files>
    .planning/phases/39-verification-backfill-deferred-items-triage/v3.0.0-NYQUIST.md
  </files>
  <action>
1. Count rows by status across the entire report:
   - `automated` count
   - `manual UAT` count
   - `untested` count
   - Compute percentages of the total SC count
   - Update the frontmatter `coverage:` block with actuals

2. Insert a **Coverage Summary** section near the top of the document body:

```markdown
## Coverage Summary

| Status | Count | % of total |
|--------|-------|-----------|
| Automated | <N> | <X>% |
| Manual UAT | <N> | <X>% |
| Untested | <N> | <X>% |
| **Total SCs** | <N> | 100% |

**Phases with full automated coverage:** <list>
**Phases with any untested SCs:** <list>
```

3. Read TRIAGE.md (Plan 39-02 output). Cross-reference:
   - For every `untested` row in NYQUIST, check whether TRIAGE.md classifies the related concern.
   - If an `untested` SC corresponds to a TRIAGE entry classified "Promote to 39.1", note that in the row's evidence cell as `untested -> promoted to 39.1 per TRIAGE.md DI-<id>`.

4. Insert an **Untested + Blocking** section near the bottom:

```markdown
## Untested + Blocking (input to 39.1)

Cross-referenced against TRIAGE.md classifications. SCs in this list are both `untested` AND involve a workflow that TRIAGE.md flagged as blocking. They should be addressed by the 39.1 phase if it is created.

| Phase | SC # | SC Text | TRIAGE entry |
|-------|------|---------|--------------|
| ... | ... | ... | DI-<id> |
```

If no untested+blocking rows exist, write "None — every untested SC is non-blocking per TRIAGE.md classifications."

5. Insert a **Methodology** footer:

```markdown
## Methodology

- Coverage data sourced from VERIFICATION.md files (Phase 39-01) for phases 30.1, 31.2, 32, 32.1, 33.
- For phases 28, 29, 30, 31, 31.1 (no VERIFICATION.md backfill — explicitly out of scope per 39-CONTEXT.md), evidence pointers come from SUMMARY.md files in .planning/milestones/v3.0.0-phases/.
- Test status definitions: `automated` = matching test file exists; `manual UAT` = documented UAT outcome; `untested` = no evidence found.
- Project-wide v2.x scope is explicitly out of scope per 39-CONTEXT.md.

_Generated: <ISO timestamp> by gsd-planner Plan 39-03 / VER-02._
```

6. NOTE on phase-list scope: 39-CONTEXT.md `<decisions>` block says VERIFICATION.md backfill is for 30.1, 31.2, 32, 32.1, 33 only. The Nyquist report covers ALL of v3.0.0 per VER-02 ("Nyquist validation coverage report for v3.0.0 listing each SC and test status"). Phases 28, 29, 30, 31, 31.1 are in the Nyquist scope even though they did not get fresh VERIFICATION.md files; cite their existing SUMMARYs.
  </action>
  <verify>
    <automated>F=.planning/phases/39-verification-backfill-deferred-items-triage/v3.0.0-NYQUIST.md; grep -q "## Coverage Summary" "$F" &amp;&amp; grep -q "## Untested + Blocking" "$F" &amp;&amp; grep -q "## Methodology" "$F" &amp;&amp; grep -qE "automated_pct: [0-9]+" "$F"</automated>
  </verify>
  <done>NYQUIST report has Coverage Summary at top, Untested+Blocking cross-reference section, and Methodology footer. Frontmatter coverage counts match the body. Untested rows that match blocking TRIAGE entries are flagged with the DI-id reference.</done>
</task>

</tasks>

<verification>
Phase-level checks for this plan:
- v3.0.0-NYQUIST.md exists at the canonical path.
- Every v3.0.0 phase (10 total) has a per-phase table with at least one SC row.
- Every row has a non-empty status and evidence column.
- Frontmatter `coverage:` counts equal the body row counts.
- Cross-references to TRIAGE.md are present for untested+blocking rows.
</verification>

<success_criteria>
- VER-02 satisfied: v3.0.0-NYQUIST.md ships with full coverage report.
- Auditable trail exists: each row maps to a SUMMARY/VERIFICATION/test file.
- Untested-blocking rows are explicitly tied to 39.1 promotion candidates via TRIAGE.md cross-reference.
</success_criteria>

<output>
After completion, create `.planning/phases/39-verification-backfill-deferred-items-triage/39-03-SUMMARY.md` listing:
- Total SCs counted across v3.0.0
- Coverage breakdown (automated / manual UAT / untested counts + percentages)
- Number of untested-blocking rows that flow into 39.1
- Confirmation that VER-01, VER-02, VER-03 are now all satisfied (cross-cite Plans 39-01, 39-02, 39-03 outputs)
</output>
</content>
</invoke>