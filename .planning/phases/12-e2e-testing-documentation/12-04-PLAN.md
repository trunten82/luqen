---
phase: 12-e2e-testing-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "README.md contains a '## Built on' section before the License section"
    - "Section groups upstream components by role: Accessibility scanning, Web framework, Frontend, Database, Security, AI / LLM, PDF, Scheduling, Testing"
    - "Each component links to its official upstream repository or homepage"
    - "Section reads as a reference, not marketing prose — concise name + link + one-line role"
  artifacts:
    - path: "README.md"
      provides: "Built on section at end of README before License"
      contains: "## Built on"
  key_links:
    - from: "README.md"
      to: "External upstream projects"
      via: "Markdown links in Built on section"
      pattern: "\\[pa11y\\]"
---

<objective>
Add a "Built on" section to the project root README.md that documents all upstream components grouped by role, with links to official repositories.

Purpose: Satisfies DOC-01 — gives users and contributors visibility into Luqen's upstream dependencies and helps them understand the technology stack at a glance.
Output: A new "## Built on" section inserted in README.md between "## Test Suite" and "## License".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/root/luqen/.planning/PROJECT.md
@/root/luqen/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert "Built on" section into README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — confirm exact line of "## Test Suite" and "## License" sections before inserting)
  </read_first>
  <action>
    Read README.md and insert the following section between the closing `---` after "## Test Suite" and the `## License` heading.

    The "## Test Suite" section ends with:
    ```
    ---

    ## License
    ```

    Insert the new section so the file ends as:
    ```
    ---

    ## Built on

    Luqen is built on proven open-source components. The table below lists every direct upstream dependency grouped by the role it plays in the platform.

    ### Accessibility scanning

    | Component | Role |
    |-----------|------|
    | [pa11y](https://github.com/pa11y/pa11y) | Core WCAG scanner — Luqen drives pa11y directly, no webservice required |
    | [axe-core](https://github.com/dequelabs/axe-core) | Secondary accessibility rule engine (used via pa11y) |
    | [Puppeteer](https://github.com/puppeteer/puppeteer) | Headless Chrome for page rendering during scans |

    ### Web framework

    | Component | Role |
    |-----------|------|
    | [Fastify](https://fastify.dev) | HTTP server for the dashboard and all microservices |
    | [@fastify/secure-session](https://github.com/fastify/fastify-secure-session) | Encrypted cookie sessions (AES-256-GCM) |
    | [@fastify/csrf-protection](https://github.com/fastify/csrf-protection) | CSRF token generation and verification |
    | [@fastify/helmet](https://github.com/fastify/fastify-helmet) | Security headers (CSP, HSTS, X-Frame-Options) |
    | [@fastify/rate-limit](https://github.com/fastify/fastify-rate-limit) | Per-route rate limiting |
    | [@fastify/multipart](https://github.com/fastify/fastify-multipart) | File upload handling (CSS import, fixtures) |
    | [@fastify/swagger](https://github.com/fastify/fastify-swagger) | OpenAPI spec generation for the LLM service |

    ### Frontend

    | Component | Role |
    |-----------|------|
    | [HTMX](https://htmx.org) | Hypermedia-driven UI — partial page swaps without a JS build step |
    | [Handlebars](https://handlebarsjs.com) | Server-side HTML templating |

    ### Database

    | Component | Role |
    |-----------|------|
    | [better-sqlite3](https://github.com/WiseLibs/better-sqlite3) | Synchronous SQLite driver — primary storage backend |
    | [ioredis](https://github.com/redis/ioredis) | Redis client for job queues and distributed rate limiting |

    ### Auth & security

    | Component | Role |
    |-----------|------|
    | [jose](https://github.com/panva/jose) | RS256 JWT signing and verification for OAuth2 service auth |
    | [bcrypt](https://github.com/kelektiv/node.bcrypt.js) | Password hashing for local user accounts |
    | [Zod](https://zod.dev) | Runtime schema validation for API inputs |

    ### AI / LLM

    | Component | Role |
    |-----------|------|
    | [Ollama](https://ollama.com) | Local LLM inference — default provider for the `@luqen/llm` service |
    | [OpenAI Node SDK](https://github.com/openai/openai-node) | API client for OpenAI and any OpenAI-compatible endpoint |

    ### Export & reporting

    | Component | Role |
    |-----------|------|
    | [PDFKit](https://pdfkit.org) | Server-side PDF report generation (no Chromium dependency) |
    | [ExcelJS](https://github.com/exceljs/exceljs) | Excel export for scan results and trends |

    ### Testing

    | Component | Role |
    |-----------|------|
    | [Vitest](https://vitest.dev) | Unit and integration test runner |
    | [Playwright](https://playwright.dev) | End-to-end browser testing |

    ---

    ## License
    ```

    Important implementation notes:
    - Use the Edit tool to insert the section — do NOT rewrite the entire README
    - Find the exact text `---\n\n## License` in the file and replace it with the full new section text above followed by `## License`
    - Preserve all existing content before and after the insertion point exactly
    - Do not change any existing heading levels, links, or formatting elsewhere in the file
  </action>
  <verify>
    <automated>grep -n "## Built on" /root/luqen/README.md && grep -c "| \[" /root/luqen/README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Built on" /root/luqen/README.md returns exactly 1 match
    - grep "pa11y" /root/luqen/README.md returns at least 2 lines (existing mention + Built on table)
    - grep "HTMX" /root/luqen/README.md returns at least 2 lines (existing mention + Built on table)
    - grep "better-sqlite3" /root/luqen/README.md returns at least 1 line in Built on section
    - grep "Ollama" /root/luqen/README.md returns at least 2 lines
    - grep "Vitest" /root/luqen/README.md returns at least 1 match in Built on section
    - grep "PDFKit" /root/luqen/README.md returns at least 1 match in Built on section
    - grep "ExcelJS" /root/luqen/README.md returns at least 1 match in Built on section
    - grep "## License" /root/luqen/README.md still returns exactly 1 match (not duplicated)
    - wc -l /root/luqen/README.md shows file is larger than original 446 lines
  </acceptance_criteria>
  <done>README.md has a "## Built on" section with all upstream components grouped by role, each linked to official upstream. License section is untouched. File is valid Markdown.</done>
</task>

</tasks>

<verification>
grep "## Built on" /root/luqen/README.md
grep -A5 "## Built on" /root/luqen/README.md
grep "## License" /root/luqen/README.md
</verification>

<success_criteria>
1. README.md has a "## Built on" section inserted between Test Suite and License
2. All major upstream components appear: pa11y, axe-core, Fastify, HTMX, Handlebars, better-sqlite3, ioredis, jose, bcrypt, Zod, Ollama, PDFKit, ExcelJS, Vitest, Playwright
3. Each component links to its official repository or homepage
4. No existing README content was altered
</success_criteria>

<output>
After completion, create /root/luqen/.planning/phases/12-e2e-testing-documentation/12-04-SUMMARY.md
</output>
