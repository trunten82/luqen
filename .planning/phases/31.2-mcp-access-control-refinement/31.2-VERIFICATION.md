---
phase: 31.2-mcp-access-control-refinement
verified: 2026-04-25T00:00:00Z
status: human_needed
score: 6/6 SCs PASS at code-evidence layer; deferred-items.md present (pre-existing pre-31.2 failures)
overrides_applied: 0
requirements_coverage:
  - id: MCPAUTH-04
    status: SATISFIED
  - id: MCPAUTH-05
    status: SATISFIED
---

# Phase 31.2: MCP Access Control Refinement — Verification Report (Backfill)

**Phase Goal (v3.0.0-ROADMAP.md):** Refine MCP access control so (a) any dashboard user connecting via MCP requires an explicit `mcp.use` RBAC permission per-org, (b) tool visibility follows the user's real org-scoped RBAC rather than broad OAuth scope bundles, (c) org admins can revoke DCR'd OAuth clients registered within their own org without needing system-admin. Closes G1/G9/G10 from Phase 31.1 smoke.

**Verified:** 2026-04-25 (lightweight backfill per Phase 39 / VER-01)
**Status:** human_needed (every SC has automated/code evidence per the 5 plan SUMMARYs; `deferred-items.md` is present so status is at minimum `human_needed` per Phase 39 backfill rule)
**Re-verification:** No — backfill of a phase that shipped without VERIFICATION.md

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A dashboard user lacking `mcp.use` in their active org cannot complete `/oauth/authorize` — sees a clear "you don't have MCP access in this org" screen rather than a silent flow completion | PASS | `31.2-02-SUMMARY.md` §"mcp.use gate placement"; gate at GET `/oauth/authorize` step 8 (`authorize.ts` lines 158-183) renders `noMcpUse` consent variant; POST `/oauth/authorize/consent` re-enforces gate at step 5 (lines 332-339) returning 403 access_denied with `error_description: 'mcp.use permission required in active org'`. Tests A, B, C, D, F, G in `tests/routes/oauth/authorize.test.ts` (commits `dd72ff7` RED → `749072d` GREEN). admin.system bypass per D-02. |
| 2 | `mcp.use` is grantable per-user-per-org via dashboard admin UI; default-ON for admin roles, default-OFF for new user roles; migration back-fills existing admin users only | PASS | `31.2-01-SUMMARY.md` §Accomplishments; `mcp.use` added to `ALL_PERMISSIONS` (Administration group) so it auto-renders in `/admin/roles` checkbox UI via `getPermissionGroups`; migration 054 `backfill-mcp-use-permission` idempotently seeds `(role_id, 'mcp.use')` onto every existing role (day-1 continuity); ORG_*_PERMISSIONS templates intentionally NOT extended (D-04 requires explicit-grant for post-31.2 roles). 6 migration tests in `tests/db/sqlite/migrations-054.test.ts` (commits `b41d8ce` RED → `7d44cb4` GREEN). |
| 3 | An org admin (`admin.org`, without `admin.system`) can revoke DCR'd OAuth clients that belong to their org via `/admin/clients` — cross-org revocation is forbidden and tested | PASS | `31.2-04-SUMMARY.md` §"Files modified" + §"Key decisions honoured"; GET `/admin/clients` preHandler relaxed to `admin.system OR admin.org` (D-19); admin.org branch uses `findByOrg(currentOrgId)` (added in Plan 01); cross-org revoke attempts emit `agent_audit_log` row `tool_name='admin.clients.cross_org_revoke_attempt'` and 403 (D-21); 13 tests in `tests/routes/admin/clients.test.ts` (Tests 1-6 + A-E) all pass. Soft-revoke via `UPDATE revoked_at` with refresh-token cascade (D-20 bullets 1+2). |
| 4 | Compliance, branding, and LLM service MCP 401 responses include `WWW-Authenticate: Bearer resource_metadata="..."` header pointing at each service's `.well-known/oauth-protected-resource` | PASS | `31.2-05-SUMMARY.md` §Accomplishments; per-service middleware factories now take REQUIRED `{ resourceMetadataUrl }` option; both 401 branches (missing-bearer + verify-failure) emit the header; verify-failure 401s carry RFC 6750 `error="invalid_token"` suffix. 16 new tests across `compliance/tests/auth/middleware.test.ts` (new), `branding/tests/auth/middleware.test.ts` (new), `llm/tests/auth/middleware.test.ts` (extended). 570 + 94 + 272 tests pass; tsc clean on all three packages. URL derived from same env var as well-known route → single source of truth. |
| 5 | An external MCP client's token — whether issued directly to Claude Desktop or minted per-request by Phase 32's agent — scopes tool visibility via the user's real RBAC (not the broad OAuth scope bundle), so a developer token gets exactly the tools the developer could invoke in the dashboard UI | PASS | `31.2-03-SUMMARY.md` §"Composition Rule"; new `filterToolsByRbac` exported from `@luqen/core/mcp`; `http-plugin.ts` `ListToolsRequestSchema` now intersects `filterToolsByScope ∩ filterToolsByRbac` for authenticated end users (`ctx.permissions.size > 0`); per-tool runtime guard via `CallToolRequestSchema` override (D-08 last-line-of-defense) returns Forbidden envelope when caller lacks `requiredPermission`. S2S empty-perms callers still use scope-only (Phase 30.1 invariant preserved — Test F). 6 new core integration tests + 5 dashboard drift-prevention tests; commits `6aa2b4b`, `8cd9d4b`, `1e98806`, `c30db20`, `771f880`. |
| 6 | All Phase 31.1 E2E smoke steps still pass after Phase 31.2 ships — this phase REFINES access control without regressing any of the 10 smoke-verified behaviors | PARTIAL | `31.2-04-SUMMARY.md` §"SMOKE-CHECKLIST.md delta" notes Step 9 timing tightened ("immediately on next tool call" via D-20 bullet 3) and Step 10 unchanged. Plan 03 SUMMARY explicitly walks through Phase 31.1 SMOKE Step 4 (admin sees scan_site, viewer doesn't) and shows it still holds under the new RBAC ∩ scope intersection (Tests A/B/C in http-plugin.test.ts). However, full live re-run of the 10-step Phase 31.1 SMOKE-CHECKLIST against Phase 31.2 was not captured as a separate evidence file — the SMOKE-CHECKLIST.md edit was deferred per Plan 04 SUMMARY ("Copy update to SMOKE-CHECKLIST.md deferred — not load-bearing; noted here"). See Gaps. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `mcp.use` in `ALL_PERMISSIONS` (Administration group) | Permission catalogue entry | VERIFIED | 31.2-01-SUMMARY §Self-Check; `packages/dashboard/src/permissions.ts` |
| Migration 054 `backfill-mcp-use-permission` | Idempotent INSERT OR IGNORE on every existing role | VERIFIED | 31.2-01-SUMMARY §Accomplishments; `packages/dashboard/src/db/sqlite/migrations.ts` |
| `OauthClientRepository.findByOrg(orgId)` | Org-scoped DCR client visibility | VERIFIED | 31.2-01-SUMMARY §Files Created/Modified; team_members JOIN; revoked rows kept visible (D-24) |
| `/oauth/authorize` mcp.use gate (GET + POST) | 2-layer defense; admin.system bypass | VERIFIED | 31.2-02-SUMMARY §"mcp.use gate placement" |
| POST `/session/switch-org` route + `noMcpUse` consent variant | Switch-org CTA when user lacks mcp.use elsewhere | VERIFIED | 31.2-02-SUMMARY §"POST /session/switch-org route summary"; 6 tests in `tests/routes/auth-switch-org.test.ts` |
| Scope narrowing (`{read, write}` only; admin.* → 400) | `VALID_SCOPES` shrunk; well-known metadata aligned | VERIFIED | 31.2-02-SUMMARY §"Scope narrowing (D-10)" + `well-known.test.ts` Test 8 |
| `client_credentials` retired from dashboard /oauth/token | 400 unsupported_grant_type; per-service /oauth/token UNCHANGED | VERIFIED | 31.2-02-SUMMARY §"client_credentials removal"; `grep` confirms no internal caller pointed at dashboard /oauth/token |
| `client_id` JWT claim REQUIRED in `MintAccessTokenInput` | Compile-time enforcement; payload includes `client_id` | VERIFIED | 31.2-02-SUMMARY §"clientId threading"; `oauth-signer.ts` |
| `recordRegistrationUser(clientId, userId)` first-consent backfill | Atomic IS NULL guard | VERIFIED | 31.2-02-SUMMARY §"First-consent backfill SQL" |
| `filterToolsByRbac` + http-plugin intersection + tools/call guard | Defense-in-depth composition | VERIFIED | 31.2-03-SUMMARY §"Composition Rule" |
| Dashboard tool-metadata drift test | 5-test lock-in suite | VERIFIED | 31.2-03-SUMMARY §"Drift-Prevention Test Inventory"; new `tests/mcp/tool-metadata-drift.test.ts` |
| `/admin/clients` org-scoped view + Revoked + Org columns | D-19 + D-24 | VERIFIED | 31.2-04-SUMMARY §"Files modified"; `views/admin/clients.hbs` |
| Soft-revoke + refresh-cascade + verifier-side revoked-client check | D-20 bullets 1+2+3 | VERIFIED | 31.2-04-SUMMARY §"Key decisions honoured"; `mcp/middleware.ts` post-JWT `findByClientId(payload.client_id)` |
| Cross-org revoke audit row | `agent_audit_log` `tool_name='admin.clients.cross_org_revoke_attempt'` | VERIFIED | 31.2-04-SUMMARY §"Files modified" |
| Service-side WWW-Authenticate parity | compliance/branding/llm middleware factories | VERIFIED | 31.2-05-SUMMARY §Accomplishments |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `/oauth/authorize` GET step 8 | `noMcpUse` view | `resolveEffectivePermissions` → `perms.has('mcp.use')` check | WIRED (Plan 02) |
| `/oauth/authorize/consent` POST step 5 | 403 access_denied | re-resolved perms; admin.system bypass | WIRED (Plan 02) |
| `noMcpUse` view "Switch" forms | POST `/session/switch-org` | body-based `safeReturnTo`; CSRF preHandler bound globally | WIRED (Plan 02) |
| `/admin/clients` GET preHandler | `admin.system OR admin.org` | dropped `compliance.view` | WIRED (Plan 04) |
| admin.org branch | `findByOrg(currentOrgId)` | team_members JOIN | WIRED (Plan 01 → consumed by Plan 04) |
| POST `/admin/clients/:id/revoke` cross-org | 403 + audit row | check `client.orgId === currentOrgId`; `agent_audit_log.append` | WIRED (Plan 04) |
| `mcp/middleware.ts` post-verify | revoked-client 401 | `findByClientId(payload.client_id)` → `revokedAt !== null` → 401 + `WWW-Authenticate: ..., error="invalid_token", error_description="client_revoked"` | WIRED (Plan 04 D-20 bullet 3) |
| `http-plugin.ts` ListTools handler | RBAC ∩ scope intersection | `filterToolsByScope` ∩ `filterToolsByRbac` when `ctx.permissions.size > 0` | WIRED (Plan 03) |
| `http-plugin.ts` CallTool handler | per-tool runtime guard | check `meta.requiredPermission ∈ ctx.permissions`; delegate to captured SDK handler | WIRED (Plan 03) |
| compliance/branding/llm 401 | `WWW-Authenticate` header | `createAuthMiddleware(verifier, { resourceMetadataUrl })` REQUIRED option | WIRED (Plan 05) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Plan 01 — migrations + findByOrg | `pnpm -F @luqen/dashboard test -- rbac roles effective-permissions migrations-054 oauth-client-repository --run` | 92/92 pass per 31.2-01-SUMMARY | PASS |
| Plan 02 — authorize + switch-org + signer + token + well-known | scoped `pnpm -F @luqen/dashboard test` | 155/155 pass per 31.2-02-SUMMARY | PASS |
| Plan 03 — core RBAC ∩ scope + drift | `@luqen/core` 251/251; `@luqen/dashboard tests/mcp/` 85/85 (was 80) per 31.2-03-SUMMARY | PASS |
| Plan 04 — admin clients + middleware revoke | `tests/routes/admin/clients.test.ts` 13 + `tests/repositories/oauth-client-repository.test.ts` 29 + `tests/mcp/middleware-revoke.test.ts` 4 + `tests/mcp/middleware.test.ts` (extended helper) + `tests/routes/admin/clients-dcr.test.ts` 5 — all green per 31.2-04-SUMMARY | PASS |
| Plan 05 — service WWW-Authenticate parity | `compliance` 570/570; `branding` 94/94; `llm` 272/272; tsc clean on all three per 31.2-05-SUMMARY | PASS |
| TDD compliance | Per 31.2-DISCUSSION-LOG.md + each plan SUMMARY | RED commit precedes GREEN commit on every task in Plans 01, 02, 03, 04, 05 | PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| MCPAUTH-04 | 31.2-01, 31.2-02, 31.2-03, 31.2-04, 31.2-05 | mcp.use RBAC gate + RBAC ∩ scope tool visibility + service-side WWW-Authenticate parity | SATISFIED | All 6 SCs above; aggregate of all 5 plan SUMMARYs |
| MCPAUTH-05 | 31.2-01, 31.2-02, 31.2-04 | Org-scoped DCR client revoke (org admin can revoke own org's clients without admin.system) | SATISFIED | SC-3 evidence; 31.2-04-SUMMARY §"Key decisions honoured" |

### Discussion-Log Backed Decisions

The phase's design-rationale is captured in `31.2-DISCUSSION-LOG.md` (archived under `v3.0.0-phases/31.2-mcp-access-control-refinement/`). Notable decisions cited across plans:

- **D-02** admin.system bypasses per-org mcp.use check — Plan 02 GET step 8 + POST step 5 honour it.
- **D-04** Day-1 continuity: migration 054 back-fills existing roles only; new roles must explicit-grant — Plan 01 ORG_*_PERMISSIONS templates intentionally untouched.
- **D-07** Defense-in-depth RBAC ∩ scope at single choke-point (http-plugin) — Plan 03 composition rule.
- **D-08** Per-tool runtime guard as last-line-of-defense — Plan 03 CallToolRequestSchema override.
- **D-13** Pre-31.2 tokens lacking `client_id` claim pass middleware silently (back-compat) — Plan 04 middleware silent-skip.
- **D-19** /admin/clients preHandler `admin.system OR admin.org` (drop compliance.view) — Plan 04.
- **D-20** Soft-revoke (UPDATE revoked_at) + refresh-cascade + verifier-side revoked-client check — Plan 04 bullets 1+2+3.
- **D-22** Service middleware factories take REQUIRED `resourceMetadataUrl` — Plan 05.
- **D-23** Service middleware factory NOT extracted — kept three byte-near-identical factories — Plan 05.

### Deferred Items

`.planning/milestones/v3.0.0-phases/31.2-mcp-access-control-refinement/deferred-items.md` is present and lists:

1. `tests/e2e/auth-flow-e2e.test.ts` — 2 pre-existing failures on master @ `6b9c230` BEFORE any 31.2-01 changes; `returnTo` query-string mismatch from Phase 31.1 inline smoke fix `4337c8d`.
2. `tests/routes/oauth/authorize.test.ts` — RESOLVED by Plan 31.2-02 rewrite (245-line test file rewrite incidentally fixed both pre-existing failures; commit `68a4b4d`).

Both items are documented as out-of-scope for Phase 31.2 by design (scope-boundary rule for the executor). The remaining `auth-flow-e2e.test.ts` pair is routed to TRIAGE.md (Plan 39-02) for triage. **See TRIAGE.md from Plan 39-02.**

### Gaps Summary

1. **SC-6 PARTIAL** — Phase 31.1 10-step SMOKE-CHECKLIST.md was not re-run live end-to-end against Phase 31.2 as a captured artefact. Plan 04 SUMMARY notes "Copy update to SMOKE-CHECKLIST.md deferred — not load-bearing; noted here." Plan 03 walks through Step 4 in principle (RBAC ∩ scope still gives admin/viewer the same outcomes as 31.1). Code-level non-regression is locked by 4 separate test suites; observation-level smoke evidence is not committed.
2. **Pre-existing `tests/e2e/auth-flow-e2e.test.ts` failures (2)** — present in `deferred-items.md`; routed to TRIAGE.md (Plan 39-02). Not a Phase 31.2 regression; predates the phase on master.

Per the Phase 39 backfill rule "if `deferred-items.md` exists for the phase, status is at minimum `human_needed`", this VERIFICATION.md's status is `human_needed` even though every SC has code-evidence PASS.

---

_Verified: 2026-04-25 (backfill)_
_Verifier: Claude (gsd-planner backfill per Phase 39 / VER-01)_
