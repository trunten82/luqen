---
phase: 82-scheduled-executive-digest
plan: 05
type: execute
wave: 5
depends_on: ["82-04"]
files_modified:
  - /root/luqen-wordpress/includes/class-digest-page.php
  - /root/luqen-wordpress/includes/class-fleet-client.php
  - /root/luqen-wordpress/includes/class-plugin.php
  - /root/luqen-wordpress/tests/e2e/digest-page.js
autonomous: false
requirements: [DIGEST-05]
must_haves:
  truths:
    - "A WordPress admin sees a per-site Accessibility Digest page (Luqen_Digest_Page) showing the company-info header (Luqen_Report_Identity), period, what-changed, and the per-site exposure band + direction + countdowns, with a 'not legal advice' disclaimer (DIGEST-05, D-11)"
    - "The digest data is fetched from the dashboard GET /api/v1/digest?site=… (built in Plan 04) via the existing OAuth2/X-Org-Id API client — using the canonical instance_url settings key (NOT api_url — the 81-04 bug), with X-Org-Id fallback to 'system' (D-11)"
    - "An unconfigured/unreachable connection or no-digest-yet degrades silently to a notice-warning/notice-info and shows no band — never assuming a local service (D-11)"
    - "The WP digest page uses no forbidden words and renders the band as label + icon + colour (never a number); an optional 'Email this digest' uses a wp_nonce + wp_mail (D-11, D-12)"
    - "The page is verified on the real wp-test LXC with a REQUIRED green Playwright spec (tests/e2e/digest-page.js) — never via a stub harness (DIGEST-05)"
  artifacts:
    - path: "/root/luqen-wordpress/includes/class-digest-page.php"
      provides: "Luqen_Digest_Page WP admin page rendering the per-site digest from API data + company-info header"
      contains: "class Luqen_Digest_Page"
    - path: "/root/luqen-wordpress/tests/e2e/digest-page.js"
      provides: "Playwright assertions (band pill no-number, direction, disclaimer, degrade, D-12 forbidden words)"
      contains: "digest"
  key_links:
    - from: "/root/luqen-wordpress/includes/class-digest-page.php"
      to: "/api/v1/digest"
      via: "Luqen_Fleet_Client->fetch_digest() -> Luqen_API_Client->get()"
      pattern: "fetch_digest"
    - from: "/root/luqen-wordpress/includes/class-plugin.php"
      to: "Luqen_Digest_Page"
      via: "instantiation in the plugin page bootstrap"
      pattern: "Luqen_Digest_Page"
---

<objective>
Mirror the digest into the WordPress plugin (SEPARATE REPO: /root/luqen-wordpress). Add a
`Luqen_Digest_Page` WP admin page that fetches the per-site digest from the dashboard
GET /api/v1/digest?site=… (shipped in Plan 04) through the plugin's existing OAuth2/X-Org-Id API client,
renders the company-info header (reusing Luqen_Report_Identity) + period + what-changed + the per-site
exposure band/direction/countdowns + a "not legal advice" disclaimer using WordPress-native admin UI,
degrades silently when disconnected, and offers an optional "Email this digest" via wp_mail. Verified on
the real wp-test LXC with a REQUIRED Playwright spec.

Purpose: deliver DIGEST-05 — the WordPress SMB beachhead gets the same conservative digest, reusing WP
company-info. The enterprise-mode fetch ships first; local computation from wp_luqen_* is deferred (D-11).
Output: a new PHP page class, a fleet-client fetch_digest method, plugin registration, a Playwright spec,
and a passing wp-test LXC + Playwright UAT (blocking human-verify).

CROSS-REPO NOTE: ALL files in this plan live in /root/luqen-wordpress (the WP plugin, v0.32.0) — a
SEPARATE git repo with its own CI. Do NOT commit these files into /root/luqen. This plan runs directly
in /root/luqen-wordpress (no luqen worktree). Sequenced AFTER Plan 04 because it consumes the
GET /api/v1/digest endpoint Plan 04 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-scheduled-executive-digest/82-CONTEXT.md
@.planning/phases/82-scheduled-executive-digest/82-PATTERNS.md
@.planning/phases/82-scheduled-executive-digest/82-UI-SPEC.md
@.planning/phases/82-04-PLAN.md
@.planning/phases/81-jurisdiction-legal-exposure-scoring-flagship/81-04-PLAN.md

<interfaces>
Dashboard API consumed (built in Plan 04):
  GET /api/v1/digest?site={encoded_url}   (OAuth2 + X-Org-Id; org-scoped; 401 unauthenticated)
  Response: { digest: { orgId, siteUrl, period: { start, end }, sites: [ { siteUrl, hasNewScan,
             errors, warnings, notices, errorsDelta, warningsDelta, noticesDelta,
             criteriaChanges: [ { criterion, newFindings, fixedFindings } ],
             exposure: { band: "lower"|"moderate"|"elevated"|"high", drivers, asOf } | null,
             direction: "worsened"|"improved"|"unchanged" } ], generatedAt } }
  The exposure carries NO disclaimer and NO number — the WP plugin renders its OWN localised disclaimer
  and maps the band to a badge. (Confirm the exact field names against the 82-04 SUMMARY before coding.)

From /root/luqen-wordpress/includes/class-api-client.php:
  public function is_configured()
  public function get( $path, $args = array() )   // injects OAuth2 bearer + X-Org-Id header
  // returns WP_Error on failure / not-configured
  CANONICAL settings key is 'instance_url' (NOT 'api_url' — that was the 81-04 bug); X-Org-Id falls back
  to 'system'.

From /root/luqen-wordpress/includes/class-fleet-client.php:
  __construct(): $this->api = new Luqen_API_Client( ... );
  existing fetch_exposure( $site_url ) (81-04) — mirror it as fetch_digest( $site_url ) calling
  $this->api->get( '/api/v1/digest?site=' . rawurlencode( $site_url ) ) and returning the digest payload
  (or WP_Error / null).

From /root/luqen-wordpress/includes/class-exposure-page.php (the EXACT structural analog — 81-04):
  class Luqen_Exposure_Page { const PAGE; __construct -> admin_menu register_menu (add_submenu_page under
  the Luqen menu, Luqen_Settings::CAP); render() current_user_can guard + '<div class="wrap luqen-wp-...">';
  band map + icons (● Lower / ▲ Moderate / ▲▲ Elevated / ⬛ High); luqen-wp-status--{band} pill + inline
  colours; notice-warning/notice-info degrade; methodology link target _blank }.

From /root/luqen-wordpress/includes/class-report-identity.php:
  Luqen_Report_Identity::config() → org/site company identity (entity_name, contact_email, address…) for
  the digest header (reuse exactly as the VPAT/ACR docs do — D-11 "WP company-info").

From /root/luqen-wordpress/includes/class-plugin.php (page instantiation block):
  page classes instantiated (… new Luqen_Exposure_Page(); …) → ADD new Luqen_Digest_Page(); and
  require_once the new file alongside the other includes/class-*-page.php loads.

WP band colours (UI-SPEC Surface 5 — identical to Phase 81 / email / PDF — single source of truth):
  lower bg #dbeafe color #1e40af · moderate bg #fef9c3 color #92400e ·
  elevated bg #fee2e2 color #991b1b · high bg #fecaca color #7f1d1d.

Playwright analog — /root/luqen-wordpress/tests/e2e/exposure-indicator.js (81-04): BASE
  http://localhost:8881, login helper, FORBIDDEN_PATTERNS regex array (compliant/100%/lawsuit-proof/
  will be sued/fault/guarantee), per-check assertions. Mirror it as tests/e2e/digest-page.js for
  page=luqen-digest. D-12 substring traps: use baseline (not default), expired (not passed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Luqen_Digest_Page + fetch_digest + plugin registration + Playwright spec</name>
  <files>/root/luqen-wordpress/includes/class-digest-page.php, /root/luqen-wordpress/includes/class-fleet-client.php, /root/luqen-wordpress/includes/class-plugin.php, /root/luqen-wordpress/tests/e2e/digest-page.js</files>
  <read_first>
    - /root/luqen-wordpress/includes/class-exposure-page.php (ALL ~283 lines — the exact structural mirror: ABSPATH guard, const PAGE, __construct/register_menu add_submenu_page, render() current_user_can guard + .wrap, band map+icons, luqen-wp-status pill, notice-warning/info degrade, methodology link)
    - /root/luqen-wordpress/includes/class-fleet-client.php (the constructor + fetch_exposure to mirror as fetch_digest; the /api/v1/* get() call shape)
    - /root/luqen-wordpress/includes/class-api-client.php (is_configured, get, X-Org-Id injection, the instance_url settings key — NOT api_url)
    - /root/luqen-wordpress/includes/class-report-identity.php (Luqen_Report_Identity::config() — the company-info header to reuse)
    - /root/luqen-wordpress/includes/class-plugin.php (the page-instantiation + require_once block)
    - /root/luqen-wordpress/tests/e2e/exposure-indicator.js (the Playwright harness, login helper, FORBIDDEN_PATTERNS to copy verbatim + extend)
    - .planning/phases/82-scheduled-executive-digest/82-UI-SPEC.md Surface 5 (WP page structure, degrade states, band badge, Email-this-digest action) + Forbidden words
    - .planning/phases/82-scheduled-executive-digest/82-PATTERNS.md (class-digest-page.php + digest-page.js sections)
  </read_first>
  <behavior>
    - With a configured connection + a site that has digest data, the page shows: the company-info header (Luqen_Report_Identity entity name + contact); the period ("Week of … – …"); a "What changed" card (errors/warnings/notices previous→current→change table + criteria-with-most-changes list); a "What's at risk" card with the per-site band pill (icon + band label, never a number) + direction ("▲ Worsened since last period" / "▼ Improved" / "— Unchanged") + countdowns; a notice-info disclaimer ("…not legal advice…") with the methodology link; and an optional "Email this digest" form.
    - The band pill uses luqen-wp-status--{band} (sanitize_html_class) + inline band colours; band is a label, never a number.
    - Degrade (D-11): no connection configured → single notice-warning ("…unavailable. Configure your Luqen connection in Settings."); connection configured but fetch fails → notice-warning ("Digest data could not be retrieved. Check your Luqen connection settings."); connection OK but no digest yet → notice-info ("No digest scheduled yet…"). No raw API bodies/errors shown.
    - "Email this digest": a standard WP form POST guarded by wp_nonce_field('luqen_digest_email','luqen_nonce') + check_admin_referer, sending via wp_mail; no HTMX.
    - All strings use esc_html__/esc_attr/esc_url with the 'luqen-wordpress' text domain; no forbidden words; every value escaped.
    - The Playwright spec asserts the band pill (no number), the direction indicator, the disclaimer, the absence of all D-12 forbidden words, and the degraded notice-warning when unconfigured.
  </behavior>
  <action>
    1. class-fleet-client.php: add public function fetch_digest( $site_url ) mirroring fetch_exposure: if ! $this->api->is_configured() return new WP_Error( 'luqen_not_configured', 'Luqen dashboard not configured.' ); else call $this->api->get( '/api/v1/digest?site=' . rawurlencode( $site_url ) ); return WP_Error on a WP_Error response, else the decoded digest payload (or null when absent). Use the instance_url-backed API client (never api_url).
    2. class-digest-page.php: new Luqen_Digest_Page with the ABSPATH guard + file-header docblock. const PAGE = 'luqen-digest'. __construct adds admin_menu → register_menu → add_submenu_page( <same parent slug the exposure page uses>, esc_html__( 'Digest', 'luqen-wordpress' ), same label, Luqen_Settings::CAP, self::PAGE, [ $this, 'render' ] ) immediately after the Exposure entry. render(): current_user_can( Luqen_Settings::CAP ) guard; echo '<div class="wrap luqen-wp-digest">' + '<h1>' . esc_html__( 'Accessibility Digest', 'luqen-wordpress' ) . '</h1>'; render the Luqen_Report_Identity::config() company header; determine the site url (home_url('/')); call ( new Luqen_Fleet_Client() )->fetch_digest( $site_url ). On is_wp_error/null → echo the appropriate notice-warning/notice-info degrade message + return. Otherwise render the period + the "What changed" widefat table + the "What's at risk" card (band pill via luqen-wp-status--{band} + inline colour + icon + translated label; direction label; localised driver/countdown strings — map known driver keys to translated strings, never echo a raw key) + the notice-info disclaimer with the esc_url methodology link (instance_url base + /methodology/legal-exposure, target _blank) + the "Email this digest" form (wp_nonce_field + handler using wp_mail). Escape every value.
    3. class-plugin.php: add new Luqen_Digest_Page(); to the page-instantiation block and require_once the new file alongside the other class-*-page.php includes.
    4. tests/e2e/digest-page.js: create a Playwright spec mirroring exposure-indicator.js (reuse login/navigation helpers + the FORBIDDEN_PATTERNS array, extended with the digest surface). It MUST: (a) log into wp-admin + open Luqen → Digest (page=luqen-digest); (b) assert the band pill is present (selector .luqen-wp-status, a band label, NO numeric/percentage text in the pill); (c) assert a direction indicator (Worsened/Improved/Unchanged) is present; (d) assert the disclaimer "not legal advice" text is present; (e) assert the page body contains NONE of the forbidden words (compliant/100%/lawsuit-proof/will be sued/fault/guarantee), case-insensitive (D-12); (f) a degraded-state path: with the connection unconfigured/unreachable, assert the single notice-warning unavailable message renders and no band pill is present. This spec is the REQUIRED UAT artifact run in Task 2 (no "or" fallback).
  </action>
  <acceptance_criteria>
    - class-digest-page.php defines class Luqen_Digest_Page with const PAGE = 'luqen-digest', register_menu (add_submenu_page under the Luqen menu), and render() guarded by current_user_can( Luqen_Settings::CAP ).
    - The page fetches via Luqen_Fleet_Client->fetch_digest(), which calls /api/v1/digest (grep shows /api/v1/digest in class-fleet-client.php fetch_digest) using the instance_url settings key — grep -n "api_url" includes/class-digest-page.php returns nothing (81-04 bug not reintroduced).
    - The company-info header reuses Luqen_Report_Identity::config() (grep shows Luqen_Report_Identity in class-digest-page.php).
    - Degraded paths: notice-warning (unconfigured/fetch-fail) + notice-info (no digest yet) present; no band/drivers on degrade.
    - Band rendered as label + icon + colour via luqen-wp-status--{band} + inline colour, never a number.
    - All user-facing strings use esc_html__/esc_attr/esc_url ('luqen-wordpress'); no forbidden words: grep -niE 'compliant|100%|lawsuit-proof|will be sued|fault|guarantee' includes/class-digest-page.php returns nothing (D-12).
    - "Email this digest" uses wp_nonce_field('luqen_digest_email', ...) + check_admin_referer + wp_mail (grep shows all three).
    - class-plugin.php instantiates Luqen_Digest_Page.
    - tests/e2e/digest-page.js asserts: band pill (no number), direction, disclaimer, D-12 forbidden-words absence, degraded notice-warning.
    - php -l passes on each modified PHP file (wp-test LXC PHP 8.2); node --check passes on the spec.
  </acceptance_criteria>
  <verify>
    <automated>cd /root/luqen-wordpress && php -l includes/class-digest-page.php && php -l includes/class-fleet-client.php && php -l includes/class-plugin.php && node --check tests/e2e/digest-page.js</automated>
  </verify>
  <done>Luqen_Digest_Page renders the per-site digest from /api/v1/digest with the WP company-info header + band badge + direction + disclaimer + methodology link, degrades silently when disconnected, uses no forbidden words, is registered in the plugin, and the Playwright spec tests/e2e/digest-page.js exists; all three PHP files pass php -l and the spec passes node --check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: wp-test LXC + Playwright UAT of the WordPress digest page</name>
  <what-built>
    The WordPress per-site Accessibility Digest page (Luqen_Digest_Page) — a new admin page that fetches
    the site's digest (period, what-changed, exposure band + direction + countdowns) from the dashboard
    GET /api/v1/digest endpoint, renders it under the company-info header (Luqen_Report_Identity) with a
    WP-native band pill + disclaimer + methodology link + an optional "Email this digest" (wp_mail), and
    degrades silently to a notice-warning/notice-info when the Luqen connection is unconfigured/unreachable
    or no digest exists yet — plus the Playwright spec tests/e2e/digest-page.js created in Task 1.
  </what-built>
  <how-to-verify>
    Per project policy, WordPress changes MUST be verified on the wp-test LXC with the real plugin path +
    Playwright — never via a stub harness, and the Playwright run is REQUIRED (not optional). Steps:
    1. Deploy the modified plugin files to the wp-test LXC (luqen-wp-test, 192.168.3.160; wp-now on :8881)
       per the established WP test workflow (memory: reference_wp_test_lxc + feedback_wp_plugin_testing_lxc).
    2. Ensure the plugin is connected to the dev Luqen dashboard (OAuth2 client + X-Org-Id via instance_url)
       AND the dashboard is running Phase 82 Plans 01-04 (so GET /api/v1/digest returns digest data), with
       at least one digest schedule that has produced data for the site.
    3. In wp-admin, open the new Luqen → Digest submenu page. CONFIRM:
       a. The company-info header (entity name / contact) from Luqen_Report_Identity renders at the top.
       b. The period + "What changed" table (errors/warnings/notices previous→current→change + criteria
          list) render.
       c. A band pill renders with an icon + a band label (Lower/Moderate/Elevated/High) + a colour — and
          NO number/percentage anywhere; a direction indicator (Worsened/Improved/Unchanged) is shown.
       d. The "View methodology" link opens the dashboard /methodology/legal-exposure page in a new tab.
       e. The disclaimer ("…not legal advice…") is present; NONE of the forbidden words (compliant / 100% /
          pass-as-verdict / lawsuit-proof / will be sued / fault / guarantee) appear anywhere (D-12).
       f. "Email this digest" sends an email via wp_mail (nonce-guarded) without a PHP error.
    4. Disconnect the Luqen connection (or point instance_url at an unreachable URL) and reload. CONFIRM it
       degrades to a single notice-warning ("…unavailable. …") with no band, no what-changed, no methodology
       link, and no PHP errors/warnings in the debug log. Also confirm the "no digest yet" notice-info path
       if a connected-but-empty state is reachable.
    5. REQUIRED: run the Playwright spec tests/e2e/digest-page.js (created in Task 1) against the wp-test LXC
       and confirm it is GREEN. This step is mandatory — the page is not signed off without a passing
       Playwright run (no "or add an assertion" fallback).
  </how-to-verify>
  <action>Deploy the Plan-05 plugin files to the wp-test LXC and run the verification steps in <how-to-verify>, including the REQUIRED Playwright run of tests/e2e/digest-page.js; this is a blocking human-verify gate (WP changes are verified on the wp-test LXC + Playwright, never stubbed). Do not auto-advance.</action>
  <verify>
    <human-check>On the wp-test LXC, the Luqen → Digest page shows the company-info header, period, what-changed table, the band pill (label+icon+colour, no number) + direction, a working methodology link, the "not legal advice" disclaimer with no forbidden words, a working "Email this digest", and degrades to a single notice-warning when disconnected; the Playwright spec tests/e2e/digest-page.js ran on the wp-test LXC and is GREEN.</human-check>
  </verify>
  <done>Human confirms (resume-signal "approved") that the wp-test LXC validates the digest page and that the REQUIRED Playwright spec tests/e2e/digest-page.js passed green, covering the company-info header, what-changed, band pill + direction, methodology link, conservative copy (D-12), email action, and silent degraded state.</done>
  <resume-signal>Type "approved" once the wp-test LXC validates the digest page AND the required Playwright spec tests/e2e/digest-page.js is green — or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WP admin browser → Luqen_Digest_Page | Authenticated WP admin (Luqen_Settings::CAP) views the page |
| WP plugin → dashboard GET /api/v1/digest | OAuth2 + X-Org-Id call to the remote dashboard (the only data source) |
| Email this digest form | Authenticated POST → wp_mail; nonce-guarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-20 | Elevation of privilege | unauthorised WP user viewing the digest page | mitigate | render() guarded by current_user_can( Luqen_Settings::CAP ); add_submenu_page uses the same CAP as the analog pages |
| T-82-21 | Information disclosure | another org's digest shown in WP | mitigate | data comes only from GET /api/v1/digest which is org-scoped by the X-Org-Id the plugin sends (enforced dashboard-side in Plan 04); the plugin requests only its own site url |
| T-82-22 | Tampering (XSS) | malicious band/driver/criteria values from the API | mitigate | every output escaped (esc_html/esc_attr/esc_url); driver keys mapped to fixed translated strings, never echoed raw; band run through sanitize_html_class |
| T-82-23 | Spoofing/CSRF | forged "Email this digest" POST | mitigate | wp_nonce_field('luqen_digest_email') + check_admin_referer before wp_mail; current_user_can guard |
| T-82-24 | Denial of service / availability | dashboard unreachable breaking the admin page | mitigate | is_configured() + is_wp_error() guards → silent notice-warning/notice-info degraded state; never assumes a local service (WP-no-local-service) |
| T-82-25 | Repudiation | fault-asserting / false-precision copy | mitigate | D-12 forbidden-words grep + the REQUIRED Playwright spec + the blocking wp-test human-verify checkpoint; band label, never a number |
| T-82-SC | Tampering | dependency installs | mitigate | No new PHP/Composer/npm dependencies; pure plugin PHP reusing the existing API client; the Playwright spec reuses the existing tests/e2e harness |
</threat_model>

<verification>
- php -l passes on all three modified plugin files; node --check passes on tests/e2e/digest-page.js.
- Blocking wp-test LXC + REQUIRED Playwright UAT (tests/e2e/digest-page.js) confirms the company-info header, what-changed, band pill (label+icon+colour, no number) + direction, methodology link, conservative disclaimer, no forbidden words, the email action, and the silent degraded state.
- The instance_url settings key is used (api_url bug not reintroduced).
</verification>

<success_criteria>
- DIGEST-05: a WordPress site produces a per-site digest reusing WP company-info, sourced from the dashboard /api/v1/digest endpoint, with no local model computation (enterprise-mode; local-compute deferred per D-11).
- D-11 (instance_url, degrade-silent, company-info header) and D-12 (band-not-number, no forbidden words) hold; verified on the real wp-test LXC path with a REQUIRED green Playwright run (no stub).
</success_criteria>

<output>
Create `.planning/phases/82-scheduled-executive-digest/82-05-SUMMARY.md` when done.
</output>
