---
phase: 82-scheduled-executive-digest
plan: 05
type: execute
wave: 5
depends_on: ["82-04"]
files_modified:
  - packages/dashboard/src/routes/api/digest.ts
  - packages/dashboard/src/server.ts
  - packages/dashboard/openapi.json
  - packages/dashboard/docs/rbac-matrix.md
  - packages/dashboard/tests/routes/digest-api.test.ts
autonomous: true
requirements: [DIGEST-01, DIGEST-03]
must_haves:
  truths:
    - "A new org-scoped GET /api/v1/digest endpoint returns the per-site digest data (OAuth2 + X-Org-Id; 401 unauthenticated; no disclaimer, no numeric exposure) for the WordPress plugin to consume in Plan 06 (D-11)"
    - "server.ts registers digestScheduleRoutes (admin, Plan 04) + digestApiRoutes (this plan) and starts the digest sweep startDigestScheduler(storage, pluginManager) on boot next to the email scheduler (D-02, D-10)"
    - "After the route/permission changes, openapi + rbac drift snapshots are regenerated and committed so the CI drift gates stay green; /api/v1/digest appears in openapi.json and the digest.manage / admin.system gate appears in the rbac matrix (D-10)"
    - "The /api/v1/digest payload carries exposure as the ordinal band label (never a number) and NO disclaimer field — the WP plugin renders its own localised disclaimer — and emits no forbidden words (D-11, D-12)"
  artifacts:
    - path: "packages/dashboard/src/routes/api/digest.ts"
      provides: "GET /api/v1/digest org-scoped endpoint for WP"
      contains: "/api/v1/digest"
    - path: "packages/dashboard/src/server.ts"
      provides: "digest route registration + scheduler startup wiring"
      contains: "startDigestScheduler"
  key_links:
    - from: "packages/dashboard/src/server.ts"
      to: "digestScheduleRoutes + digestApiRoutes + startDigestScheduler"
      via: "route registration + scheduler startup"
      pattern: "digestScheduleRoutes|digestApiRoutes|startDigestScheduler"
    - from: "packages/dashboard/src/routes/api/digest.ts"
      to: "buildDigest"
      via: "GET /api/v1/digest handler"
      pattern: "buildDigest"
---

<objective>
Ship the org-scoped `GET /api/v1/digest` endpoint (the WordPress plugin's data source in Plan 06), wire
the digest into the running server (register `digestScheduleRoutes` from Plan 04 + the new
`digestApiRoutes`, and start the digest sweep `startDigestScheduler` from Plan 03 on boot), and regenerate
+ commit the openapi/rbac drift snapshots so CI stays green.

This plan was split out of the original Plan 04 (which exceeded the 15-file scope threshold). Plan 04
authored the admin UX + the route MODULES; this plan registers them, adds the API surface, and
regenerates the drift snapshots.

Purpose: deliver the API the WP plugin consumes (D-11) + the server wiring (D-02/D-10). Conservative
framing on the API payload (D-12). Output: routes/api/digest.ts, server.ts wiring, regenerated
openapi.json + rbac-matrix.md, and the API route test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-scheduled-executive-digest/82-CONTEXT.md
@.planning/phases/82-scheduled-executive-digest/82-PATTERNS.md
@.planning/phases/82-scheduled-executive-digest/82-UI-SPEC.md
@.planning/phases/82-scheduled-executive-digest/82-04-SUMMARY.md

<interfaces>
From Plans 01-04:
  buildDigest(storage, { orgId, siteUrl }, period): Promise<DigestData> (Plan 02)
  DigestData type (Plan 02) — each site carries currentExposure: ExposureResult|null (band + drivers + asOf + disclaimer)
  digestScheduleRoutes(server, storage, pluginManager?) — admin route module (Plan 04, NOT yet registered)
  startDigestScheduler(storage, pluginManager?, intervalMs?) → NodeJS.Timeout (Plan 03)

API route analog — packages/dashboard/src/routes/api/wp-network.ts (GET /api/v1/fleet):
  server.get('/api/v1/fleet', { config: rateLimitConfig, schema:{...} }, handler); resolves orgId from
  request.user.currentOrgId, 401 when absent; ExposureSchema is a Union (no disclaimer, no number).
  Mirror this for GET /api/v1/digest?site=… → calls buildDigest({ orgId, siteUrl: site ?? null }, latest
  period) and returns { digest: DigestData-without-disclaimer }. GET /api/v1/digest is auth-gated, so NO
  isPublicPath allowlisting in server.ts is needed.

server.ts wiring points:
  - ≈ line 1126: `await emailReportRoutes(server, storage, pluginManager);` → ADD
    `await digestScheduleRoutes(server, storage, pluginManager);` next to it (import from
    './routes/admin/digest-schedules.js').
  - next to the wp-network/other /api/v1 route registrations → ADD
    `await digestApiRoutes(server, storage);` (import from './routes/api/digest.js').
  - ≈ line 1411: `const emailTimer = startEmailScheduler(storage, pluginManager);` → ADD
    `const digestTimer = startDigestScheduler(storage, pluginManager);` (import from
    './email/digest-scheduler.js'); include digestTimer in any timer-cleanup/shutdown list emailTimer is in.

Drift gate (feedback_dashboard_ship_gotchas): after the route/permission changes, RUN
  `npm run docs:openapi && npm run docs:rbac` (this is part of the ACTION, not the verify) and commit the
  regenerated openapi.json + docs/rbac-matrix.md. The <verify> only ASSERTS (greps the generated files).
D-12 forbidden words on the API payload: the digest sites map must not introduce 'compliant'/'100%'/
  'lawsuit-proof'/'will be sued'/'fault'/'guarantee'; band is the label only, never a number.
Test discovery: tests under tests/routes/ (NOT colocated).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/v1/digest endpoint + API route test</name>
  <files>packages/dashboard/src/routes/api/digest.ts, packages/dashboard/tests/routes/digest-api.test.ts</files>
  <read_first>
    - packages/dashboard/src/routes/api/wp-network.ts (GET /api/v1/fleet — rateLimitConfig, 401-on-no-org, ExposureSchema Union without disclaimer/number, the response-mapping pattern)
    - packages/dashboard/src/services/digest-service.ts (buildDigest signature + DigestData/SiteDelta shape — currentExposure carries a disclaimer that must be stripped)
    - .planning/phases/82-scheduled-executive-digest/82-PATTERNS.md (routes/api/digest.ts patterns — querystring, response schema, disclaimer strip)
  </read_first>
  <behavior>
    - GET /api/v1/digest?site=… : OAuth2 + X-Org-Id; resolves orgId from request.user.currentOrgId (401 if absent); rate-limited; returns { digest } where each site carries band (label, never a number) + drivers + asOf + delta fields, WITHOUT the disclaimer (the WP plugin renders its own). site omitted → org-wide digest; period = the latest period (lastSentAt→now equivalent or a default recent window).
    - The response maps DigestData.sites, stripping currentExposure.disclaimer; no forbidden words; band ∈ {lower,moderate,elevated,high}.
  </behavior>
  <action>
    1. Create routes/api/digest.ts: digestApiRoutes(server, storage) exporting GET /api/v1/digest with the rateLimitConfig + a TypeBox querystring (optional site) + a response schema mirroring wp-network ExposureSchema (band Union, drivers, asOf, delta fields; NO disclaimer field). Resolve orgId from request.user.currentOrgId → 401 when absent. Call buildDigest({ orgId, siteUrl: site ?? null }, latestPeriod) and map DigestData.sites to the response (strip currentExposure.disclaimer; keep band/drivers/asOf + the delta fields).
    2. Create tests/routes/digest-api.test.ts (inject-style Fastify test): GET /api/v1/digest without auth → 401; with an authed org context → 200 + { digest } whose site exposure has a band string (one of lower/moderate/elevated/high) and NO disclaimer field and NO numeric exposure score; org-wide vs ?site= both return shapes.
  </action>
  <acceptance_criteria>
    - grep -n "/api/v1/digest" routes/api/digest.ts + "currentOrgId" + a 401 path present; the response schema has no `disclaimer` field (grep -n disclaimer routes/api/digest.ts returns nothing, or only a strip comment).
    - grep -n "buildDigest" routes/api/digest.ts matches.
    - Forbidden words absent in routes/api/digest.ts (grep -niE 'compliant|100%|lawsuit-proof|will be sued|fault|guarantee' returns nothing).
    - npx vitest run tests/routes/digest-api.test.ts passes (401 + 200 shape + no-disclaimer/no-number).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/routes/digest-api.test.ts</automated>
  </verify>
  <done>The org-scoped GET /api/v1/digest endpoint exists (OAuth2/X-Org-Id, 401 unauthenticated, no disclaimer, no number), conservative and tested; the WP plugin (Plan 06) has its data source.</done>
</task>

<task type="auto">
  <name>Task 2: Register digest routes + start the sweep in server.ts + regenerate openapi/rbac drift snapshots</name>
  <files>packages/dashboard/src/server.ts, packages/dashboard/openapi.json, packages/dashboard/docs/rbac-matrix.md</files>
  <read_first>
    - packages/dashboard/src/server.ts ≈ line 1126 (await emailReportRoutes(...) registration), the /api/v1 route registrations (wp-network), + ≈ line 1411 (const emailTimer = startEmailScheduler(...) + the shutdown/cleanup list emailTimer belongs to)
    - packages/dashboard/src/routes/admin/digest-schedules.ts (digestScheduleRoutes export — Plan 04)
    - packages/dashboard/src/routes/api/digest.ts (digestApiRoutes export — Task 1)
    - packages/dashboard/src/email/digest-scheduler.ts (startDigestScheduler export — Plan 03)
    - packages/dashboard/package.json (the docs:openapi + docs:rbac scripts)
    - .planning/phases/82-scheduled-executive-digest/82-PATTERNS.md (openapi+rbac drift section — GET /api/v1/digest is auth-gated, no isPublicPath allowlisting needed)
  </read_first>
  <behavior>
    - server.ts registers `await digestScheduleRoutes(server, storage, pluginManager);` next to emailReportRoutes, and `await digestApiRoutes(server, storage);` next to the other /api/v1 route registrations.
    - server.ts starts the digest sweep: `const digestTimer = startDigestScheduler(storage, pluginManager);` next to startEmailScheduler, and includes digestTimer in any timer-cleanup/shutdown list emailTimer is part of.
    - Running docs:openapi + docs:rbac regenerates the drift snapshots so CI drift gates stay green; the new /api/v1/digest endpoint and digest.manage permission (or the admin.system gate covering the admin routes) appear in the snapshots.
  </behavior>
  <action>
    1. In server.ts: import digestScheduleRoutes (routes/admin/digest-schedules.js), digestApiRoutes (routes/api/digest.js), startDigestScheduler (email/digest-scheduler.js). Register `await digestScheduleRoutes(server, storage, pluginManager);` next to emailReportRoutes; register `await digestApiRoutes(server, storage);` next to the wp-network/api route registrations. Add `const digestTimer = startDigestScheduler(storage, pluginManager);` next to emailTimer (and include it in any timer-cleanup/shutdown list that emailTimer is part of).
    2. RUN `npm run docs:openapi && npm run docs:rbac` from packages/dashboard and commit the regenerated openapi.json + docs/rbac-matrix.md. Confirm /api/v1/digest appears in openapi.json and digest.manage (or the admin.system gate covering it) appears in the rbac matrix. (This RUN is part of the action — the <verify> below only greps the generated artifacts, it does NOT re-run the generators.)
  </action>
  <acceptance_criteria>
    - grep -n "digestScheduleRoutes\|digestApiRoutes" packages/dashboard/src/server.ts shows both registered; grep -n "startDigestScheduler" packages/dashboard/src/server.ts shows the sweep started.
    - grep -n "/api/v1/digest" packages/dashboard/openapi.json matches (snapshot regenerated).
    - git diff --name-only shows openapi.json and docs/rbac-matrix.md regenerated (drift gate satisfied) — they are NOT left stale.
    - npm --prefix packages/dashboard run build (or typecheck) passes with the new wiring.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/dashboard && grep -q "/api/v1/digest" openapi.json && grep -qE "digestApiRoutes|digestScheduleRoutes" src/server.ts && grep -q "startDigestScheduler" src/server.ts && echo OK</automated>
  </verify>
  <done>The digest routes + sweep are wired into server.ts and the openapi/rbac drift snapshots are regenerated and committed — CI drift gates stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WP plugin / API client → GET /api/v1/digest | OAuth2 + X-Org-Id; org-scoped digest data crosses to a remote consumer |
| new API endpoint | Untrusted query input (site) crosses into buildDigest |
| server boot wiring | Registering routes + starting the unattended sweep on every boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-15 | Information disclosure | one org reading another org's digest via /api/v1/digest | mitigate | orgId resolved from request.user.currentOrgId (X-Org-Id), 401 when absent; buildDigest scoped to that org; site filtered within the org |
| T-82-18 | Denial of service | DoS via unbounded GET /api/v1/digest | mitigate | rateLimitConfig (max 120 / 1 min) mirroring wp-network; org-scoped query |
| T-82-19b | Repudiation | API payload asserting compliance/fault or numeric exposure | mitigate | D-12 forbidden-words grep on routes/api/digest.ts; band Union (label not number); disclaimer field stripped (WP renders its own) |
| T-82-26 | Tampering | stale openapi/rbac snapshots masking the new surface in CI | mitigate | docs:openapi + docs:rbac regenerated and committed; <verify> greps the generated openapi.json for /api/v1/digest |
| T-82-SC | Tampering | npm installs | mitigate | No new dependencies — reuses fastify, typebox, the existing scheduler + digest service |
</threat_model>

<verification>
- npx vitest run tests/routes/digest-api.test.ts green (401 + 200 shape + no-disclaimer/no-number).
- server.ts registers both digest route modules + starts the sweep; openapi.json + docs/rbac-matrix.md regenerated (drift gate); /api/v1/digest present.
- Forbidden words absent in routes/api/digest.ts.
</verification>

<success_criteria>
- The org-scoped GET /api/v1/digest endpoint is shipped for the WordPress plugin (Plan 06).
- The digest admin routes + API + sweep are wired into the running server; drift snapshots regenerated.
- D-02/D-10/D-11/D-12 honoured.
</success_criteria>

<output>
Create `.planning/phases/82-scheduled-executive-digest/82-05-SUMMARY.md` when done.
</output>
</content>
