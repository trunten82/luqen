---
phase: 23-brand-overview-page
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - packages/dashboard/src/routes/brand-overview.ts
  - packages/dashboard/src/views/brand-overview.hbs
  - packages/dashboard/src/views/partials/brand-overview-detail.hbs
  - packages/dashboard/src/routes/brand-overview.test.ts
  - packages/dashboard/src/static/style.css
autonomous: false
requirements:
  - BOVW-01
  - BOVW-02
  - BOVW-03

must_haves:
  truths:
    - "Selecting a site from the sidebar swaps the detail panel via HTMX without full page reload"
    - "Org-level summary card shows average score, total scored sites, improving and regressing counts"
    - "HTMX partial request returns only the #brand-detail-panel content, not the full page"
    - "Tests cover route handler for both full-page and HTMX partial requests"
    - "Tests cover org-level summary computation (average, improving, regressing)"
  artifacts:
    - path: "packages/dashboard/src/routes/brand-overview.test.ts"
      provides: "Route handler tests"
  key_links:
    - from: "views/brand-overview.hbs site links"
      to: "GET /brand-overview?site=<url>"
      via: "hx-get with hx-target=#brand-overview-content"
      pattern: "hx-get.*brand-overview.*hx-target.*brand-overview-content"
---

<objective>
Verify HTMX site-selector swap works correctly, confirm org-level summary card renders properly, and add test coverage for the brand overview route.

Purpose: BOVW-02 (HTMX site selector) and BOVW-03 (org summary) require verification that the interactivity works end-to-end. Tests ensure the route handler correctly computes aggregates and handles partial HTMX responses.

Output: Working HTMX site selector, verified org summary card, and test suite for the route.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-brand-overview-page/23-01-SUMMARY.md

@packages/dashboard/src/routes/brand-overview.ts
@packages/dashboard/src/views/brand-overview.hbs
@packages/dashboard/src/views/partials/brand-overview-detail.hbs
@packages/dashboard/src/services/sparkline.ts
@packages/dashboard/src/db/interfaces/brand-score-repository.ts
@packages/dashboard/src/auth/middleware.ts

<interfaces>
<!-- From Plan 23-01 outputs -->

From packages/dashboard/src/services/sparkline.ts:
```typescript
export function computeSparklinePoints(values: readonly number[], viewBoxW?: number, viewBoxH?: number): string;
```

From packages/dashboard/src/routes/brand-overview.ts (created in Plan 01):
```typescript
export async function brandOverviewRoutes(server: FastifyInstance, storage: StorageAdapter): Promise<void>;
// GET /brand-overview — full page
// GET /brand-overview?site=<url> — when hx-request header present, server.ts renders partial (HTMX)
```

HTMX partial rendering pattern (from server.ts lines 733-739):
```typescript
// HTMX partial requests: render template without layout
if (isHtmxRequest) {
  const templatePath = join(viewsDir, page);
  const source = readFileSync(templatePath, 'utf-8');
  const compiled = handlebars.compile(source);
  return reply.type('text/html').send(compiled(merged));
}
```
This means when `hx-request: true` header is present, the server renders the template WITHOUT the main.hbs layout wrapper. The HTMX `hx-select="#brand-overview-content"` on the client side then extracts just the content wrapper from the response. This is the existing pattern — no custom partial rendering needed in the route.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route handler tests + HTMX partial verification</name>
  <files>packages/dashboard/src/routes/brand-overview.test.ts</files>
  <behavior>
    - GET /brand-overview without auth returns 302 redirect to /login (or 401)
    - GET /brand-overview with authenticated user lacking branding.view returns 403
    - GET /brand-overview with branding.view permission returns 200 with HTML containing "brand-detail-panel"
    - GET /brand-overview?site=https://example.com with branding.view returns 200 with the selected site's detail
    - When org has no branded sites, response contains the empty state message (brandOverview.noSites i18n key or equivalent)
    - Summary card: with 3 sites scoring [80, 60, 90], avgScore = 77 (Math.round(230/3))
    - Summary card: site with delta > 0 counts as improving, delta < 0 as regressing, delta === 0 or null as stable
    - GET /brand-overview with hx-request:true header returns HTML without the full layout wrapper (no DOCTYPE)
  </behavior>
  <action>
    Create `packages/dashboard/src/routes/brand-overview.test.ts` following the test patterns used in existing route tests in the codebase (check for files like `routes/*.test.ts` or `__tests__/` directories).

    The test approach depends on the existing test infrastructure:
    - If the codebase uses `@fastify/inject` for route testing (most likely), create a test Fastify instance with mocked storage and auth
    - If there's a test helper/factory for creating authenticated requests, reuse it

    **Before writing tests**, read:
    1. Any existing route test file to understand the test setup pattern (e.g., `packages/dashboard/src/routes/__tests__/` or similar)
    2. How auth/permissions are mocked in existing tests

    **Test cases to implement:**

    1. **Permission gate**: Mock user without `branding.view` — expect 403
    2. **Happy path — full page**: Mock user with `branding.view`, mock `getLatestPerSite` returning 2 scans, mock `getHistoryForSite` returning scored entries — expect 200, response contains `brand-detail-panel`
    3. **Site selection**: Same mocks but with `?site=https://example.com` — expect 200, selected site is the one matching the query param
    4. **Empty state**: Mock `getLatestPerSite` returning [] — expect 200, response contains empty state text
    5. **Summary computation**: Mock 3 sites with scores [80, 60, 90] where first has delta +5, second has delta -3, third has delta 0 — verify summary.avgScore=77, summary.improving=1, summary.regressing=1, summary.stable=1
    6. **HTMX partial**: Send request with `hx-request: true` header — verify response does NOT contain `<!DOCTYPE` (partial rendering)

    **Important**: If the existing test infrastructure makes it impractical to test the full Fastify rendering pipeline (e.g., Handlebars requires file system access), focus on testing the data computation logic by extracting the summary computation into a testable pure function. Add a `computeOrgSummary` helper in the route file or a separate utility, and test that directly.

    If creating integration tests is the established pattern, follow it. If unit tests on extracted functions is more practical, do that. Match the codebase convention.
  </action>
  <verify>
    <automated>cd /root/luqen && npx vitest run packages/dashboard/src/routes/brand-overview.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Test file covers permission gate, happy path, site selection, empty state, summary computation, HTMX partial
    - All tests pass
    - Tests follow existing codebase test patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: HTMX site selector polish + responsive fixes</name>
  <files>packages/dashboard/src/views/brand-overview.hbs, packages/dashboard/src/views/partials/brand-overview-detail.hbs, packages/dashboard/src/routes/brand-overview.ts, packages/dashboard/src/static/style.css</files>
  <action>
    After Task 1 tests are green, verify and polish the HTMX interaction:

    1. **HTMX swap verification**: Ensure the `hx-get`, `hx-target`, `hx-select`, `hx-swap` attributes on site links work correctly:
       - `hx-get="/brand-overview?site={{encodeURIComponent this.siteUrl}}"` fetches the full page
       - `hx-target="#brand-overview-content"` targets the content wrapper (both site list + detail)
       - `hx-select="#brand-overview-content"` extracts the content wrapper from the response
       - `hx-swap="outerHTML"` replaces the entire content wrapper
       - `hx-push-url="true"` updates browser URL

    2. **Active state update**: When HTMX swaps the detail panel, the site list's active state must also update. Wrap both the site list nav and detail panel in a single swap target:

       ```html
       <div id="brand-overview-content">
         <div class="brand-overview__layout">
           <nav>...</nav>
           <div id="brand-detail-panel">...</div>
         </div>
       </div>
       ```
       Update site links: `hx-target="#brand-overview-content" hx-select="#brand-overview-content" hx-swap="outerHTML"`

       This way clicking a site re-renders both the site list (with updated active class) and the detail panel.

    3. **Responsive layout**: Verify the `brand-overview__layout` class has the mobile breakpoint in style.css:
       ```css
       .brand-overview__layout { display: grid; grid-template-columns: 280px 1fr; gap: var(--space-lg); }
       @media (max-width: 768px) { .brand-overview__layout { grid-template-columns: 1fr; } }
       ```

    4. **Mobile data-table**: The history table should use the existing mobile card pattern from style.css (the `data-table` class already has responsive styles). Ensure `<td data-label="...">` attributes are present on each cell.

    5. **Loading indicator**: Add `hx-indicator` to show a loading state during HTMX swap. Use the existing loading pattern if one exists in the codebase.

    6. **Verify `encodeURIComponent`**: Handlebars does not have a built-in `encodeURIComponent` helper. Check if one exists in server.ts helper registrations. If not, register one:
       ```typescript
       handlebars.registerHelper('encodeURIComponent', (str: string) => encodeURIComponent(str));
       ```
       Add it alongside other helper registrations in server.ts. Alternatively, since site URLs in the database are already URL-safe strings, simple URL encoding may not be needed — but special characters in query params (like `&`, `=`) could break. Use the helper for safety.
  </action>
  <verify>
    <automated>cd /root/luqen && npm run lint 2>&1 | tail -10 && npx vitest run packages/dashboard/src/routes/brand-overview.test.ts packages/dashboard/src/services/sparkline.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - HTMX site selector swaps both the site list (active state) and detail panel
    - Browser URL updates when selecting a site
    - Mobile layout collapses to single column
    - History table is readable on mobile via data-label attributes
    - encodeURIComponent helper registered if needed
    - All existing tests still pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — Brand overview page end-to-end verification</name>
  <files>n/a</files>
  <action>
    Human verifies the brand overview page end-to-end. All automation is complete in Tasks 1-2. This checkpoint confirms visual correctness, HTMX interactivity, responsive layout, and permission gating on the live running application.
  </action>
  <what-built>
    Brand overview page with HTMX site selector, org-level summary card, per-site detail panel with sparkline + sub-scores + history table, and sidebar navigation link.
  </what-built>
  <how-to-verify>
    1. Log in as a user with `branding.view` permission (Owner or Admin role)
    2. Check sidebar — "Brand Overview" link should appear in the Branding section
    3. Click "Brand Overview" — page should load at `/brand-overview`
    4. Verify org summary card at top: average score, scored sites count, improving/regressing counts
    5. Verify site list on left with scores and guideline names
    6. Click a different site in the list — detail panel should swap via HTMX (no full page reload)
    7. Verify detail panel: composite score with color class, delta arrow, sparkline SVG, 3 sub-score progress bars, issue counter, history table
    8. Resize browser to mobile width — layout should collapse to single column
    9. Log in as a user WITHOUT branding.view — navigating to `/brand-overview` should return 403
  </how-to-verify>
  <verify>
    <automated>echo "Manual UAT checkpoint — no automated verification"</automated>
  </verify>
  <done>Human confirms all 9 verification steps pass, or provides issues list for remediation</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTMX swap | Client-side HTMX extracts #brand-overview-content from server response |
| ?site= param | User-controlled query parameter selects which site detail to show |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | T (Tampering) | ?site= URL param | mitigate | Site param is matched against org-scoped data already loaded from DB — if no match, falls back to first site. No SQL injection vector (param never used in raw query). |
| T-23-06 | I (Info Disclosure) | HTMX partial response | accept | Partial response still passes through auth middleware and org-scoped queries — no data leakage beyond what the full page shows |
| T-23-07 | E (Elevation) | encodeURIComponent helper | mitigate | Helper only encodes output for URL safety — Handlebars auto-escapes HTML by default, preventing XSS in rendered template |
</threat_model>

<verification>
1. `npx vitest run packages/dashboard/src/routes/brand-overview.test.ts` — all tests pass
2. `npx vitest run packages/dashboard/src/services/sparkline.test.ts` — sparkline tests still pass
3. `npm run lint` — no new errors
4. Manual UAT: full flow from sidebar click through site selection to detail panel swap
</verification>

<success_criteria>
- HTMX site selector swaps detail panel without full reload (BOVW-02)
- Org summary card shows average, count, improving/regressing (BOVW-03)
- Test suite covers route handler, permission gate, summary computation
- Human UAT approved
</success_criteria>

<output>
After completion, create `.planning/phases/23-brand-overview-page/23-02-SUMMARY.md`
</output>
