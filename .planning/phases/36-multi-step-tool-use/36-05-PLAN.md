---
phase: 36-multi-step-tool-use
plan: 05
type: execute
wave: 3
depends_on: [36-01]
files_modified:
  - packages/dashboard/src/routes/admin/agent-audit.ts
  - packages/dashboard/src/views/admin/agent-audit.hbs
  - packages/dashboard/src/static/style.css
  - packages/dashboard/src/static/agent-audit.js
  - packages/dashboard/src/i18n/locales/en.json
  - packages/dashboard/tests/routes/admin-agent-audit.test.ts
autonomous: true
requirements: [ATOOL-04]
must_haves:
  truths:
    - "/admin/audit shows a truncated rationale (first ~80 chars) inline in each row."
    - "Clicking a row expands the full rationale and outcomeDetail in-place via a CSP-strict, no-inline-script toggle."
    - "/admin/audit filter bar accepts an outcomeDetail filter exposing the iteration_cap value."
  artifacts:
    - path: "packages/dashboard/src/routes/admin/agent-audit.ts"
      provides: "Route accepts outcomeDetail query param; renders rationale on each row"
    - path: "packages/dashboard/src/views/admin/agent-audit.hbs"
      provides: "Inline rationale truncation + expandable details cell"
    - path: "packages/dashboard/src/static/agent-audit.js"
      provides: "delegated click handler toggling [aria-expanded] on rationale rows (CSP-safe)"
  key_links:
    - from: "/admin/audit query string"
      to: "AgentAuditFilters.outcomeDetail"
      via: "z.string parse + repository filter"
      pattern: "outcomeDetail"
    - from: "agent_audit_log.rationale"
      to: "audit row UI"
      via: "Handlebars template renders truncated rationale; JS expands full text"
      pattern: "rationale"
---

<objective>
Surface rationale + outcomeDetail in the existing /admin/audit UI per D-CONTEXT "Audit UX" section: inline truncated + expandable rationale, filter by outcome AND outcomeDetail (specifically iteration_cap).

Purpose: ATOOL-04 — admins can audit *why* a tool fired, not just *that* it fired.

Output: route handler accepts new filter, view renders rationale, small dedicated agent-audit.js handles expand/collapse without inline scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-multi-step-tool-use/36-CONTEXT.md
@.planning/phases/36-multi-step-tool-use/36-01-PLAN.md
@packages/dashboard/src/routes/admin/agent-audit.ts
@packages/dashboard/src/views/admin/agent-audit.hbs
@packages/dashboard/src/db/interfaces/agent-audit-repository.ts

<interfaces>
After 36-01 ships, AgentAuditEntry has rationale: string | null and AgentAuditFilters
accepts outcomeDetail?: string. The route already supports filtering by outcome
(success/error/timeout/denied); we add outcomeDetail as a separate filter, not a
replacement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route accepts outcomeDetail filter; view renders rationale</name>
  <files>packages/dashboard/src/routes/admin/agent-audit.ts, packages/dashboard/src/views/admin/agent-audit.hbs, packages/dashboard/src/i18n/locales/en.json, packages/dashboard/tests/routes/admin-agent-audit.test.ts</files>
  <read_first>
    - packages/dashboard/src/routes/admin/agent-audit.ts (full file — AuditQuerySchema, resolveScope, list handler, CSV handler)
    - packages/dashboard/src/views/admin/agent-audit.hbs (full file — filter bar + table structure)
    - packages/dashboard/tests/routes/admin-agent-audit.test.ts (existing test patterns — likely uses fastify inject)
    - packages/dashboard/src/i18n/locales/en.json (search admin.audit. to match existing key style)
  </read_first>
  <behavior>
    - Test 1 (route filter): GET /admin/audit?outcomeDetail=iteration_cap — repository's listForOrg is called with filters.outcomeDetail === 'iteration_cap'. Test verifies via spy.
    - Test 2 (route filter — empty value): GET /admin/audit?outcomeDetail= (empty string) — outcomeDetail is omitted from filters (treated as no filter). Same for missing param.
    - Test 3 (route filter — preserved in pagination links): GET /admin/audit?outcomeDetail=iteration_cap&offset=50 renders pagination links that retain outcomeDetail=iteration_cap in their query strings.
    - Test 4 (view truncation): rendered HTML for a row with rationale length > 80 chars includes the first ~80 chars in a preview span AND the FULL text appears inside a hidden details panel attached to the row.
    - Test 5 (view null): rendered HTML for a row with rationale: null shows an em-dash placeholder, no expand button.
    - Test 6 (CSV): the CSV export includes a rationale column appended to existing columns; existing columns preserved in original order.
  </behavior>
  <action>
    Step A — Schema. In agent-audit.ts, extend AuditQuerySchema with an optional outcomeDetail field. Use a z.preprocess that maps blank-string to undefined, then z.string().trim().min(1).max(64).optional() for the inner schema. This makes Test 2 (empty string) pass without throwing.

    Step B — Filter passthrough. In the resolveScope filter-builder block (around lines 75-85, conditional spreads for from/to/userId/toolName/outcome), add a conditional spread for outcomeDetail when defined.

    Step C — View payload. In the list handler render call, pass:
      - filters.outcomeDetail through to the template,
      - outcomeDetailHints array for a datalist hint. Hardcode this constant near the top of the file: ['iteration_cap','timeout','unknown_tool','invalid_args'].

    Step D — CSV handler. Add a rationale column to the CSV header row and the per-row output. Reuse the existing csvEscape helper in this file. Replace newlines in rationale with single spaces before escaping. Append rationale as the last column to preserve existing column order.

    Step E — agent-audit.hbs filter bar. Add a new form-group AFTER the existing outcome select, structured like the existing form-groups. Use a text input with a datalist for hints. Bind value to filters.outcomeDetail. maxlength=64 to match the schema.

    Step F — agent-audit.hbs table. Add a new th cell labelled with the i18n key admin.audit.rationaleHeader. Add a new td cell at the end of each data row. When the row has a rationale string, render a button containing a truncated preview (first 80 chars) plus a hidden div containing the full rationale and the outcome detail. The button carries data-action="toggleRationale", aria-expanded="false", and aria-controls pointing to a unique audit-rationale-<id> id. When rationale is null, render an em-dash with a screen-reader label admin.audit.noRationale.

    Step G — Handlebars truncate helper. If a truncate helper is not already registered (search Handlebars.registerHelper('truncate' across the project), register it where other view helpers are wired (likely in src/views/handlebars-helpers.ts or directly in server.ts where Handlebars is configured). Implementation: function (str, len) returns String(str || '').slice(0, len) + ((String(str || '').length > len) ? '…' : '').

    Step H — Page JS attach. Reference <script src="/static/agent-audit.js" defer></script> at the bottom of agent-audit.hbs (or via the per-route layout slot — match what other admin views do).

    Step I — i18n keys. Add to en.json under the existing admin.audit.* namespace:
      - admin.audit.filterOutcomeDetail = "Outcome detail"
      - admin.audit.outcomeDetailLabel = "Outcome detail"
      - admin.audit.noRationale = "No rationale recorded"
      - admin.audit.rationaleHeader = "Rationale"
      - admin.audit.rationaleExpand = "Show full rationale"
      - admin.audit.rationaleCollapse = "Hide full rationale"

    Step J — Tests. Add Tests 1-6 to tests/routes/admin-agent-audit.test.ts. Use fastify inject and a fake AgentAuditRepository (mirror existing mocking pattern). For Tests 4-5 assert against substrings in the response body. For Test 6, assert against the CSV body.
  </action>
  <acceptance_criteria>
    - vitest run tests/routes/admin-agent-audit.test.ts: all existing + 6 new tests pass.
    - tsc --noEmit clean for routes/admin/agent-audit.ts.
    - en.json contains all 6 new keys; node -e parse smoke test passes (see verify block).
    - The CSV export now ends each data row with the rationale column; header row contains "rationale".
  </acceptance_criteria>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/routes/admin-agent-audit.test.ts</automated>
    <automated>cd packages/dashboard && npx tsc --noEmit 2>&1 | grep agent-audit | head -10</automated>
    <automated>cd packages/dashboard && node -e "const j=require('./src/i18n/locales/en.json'); ['admin.audit.filterOutcomeDetail','admin.audit.outcomeDetailLabel','admin.audit.noRationale','admin.audit.rationaleHeader','admin.audit.rationaleExpand','admin.audit.rationaleCollapse'].forEach(k=>{ if(!(k in j)) throw new Error('missing '+k); }); console.log('audit i18n OK');"</automated>
  </verify>
  <done>Route accepts outcomeDetail filter; CSV exports rationale; view renders truncated + hidden-full rationale; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: agent-audit.js expand/collapse handler + BEM CSS for rationale cell</name>
  <files>packages/dashboard/src/static/agent-audit.js, packages/dashboard/src/static/style.css</files>
  <read_first>
    - packages/dashboard/src/static/agent.js (for the delegated-click pattern reference — see lines around 637-675)
    - packages/dashboard/src/static/style.css (search for existing .text-muted, button, and admin-table styles to align tokens)
  </read_first>
  <acceptance_criteria>
    - File packages/dashboard/src/static/agent-audit.js exists.
    - It registers a single document-level click delegated listener that targets [data-action="toggleRationale"] elements only.
    - On click: toggles the corresponding [id=aria-controls-target] hidden attribute and the button's aria-expanded between "true" and "false".
    - No globals leaked; entire file is wrapped in an IIFE.
    - No innerHTML usage.
    - style.css contains a new BEM block .audit-rationale__toggle, .audit-rationale__preview, .audit-rationale__full, .audit-rationale__text using only existing design tokens (no new custom properties).
    - The expand/collapse animation respects @media (prefers-reduced-motion: reduce) — full panel transition disabled.
  </acceptance_criteria>
  <action>
    Step A — Create packages/dashboard/src/static/agent-audit.js with this structure (CSP-safe, IIFE-wrapped):
      - Add a document-level click listener.
      - In the listener, find the closest [data-action="toggleRationale"] element using closest(); bail if not found.
      - Read the button's aria-controls attribute to find the target panel id; getElementById to fetch the panel.
      - Toggle the panel's hidden attribute (panel.hidden = !panel.hidden).
      - Update the button's aria-expanded attribute to match the new visible state ("true" when shown, "false" when hidden).
      - Optionally swap the button's aria-label between admin.audit.rationaleExpand and admin.audit.rationaleCollapse — pull these from the same in-page i18n source agent.js uses (or fall back to a data-* attribute on the button containing the alternate label).

    Step B — Append a new section to style.css labelled /* Phase 36 — admin audit rationale cell */:
      - .audit-rationale__toggle: text-align left, white-space normal, display block, max-width 32ch, padding from var(--space-xs).
      - .audit-rationale__preview: color inherit, font-size var(--font-size-sm).
      - .audit-rationale__full: padding var(--space-sm), background var(--bg-secondary), border-radius var(--radius-sm), margin-top var(--space-xs), max-width 60ch.
      - .audit-rationale__text: white-space pre-wrap, word-break break-word.
      - @media (prefers-reduced-motion: reduce): no transitions on .audit-rationale__full.
      - All token names checked against existing style.css; if any token is missing, fall back to the closest existing token (e.g. --radius-sm → --radius-md).

    Step C — Do NOT add any inline event handlers in agent-audit.hbs. The data-action attribute is the only hook; agent-audit.js binds to it via delegation.
  </action>
  <verify>
    <automated>cd packages/dashboard && test -f src/static/agent-audit.js && echo "agent-audit.js present"</automated>
    <automated>cd packages/dashboard && node -e "const fs=require('fs'); const js=fs.readFileSync('src/static/agent-audit.js','utf8'); if(js.indexOf('innerHTML')>=0) throw new Error('innerHTML present'); if(js.indexOf('toggleRationale')<0) throw new Error('handler missing'); console.log('agent-audit.js OK');"</automated>
    <automated>cd packages/dashboard && grep -q "audit-rationale__" src/static/style.css && echo "audit-rationale CSS present"</automated>
  </verify>
  <done>agent-audit.js delegated handler works; toggle flips hidden + aria-expanded; CSS block added; no innerHTML, no inline scripts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>/admin/audit page now displays a Rationale column with truncated previews. Clicking a row expands the full rationale + outcomeDetail. Filter bar has a new Outcome detail input with iteration_cap as a datalist hint.</what-built>
  <how-to-verify>
    1. Run a multi-step turn against the agent that exercises tools (any prompt that triggers tool_use).
    2. Visit /admin/audit. Confirm:
       - A new "Rationale" column appears at the right of the table.
       - Recent rows show truncated previews; clicking expands the full text plus the outcomeDetail.
       - Older rows (pre-Phase 36) show em-dash (no rationale recorded) — the migration left rationale NULL.
       - Filter bar shows an "Outcome detail" input with iteration_cap, timeout, etc. in the datalist.
       - Filtering by outcomeDetail=iteration_cap returns only rows with that detail value.
       - CSV export contains a rationale column.
       - Pagination links retain the outcomeDetail filter.
    3. Verify keyboard accessibility: Tab to the toggle button, Enter expands; Esc does not collapse (browser default — acceptable); aria-expanded toggles correctly per axe-core devtools.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outcomeDetail query param | User-controlled string; reaches SQL via parameterised bind |
| rationale rendered in DOM | Originates from model output via Phase 36-03; rendered as text via Handlebars escaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-14 | Injection | outcomeDetail SQL | mitigate | z.string().max(64) input cap; parameterised bind in repo (36-01); no string concatenation in SQL |
| T-36-15 | XSS | rationale rendered in HBS | mitigate | Handlebars escapes by default; the only triple-mustache use in this plan is forbidden (verify by grep "{{{" agent-audit.hbs) |
| T-36-16 | Information Disclosure | cross-org rationale leak | mitigate | Existing /admin/audit org-scope guard (cross_org_forbidden 403 for org admins) covers the new column; no scope change |
</threat_model>

<verification>
- vitest run tests/routes/admin-agent-audit.test.ts green.
- tsc --noEmit clean.
- Manual UAT confirms expand/collapse + filter behaviour.
</verification>

<success_criteria>
- Admins can see model rationale per tool dispatch in the audit log.
- Admins can filter by outcomeDetail (specifically iteration_cap).
- CSV export contains rationale column.
- No CSP violations; all interaction goes through agent-audit.js delegated handler.
</success_criteria>

<output>
After completion, create .planning/phases/36-multi-step-tool-use/36-05-SUMMARY.md documenting: schema diff for AuditQuerySchema, view diff (filter bar + table), agent-audit.js handler structure, UAT outcome.
</output>
