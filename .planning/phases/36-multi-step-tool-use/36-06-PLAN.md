---
phase: 36-multi-step-tool-use
plan: 06
type: execute
wave: 4
depends_on: [36-03, 36-04, 36-05]
files_modified:
  - packages/dashboard/tests/agent/multi-step-tool-use.integration.test.ts
  - packages/dashboard/tests/e2e/agent-multi-step.spec.ts
autonomous: true
requirements: [ATOOL-01, ATOOL-02, ATOOL-03, ATOOL-04]
must_haves:
  truths:
    - "An end-to-end test proves SC#1: multiple tool_use blocks dispatch in parallel and results stream back together."
    - "An end-to-end test proves SC#2: a failed tool result is surfaced to the model with retry guidance and the agent recovers within the per-turn budget."
    - "An end-to-end test proves SC#3: chaining respects MAX_TOOL_ITERATIONS=5 and the cap-hit chip appears."
    - "An end-to-end test proves SC#4: rationale + outcome appear in /admin/audit filterable by tool."
  artifacts:
    - path: "packages/dashboard/tests/agent/multi-step-tool-use.integration.test.ts"
      provides: "Integration test against AgentService with real ToolDispatcher + in-memory storage"
    - path: "packages/dashboard/tests/e2e/agent-multi-step.spec.ts"
      provides: "Playwright e2e covering chip strip + audit page rationale visibility"
  key_links:
    - from: "Phase 36 success criteria 1-4"
      to: "automated tests"
      via: "one test per SC, asserting observable behaviour"
      pattern: "ATOOL-0[1-4]"
---

<objective>
Lock in the four phase success criteria with executable tests that exercise the full agent loop end-to-end. This is the Nyquist coverage step — every SC has at least one automated test.

Purpose: prevent regression and provide a verifiable trail for Phase 39's verification backfill.

Output: one integration test file (real AgentService + real ToolDispatcher + stub LLM + in-memory SQLite) and one Playwright e2e covering the visible UX surfaces (chip strip + audit page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-multi-step-tool-use/36-CONTEXT.md
@.planning/phases/36-multi-step-tool-use/36-03-PLAN.md
@.planning/phases/36-multi-step-tool-use/36-04-PLAN.md
@.planning/phases/36-multi-step-tool-use/36-05-PLAN.md
@packages/dashboard/tests/agent/agent-service.test.ts

<interfaces>
Available test infrastructure:
- in-memory SQLite via better-sqlite3 with the project's migration runner (used by repository tests).
- Stub LlmClient pattern from agent-service.test.ts (returns canned AgentStreamTurn sequences).
- Playwright config already set up for Phase 35 e2e tests (see tests/e2e/agent-history.spec.ts as the pattern reference).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration test — full AgentService loop covering all 4 SCs</name>
  <files>packages/dashboard/tests/agent/multi-step-tool-use.integration.test.ts</files>
  <read_first>
    - packages/dashboard/tests/agent/agent-service.test.ts (full file — for stub patterns and how it instantiates AgentService with real ConversationRepository / AgentAuditRepository)
    - packages/dashboard/tests/agent/agent-service-title-hook.test.ts (Phase 35 reference for fire-and-forget hook tests)
    - packages/dashboard/src/agent/agent-service.ts (lines 268-476 — runTurn body for understanding what to assert against)
  </read_first>
  <behavior>
    - Test SC1 (parallel dispatch): A stub LLM returns a turn with toolCalls = [a, b, c]. Real ToolDispatcher dispatches against a manifest of three handlers with controlled timing (a:50ms, b:10ms, c:30ms). After runTurn:
      - Three tool result rows persisted in input order (a, b, c).
      - Three audit rows in input order.
      - Wall-clock duration < 100ms (proves parallelism vs ~90ms sequential).
      - Captured emit() frames include 3 tool_started followed by 3 tool_completed (status: success).
    - Test SC2 (error retry recovery): Stub LLM scripts two iterations:
      - Iteration 0: toolCalls = [a, b] where a → timeout, b → success.
      - Iteration 1: assistant text only (final answer).
      The persisted assistant tool-result row for `a` contains the substring "retry attempt" AND mentions "timeout". The audit row for `a` has outcome='timeout', outcomeDetail='timeout'. The assistant turn completes with a 'done' frame. retriesRemaining decremented from 3 to 2 after iteration 0 (verified indirectly via behaviour on a follow-up failure path in a sibling assertion).
    - Test SC3 (multi-step iteration cap): Stub LLM scripts iterations 0-4 each returning a fresh tool_use, then iteration 5 the forced-final-answer path produces a plain text reply. Assertions:
      - 5 batches of dispatch occurred.
      - The agentAudit row for the loop cap exists with toolName='__loop__', outcome='error', outcomeDetail='iteration_cap'.
      - One tool_completed frame with toolCallId='__loop__' and errorMessage='iteration_cap' was emitted just before the final assistant text.
      - The 'done' frame fires last.
    - Test SC4 (rationale visibility): Stub LLM returns turn with text='Looking up org X to compare against the dashboard.' and toolCalls=[a]. The persisted audit row has rationale equal to that exact text. A subsequent storage.agentAudit.listForOrg call with no filter returns the row, and listForOrg with filters.toolName=a.name finds the same row.
    - Regression test (single-tool path): Single tool_use → no behaviour regression vs Phase 32 baseline. Audit row exists; tool result row exists; rationale captured if assistant text was non-empty.
  </behavior>
  <action>
    Step A — Set up the integration harness at the top of the test file. Build a real AgentService instance with:
      - ConversationRepository: real SQLite impl on :memory: with migrations 001-057 applied.
      - AgentAuditRepository: same.
      - ToolDispatcher: real instance with three test handlers (named 'tool_a', 'tool_b', 'tool_c') registered, each accepting a small zod schema and returning either a controllable success payload, or rejecting with a timeout/internal error sentinel based on an injected behaviour map. Use a per-test behaviour map to flip outcomes between tests.
      - signer: minimal stub that returns a fixed JWT (no real key needed for integration scope).
      - resolveScopes: returns ['admin.system'] for the test user.
      - Stub LlmClient implementing streamAgentConversation as a scripted sequence — accept an array of canned AgentStreamTurn responses and pop one per call.
      - resolvePermissions: returns the same permission set on every iteration.

    Step B — Write Test SC1. Use vi.useRealTimers(). Wire emit into a captured frames array. Run runTurn and await its completion. Assert ordering and timing as listed in the behaviour block.

    Step C — Write Test SC2. Script two LLM responses. After runTurn completes, query storage.conversations.getFullHistory(conversationId) and assert the tool message for 'tool_a' has toolResultJson containing a JSON object whose stringified form contains "retry attempt" and "timeout". Query storage.agentAudit.listForOrg(orgId, {}, {}) and assert the audit entry for tool_a has outcome='timeout' and outcomeDetail='timeout'.

    Step D — Write Test SC3. Script 5 iterations of tool_use plus 1 final-answer iteration. After runTurn assert the agentAudit table contains a row with toolName='__loop__', outcomeDetail='iteration_cap'. Inspect captured frames for a tool_completed frame whose toolCallId === '__loop__'.

    Step E — Write Test SC4. After runTurn, query storage.agentAudit.listForOrg(orgId, { toolName: 'tool_a' }, { limit: 10 }) and assert the returned row has rationale === 'Looking up org X to compare against the dashboard.'.

    Step F — Write the regression test for the single-tool path. Mirror the existing Phase 32 single-tool path test from agent-service.test.ts but assert the new rationale and lifecycle frame behaviour.
  </action>
  <acceptance_criteria>
    - File packages/dashboard/tests/agent/multi-step-tool-use.integration.test.ts exists and contains exactly the 5 tests above (4 SCs + 1 regression).
    - All tests pass on first run.
    - No new production code modified by this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/agent/multi-step-tool-use.integration.test.ts</automated>
  </verify>
  <done>5 integration tests, all green; covers ATOOL-01 through ATOOL-04 end-to-end at the AgentService level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright e2e — chip strip + audit rationale visibility</name>
  <files>packages/dashboard/tests/e2e/agent-multi-step.spec.ts</files>
  <read_first>
    - packages/dashboard/tests/e2e/agent-history.spec.ts (Phase 35 e2e — for setup, login, drawer-open patterns)
    - packages/dashboard/playwright.config.ts (or wherever the project's Playwright config lives — for baseURL and existing fixtures)
  </read_first>
  <behavior>
    - Test E1 (chip strip visible during parallel dispatch): Open the agent drawer, send a prompt known to trigger 2+ tool calls. Assert:
      - Within 500ms of send, the .agent-drawer__tool-chips container becomes non-empty.
      - At least 2 chips with class .agent-drawer__tool-chip--running are visible.
      - Within 5s, every chip transitions to either --success or --error.
      - Chips remain in the DOM after the assistant message settles (final message rendered, .agent-drawer__stream-status hidden, but chips still present).
    - Test E2 (chip strip cleared on new turn): After E1, send a second message. Assert the chip strip is empty when the new SSE stream opens, then populates with the new turn's chips.
    - Test E3 (audit rationale visible): Navigate to /admin/audit as an admin.system user. Filter by tool name from the test prompt. Click a row's rationale toggle. Assert the full rationale text is now visible (not hidden), aria-expanded='true' on the button. Click again to collapse.
    - Test E4 (audit outcomeDetail filter): Navigate to /admin/audit?outcomeDetail=iteration_cap. Assert the filter input is pre-populated with iteration_cap. (Run only if a real iteration_cap row exists in the test fixture, else skip with test.skip — document why.)
  </behavior>
  <action>
    Step A — Reuse the existing test login fixture from tests/e2e/agent-history.spec.ts. If a global fixture exists in playwright.config.ts use that.

    Step B — Pick a deterministic prompt that triggers multiple tools. Read the dashboard tool catalog (packages/dashboard/src/agent/system-prompt.ts or wherever tools are listed) and pick a prompt phrasing that the configured test LLM (likely a stub or local Ollama in CI) reliably routes to 2+ tools. If the test environment does not have a deterministic LLM available, mark the e2e file with test.describe.configure({ mode: 'serial' }) and skip with test.skip when the LLM provider is unset (gate via process.env.E2E_LLM_PROVIDER).

    Step C — Implement E1 with page.locator('.agent-drawer__tool-chip').count() polling. Use waitFor with a timeout to allow LLM streaming.

    Step D — Implement E2 by sending two messages back-to-back and asserting the chip strip empties between them.

    Step E — Implement E3 by navigating to /admin/audit, locating the rationale toggle button via [data-action="toggleRationale"], asserting aria-expanded transitions, and asserting the .audit-rationale__full panel's hidden attribute toggles.

    Step F — Implement E4. If no iteration_cap row exists in the test DB, gracefully skip with a test.skip annotation and a comment explaining the fixture gap. Document this gap in the SUMMARY.

    Step G — All assertions use Playwright's built-in expect(locator).toBeVisible / toHaveAttribute. No manual sleeps; use waitFor / poll instead.
  </action>
  <acceptance_criteria>
    - File packages/dashboard/tests/e2e/agent-multi-step.spec.ts exists with the 4 tests.
    - When run against a configured E2E LLM provider, all tests pass.
    - When run without an E2E LLM provider, tests E1-E2 skip gracefully (E3-E4 still run if fixture data is seeded).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/dashboard && npx playwright test tests/e2e/agent-multi-step.spec.ts --reporter=list</automated>
  </verify>
  <done>Playwright e2e file present; tests cover chip strip lifecycle + audit rationale; CI either runs them green or skips with documented reason.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end Nyquist coverage for Phase 36 success criteria. Integration tests exercise the full AgentService loop; Playwright e2e covers visible UX.</what-built>
  <how-to-verify>
    1. Run the full dashboard test suite: cd packages/dashboard && npm test. Confirm zero new failures.
    2. Run the integration test alone: npx vitest run tests/agent/multi-step-tool-use.integration.test.ts. All 5 tests green.
    3. Run the e2e suite: npx playwright test tests/e2e/agent-multi-step.spec.ts. All tests either green or skipped with a documented reason.
    4. Check that no flaky behaviour appears across 3 consecutive runs of the integration suite (timing-sensitive Test SC1).
    5. Manually inspect /admin/audit one more time after a real multi-tool turn to confirm the rationale column matches what the model said.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture LLM → AgentService | Scripted responses; test-controlled |
| Test signer / scopes | In-memory only; no real RS256 keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-17 | Tampering | flaky timing assertion | mitigate | Use realtime clocks; bound assertion to a generous wall-clock window (Test SC1 < 100ms when sequential floor is 90ms — 10ms slack) |
| T-36-18 | DoS | runaway test loop on iteration cap | mitigate | Test SC3 scripts exactly 6 LLM calls (5 iterations + final) — cannot loop indefinitely |
</threat_model>

<verification>
- vitest run tests/agent/multi-step-tool-use.integration.test.ts green.
- playwright e2e green or cleanly skipped with reason.
- npm test (full suite) green.
</verification>

<success_criteria>
- All four ATOOL success criteria covered by at least one automated test.
- Phase 39 verification can rely on these tests as the SC evidence record.
</success_criteria>

<output>
After completion, create .planning/phases/36-multi-step-tool-use/36-06-SUMMARY.md documenting: test file inventory, SC-to-test traceability matrix, any e2e skip reasons, and the final phase VERIFICATION.md outline ready for Phase 39.
</output>
