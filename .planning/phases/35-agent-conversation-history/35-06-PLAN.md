---
phase: 35
plan: 06
type: execute
wave: 5
depends_on: ["35-05"]
files_modified:
  - packages/dashboard/tests/e2e/agent-history.e2e.test.ts
  - packages/dashboard/tests/e2e/agent-history-a11y.e2e.test.ts
autonomous: true
requirements: [AHIST-01, AHIST-02, AHIST-03, AHIST-04, AHIST-05]
tags: [agent, history, e2e, a11y, playwright, wcag]

must_haves:
  truths:
    - "Playwright e2e proves list → open → search → resume → rename → delete round-trip"
    - "axe-core run on the open history panel reports zero WCAG 2.2 AA violations"
    - "Keyboard-only flow (Tab / Enter / Esc / Arrow / Shift+F10) completes resume + delete without mouse"
  artifacts:
    - path: "packages/dashboard/tests/e2e/agent-history.e2e.test.ts"
      provides: "happy-path round-trip + soft-delete audit verification"
    - path: "packages/dashboard/tests/e2e/agent-history-a11y.e2e.test.ts"
      provides: "axe-core scan of the panel in populated + empty + search-active states"
  key_links:
    - from: "e2e spec"
      to: "dashboard dev server"
      via: "existing playwright config + auth fixture"
      pattern: "test\\("
---

<objective>
Prove AHIST-01..05 end-to-end against a running dashboard with real DB + real routes. Formalise
WCAG 2.2 AA compliance via axe-core.

Scope:
1. Happy-path e2e round-trip.
2. Accessibility scan with axe-core on all three surface states (populated, empty, search-active).
3. Keyboard-only navigation smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/35-agent-conversation-history/35-CONTEXT.md
@.planning/phases/35-agent-conversation-history/35-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright round-trip e2e covering list/search/resume/rename/delete</name>
  <files>packages/dashboard/tests/e2e/agent-history.e2e.test.ts</files>
  <read_first>
    - packages/dashboard/tests/e2e/ (existing specs — auth fixture, server bootstrap, base URL)
    - packages/dashboard/playwright.config.ts (or equivalent) for project layout
    - .planning/phases/35-agent-conversation-history/35-CONTEXT.md (for fixture conversations — 21 seeded rows to prove pagination)
  </read_first>
  <action>
    Follow the project's existing e2e harness (grep `tests/e2e/*.test.ts` for the login fixture
    pattern and storage seeding helper). The spec MUST cover:

    1. **Setup**: seed 21 conversations for the test user/org (20 + 1 to force a second page), each with at least one user + one assistant message. Use the existing sqlite test fixture or the repo directly — do NOT go through `/agent/message` to keep runtime bounded.

    2. **Open + list**: log in, click History button, assert ≥20 items visible and `data-conversation-id` set on each.

    3. **Infinite scroll**: scroll list to bottom, assert 21st item loads (intersection observer fires real in browser).

    4. **Search**: type `"uniqueSeedToken123"` (embedded into one conversation's assistant reply at seed time), wait 300 ms, assert exactly 1 result row visible and `<mark>` wraps the matched token.

    5. **Resume**: click the matched row, assert URL's visible conversation ID matches, assert messages region shows the seeded history.

    6. **Rename**: open history, open three-dot menu on first row → Rename → type `"Renamed in e2e"` → Enter → assert new title persists after reload.

    7. **Delete**: open three-dot → Delete → Delete → assert row disappears; reload panel → assert row STILL absent (soft-delete filter); query DB directly → assert `is_deleted = 1` + `deleted_at` non-null; query agent_audit_log → assert a `conversation_soft_deleted` row exists with matching conversationId.

    All assertions via Playwright locators with explicit timeouts (≤ 5s). Use `test.use({ storageState })` for authenticated fixture.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx playwright test tests/e2e/agent-history.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `packages/dashboard/tests/e2e/agent-history.e2e.test.ts`
    - `grep -c "test\\(" packages/dashboard/tests/e2e/agent-history.e2e.test.ts` returns ≥7 (one per step above)
    - `grep -n "is_deleted" packages/dashboard/tests/e2e/agent-history.e2e.test.ts` returns ≥1 line (DB-level soft-delete assertion)
    - `grep -n "conversation_soft_deleted\\|agent_audit_log" packages/dashboard/tests/e2e/agent-history.e2e.test.ts` returns ≥1 line
    - Playwright run exits 0 on a fresh machine
  </acceptance_criteria>
  <done>End-to-end round-trip green against running dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: axe-core accessibility scan of history panel (populated / empty / search-active)</name>
  <files>packages/dashboard/tests/e2e/agent-history-a11y.e2e.test.ts</files>
  <read_first>
    - packages/dashboard/tests/e2e/ (existing a11y specs, if any — grep for `@axe-core/playwright`)
    - .planning/phases/35-agent-conversation-history/35-UI-SPEC.md §Keyboard & Screen-Reader Contract, §Color contrast gates
  </read_first>
  <action>
    Install `@axe-core/playwright` if not already present (check `package.json`; if absent, add as a devDependency — this is a tooling install, allowed per phase scope).

    Spec cases:

    1. **Populated panel a11y**: seed ≥3 conversations, open panel, run `new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa','wcag22aa']).analyze()` scoped to `#agent-history-panel`. Assert `violations.length === 0`. Print violations on failure.

    2. **Empty panel a11y**: no conversations, open panel, run axe — assert zero violations (empty state must be labelled + readable).

    3. **Search-active a11y**: type a query producing matches with `<mark>` highlights; run axe — assert zero violations AND assert `aria-live="polite"` live region received a results-count announcement (check textContent).

    4. **Keyboard-only round-trip**: without mouse — Tab to History button, Enter, Tab into search, type query, Tab into list, ArrowDown × N, Enter → resume. Assert focus chain matches UI-SPEC. Specifically:
       - After Enter on History button, active element is back button or first item.
       - After Esc, active element is History button again.
       - After Shift+F10 on a focused item, `role="menu"` popover is visible and `activeElement` is inside it.

    Snapshot focus-visible outline on the three-dot trigger + search input and assert the computed outline colour equals `var(--accent)` resolved (via `getComputedStyle`).
  </action>
  <verify>
    <automated>cd packages/dashboard && npx playwright test tests/e2e/agent-history-a11y.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `packages/dashboard/tests/e2e/agent-history-a11y.e2e.test.ts`
    - `grep -n "AxeBuilder\\|@axe-core/playwright" packages/dashboard/tests/e2e/agent-history-a11y.e2e.test.ts` returns ≥1 line
    - `grep -n "wcag22aa\\|wcag2aa" packages/dashboard/tests/e2e/agent-history-a11y.e2e.test.ts` returns ≥1 line
    - `grep -n "Shift+F10\\|ContextMenu\\|ArrowDown" packages/dashboard/tests/e2e/agent-history-a11y.e2e.test.ts` returns ≥1 line
    - Playwright run exits 0
    - All 4 cases green; zero axe violations reported
  </acceptance_criteria>
  <done>WCAG 2.2 AA compliance proven; keyboard flow locked behind automated gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — test code only. Inherits prior phase boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-20 | R (Repudiation) | soft-delete audit | mitigate | E2E asserts that `agent_audit_log` row exists after delete — automated evidence that audit cannot be silently skipped by a future regression |
</threat_model>

<verification>
- `cd packages/dashboard && npx playwright test tests/e2e/agent-history.e2e.test.ts tests/e2e/agent-history-a11y.e2e.test.ts` exits 0
- CI regression: full vitest + playwright suite green
</verification>

<success_criteria>
- 7 round-trip e2e cases + 4 a11y cases green
- axe-core reports zero WCAG 2.2 AA violations on all three panel states
- Keyboard-only flow provably completes resume + delete without mouse input
</success_criteria>

<output>
After completion, create `.planning/phases/35-agent-conversation-history/35-06-SUMMARY.md`
</output>
