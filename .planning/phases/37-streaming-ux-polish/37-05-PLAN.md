---
phase: 37-streaming-ux-polish
plan: 05
type: execute
wave: 4
depends_on: ["37-04"]
files_modified:
  - packages/dashboard/tests/e2e/agent-streaming-ux.spec.ts
  - packages/dashboard/src/views/agent-share-view.hbs
  - packages/dashboard/src/static/style.css
  - .planning/phases/37-streaming-ux-polish/37-VERIFICATION.md
autonomous: false
requirements: [AUX-01, AUX-02, AUX-03, AUX-04, AUX-05]
task_count: 3
tags: [e2e, playwright, verification, share-view, polish]

must_haves:
  truths:
    - "Playwright e2e covers all 5 AUX success criteria, end-to-end, against a running dashboard"
    - "Share view page is accessible (axe-core scan clean for serious + critical) and renders correctly on mobile + desktop"
    - "VERIFICATION.md captures the success criteria checklist with each outcome and a Nyquist coverage note"
  artifacts:
    - path: packages/dashboard/tests/e2e/agent-streaming-ux.spec.ts
      provides: "Playwright spec covering stop, retry, edit-resend, copy, share flows"
      min_lines: 80
    - path: .planning/phases/37-streaming-ux-polish/37-VERIFICATION.md
      provides: "Phase verification record (per Phase 39 VER-01 standard)"
  key_links:
    - from: packages/dashboard/tests/e2e/agent-streaming-ux.spec.ts
      to: packages/dashboard/src/routes/agent.ts
      via: "real HTTP requests against running dashboard server"
      pattern: "/agent/conversations.*messages.*(retry|edit-resend|share)"
---

<objective>
Close out Phase 37 with end-to-end Playwright coverage of the 5 AUX criteria, polish the share-view page (mobile responsiveness + axe-core a11y scan), and produce the phase VERIFICATION.md record per the Phase 39 standard. This plan is the formal phase exit gate.

Purpose: Plans 01-04 ship and unit-test the building blocks; this plan proves they work together against a real running dashboard, with the share-view receiving a final accessibility pass.
Output: One Playwright spec, share-view CSS polish, phase VERIFICATION.md, and a final UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-streaming-ux-polish/37-CONTEXT.md
@.planning/phases/37-streaming-ux-polish/37-01-PLAN.md
@.planning/phases/37-streaming-ux-polish/37-02-PLAN.md
@.planning/phases/37-streaming-ux-polish/37-03-PLAN.md
@.planning/phases/37-streaming-ux-polish/37-04-PLAN.md
@.planning/phases/35-agent-conversation-history/35-06-SUMMARY.md
@packages/dashboard/playwright.config.ts
@packages/dashboard/tests/e2e/

<interfaces>
<!-- Existing Playwright e2e harness from Phase 35-06: -->
- Tests run against a real fastify server started by playwright.config.ts global setup
- Auth seeded via OAuth client credentials → token cookie
- LLM provider stubbed by env var to a deterministic mock (locate via grep for AGENT_LLM_PROVIDER or LLM_STUB)
- axe-core integration: @axe-core/playwright; existing test in tests/e2e/agent-history.spec.ts (35-06)

<!-- Behavior under test, recap from Plans 03 + 04: -->
Stop → AbortSignal → markMessageStopped + audit
Retry → POST .../retry → markMessagesSuperseded + audit + new SSE
Edit-resend → POST .../edit-resend with body.content → supersede prior + new user msg + new SSE
Copy → navigator.clipboard.writeText with markdown source
Share → POST .../share → 201 { shareId, url } → GET /agent/share/:id renders read-only view (403 cross-org)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright spec covering all 5 AUX flows</name>
  <files>packages/dashboard/tests/e2e/agent-streaming-ux.spec.ts</files>
  <read_first>
    - packages/dashboard/tests/e2e/agent-history.spec.ts (Phase 35-06 — closest analog; copy harness setup, login flow, axe-core wiring)
    - packages/dashboard/playwright.config.ts (project config, global setup)
    - .planning/phases/35-agent-conversation-history/35-06-SUMMARY.md (e2e patterns + LLM stub details)
  </read_first>
  <behavior>
    Test cases (each `test()` block):
      1. AUX-01 stop persists partial: send a long prompt, click stop within 200ms of first delta, expect: stopped chip visible, page reload still shows partial text + chip, /admin/audit row exists with toolName='message_stopped'.
      2. AUX-02 retry replaces last assistant turn: send a message, wait for completion, click retry, expect: prior assistant bubble removed, new assistant text streams, /admin/audit row toolName='message_retried'.
      3. AUX-03 edit-resend branches: send "Hello", wait for reply, click edit pencil on user msg, type "Hello there", Save, expect: original "Hello" + its reply removed from DOM, "Hello there" + new reply visible, /admin/audit row toolName='message_edit_resend', GET /agent/conversations/:cid/messages?include_superseded=1 (or similar admin path) shows the superseded rows still in DB.
      4. AUX-04 copy puts markdown on clipboard: send a prompt that elicits a known markdown response (LLM stub returns deterministic `# Heading\n\n**bold**`), click copy, read clipboard via `page.evaluate(() => navigator.clipboard.readText())` (Playwright clipboard permission must be granted in context options), expect text to equal the source markdown with no HTML tags.
      5. AUX-05 share end-to-end + cross-org 403:
         a. Click share, capture toast announcement, paste URL: open new page in same browser context, expect read-only render with no composer / no action buttons.
         b. Open URL in a SECOND browser context logged in as a foreign-org user, expect HTTP 403 page.
         c. /admin/audit row toolName='share_created'.
      6. Accessibility scan: run @axe-core/playwright against the share-view page; assert zero serious + critical violations.
      7. Mobile viewport (Pixel 5 or 375x667 emulation): on share page, verify message thread renders without horizontal scroll, action row markup IS NOT present, and the header is readable.

    LLM stub: ensure the e2e harness uses a deterministic LLM stub that streams predictable text. If the existing harness uses a real LLM, gate on env (LLM_E2E_MODE=stub) and skip otherwise.

    Each test: ≤ 30 seconds wall time. Total spec: aim for ≤ 4 minutes.

    Use the same authentication helper used by tests/e2e/agent-history.spec.ts (login via UI or seeded session cookie).
  </behavior>
  <action>
    1. Read tests/e2e/agent-history.spec.ts and replicate its setup helpers (test.beforeEach, login utility).
    2. Add clipboard permission to the Playwright context: `test.use({ contextOptions: { permissions: ['clipboard-read', 'clipboard-write'] } });` or per-test `await context.grantPermissions(['clipboard-read'])`.
    3. Write the 7 test blocks above. Use locators by `data-action` attributes (CSP-strict locator strategy from Phase 36-04).
    4. Run the spec: `npx playwright test agent-streaming-ux.spec.ts --reporter=line`. All tests must pass.
    5. If a foreign-org test user is not seeded by the existing harness, add a small fixture in tests/e2e/fixtures/users.ts (or extend existing) that creates a 2nd-org user. Match the seed pattern Phase 35-06 used.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx playwright test agent-streaming-ux.spec.ts --reporter=line</automated>
  </verify>
  <done>
    All 7 e2e cases pass green twice in a row (flake check). Wall time ≤ 4 minutes. axe-core scan reports zero serious + critical violations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Share-view CSS polish + a11y fixes</name>
  <files>
    packages/dashboard/src/views/agent-share-view.hbs,
    packages/dashboard/src/static/style.css
  </files>
  <read_first>
    - packages/dashboard/src/views/agent-share-view.hbs (created in Plan 03)
    - packages/dashboard/src/static/style.css (existing .agent-msg block to extend)
    - Phase 35-06 SUMMARY for axe-core patterns
  </read_first>
  <behavior>
    Polish items:
      - Add `.agent-share` BEM block to style.css with: max-width 720px, margin-inline auto, padding var(--space-6) var(--space-4), background var(--bg-primary).
      - `.agent-share__head h1`: font-size var(--font-size-xl), margin-bottom var(--space-1).
      - `.agent-share__thread`: gap var(--space-4) between messages (use `display: flex; flex-direction: column;` if not inherited from .agent-msgs).
      - Ensure share view page works at 375px viewport: messages don't horizontal-scroll; markdown code blocks have overflow-x: auto.
      - In agent-share-view.hbs: ensure the page has a proper `<title>` element rendered via the layout slot — title = conversation title || agent.share.title_fallback.
      - Add `<meta name="robots" content="noindex,nofollow">` to the share-view layout slot — share links are auth-gated, must not appear in search results even via leak.
      - Skip-to-content link: ensure the layout's existing skip link targets the .agent-share__thread region.
      - Verify axe-core: heading order, landmark regions (`<main>` already in Plan 03), color contrast on .agent-share__head .form-hint.

    Run axe-core via Playwright (Task 1) — this task fixes anything that comes up in that scan.
  </behavior>
  <action>
    1. Add the `.agent-share` BEM block + 375px responsive rule to style.css.
    2. Add the `<title>` and `<meta robots>` to agent-share-view.hbs.
    3. Re-run Task 1's axe-core test; fix any serious + critical findings until clean.
    4. If color-contrast fails on form-hint, swap to var(--text-secondary) or another existing token (no new tokens).
  </action>
  <verify>
    <automated>cd packages/dashboard && npx playwright test agent-streaming-ux.spec.ts -t "Accessibility scan"</automated>
  </verify>
  <done>
    axe-core scan reports zero serious + critical violations on /agent/share/:id. Mobile viewport has no horizontal scroll. Page has indexable=false meta. tsc clean (no TS changes expected).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 37 Final UAT + VERIFICATION.md</name>
  <what-built>
    Phase 37 complete: 5 plans shipped, all 5 AUX requirements satisfied end-to-end, e2e green, axe-core clean, share view polished.
  </what-built>
  <how-to-verify>
    1. Run the full test suite locally:
       - `cd packages/dashboard && npx vitest run` — expect all unit/integration tests green
       - `cd packages/dashboard && npx playwright test` — expect agent-streaming-ux.spec.ts + all prior e2e specs green
       - `cd packages/dashboard && npx tsc --noEmit` — exit 0
       - `cd packages/dashboard && npm run build` — clean

    2. Real-browser smoke (cross-checks Plan 04's UAT — quick sanity pass, not full re-run):
       - Stop → chip persists across reload → audit row visible
       - Retry → prior turn replaced → audit row visible
       - Edit-resend → branch swap → audit row visible
       - Copy → markdown lands on clipboard
       - Share → permalink works for same-org, 403 for foreign-org, audit row visible

    3. Author 37-VERIFICATION.md per the Phase 39 VER-01 standard:
       - Each of the 5 SC from ROADMAP listed with PASS/FAIL/PARTIAL
       - For each: verification method (unit, integration, e2e, manual UAT)
       - Nyquist coverage note: which SCs have automated coverage vs manual-only
       - Observed gaps (if any) carried to Phase 39 backfill
       - Audit log evidence: list audit toolNames introduced (message_stopped, message_retried, message_edit_resend, share_created) and confirm all present in /admin/audit during UAT

    4. Commit phase outputs:
       - All Plan 01-05 SUMMARYs present
       - 37-VERIFICATION.md committed
       - ROADMAP.md updated: Phase 37 marked complete with date

    5. Final readback: state in chat any deferred items observed during UAT (e.g., copy-with-tool-blocks, branch-tree UI) — these go on the deferred-ideas list for Phase 39 triage, NOT into 37 scope.
  </how-to-verify>
  <resume-signal>Type "phase-complete" with VERIFICATION.md path, or list any failing criteria</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright browser context → dashboard | Standard auth + CSRF per existing harness |
| axe-core scan | Reads DOM, no execution risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-20 | Information disclosure | Share-view indexability | mitigate | `<meta name="robots" content="noindex,nofollow">` added in Task 2 |
| T-37-21 | Repudiation | Verification gaps slip into release | mitigate | VERIFICATION.md required artifact lists each SC with method + outcome |
</threat_model>

<verification>
- All 7 e2e cases green twice in a row.
- axe-core scan: zero serious + critical violations on /agent/share/:id.
- Full unit + integration suite green.
- VERIFICATION.md exists with PASS/FAIL line per AUX-01..05.
- ROADMAP.md updated: Phase 37 marked complete.
</verification>

<success_criteria>
- All 5 AUX requirements verified PASS in 37-VERIFICATION.md.
- Phase 37 closeout commit pushed (or staged) with all summaries + verification record.
- No serious/critical a11y violations on the share view.
- Final UAT checkpoint approved.
</success_criteria>

<output>
After completion, create:
- `.planning/phases/37-streaming-ux-polish/37-05-SUMMARY.md` — plan summary
- `.planning/phases/37-streaming-ux-polish/37-VERIFICATION.md` — phase verification record per Phase 39 VER-01 format
</output>
