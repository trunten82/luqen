---
phase: 37-streaming-ux-polish
plan: 04
type: execute
wave: 3
depends_on: ["37-02", "37-03"]
files_modified:
  - packages/dashboard/src/static/agent.js
  - packages/dashboard/tests/static/agent-actions-handlers.test.ts
autonomous: false
requirements: [AUX-01, AUX-02, AUX-03, AUX-04, AUX-05]
task_count: 3
tags: [client, agent-js, csp, sse, ux]

must_haves:
  truths:
    - "Clicking #agent-stop closes activeStream AND posts an explicit stop signal so the server-side handler runs the markMessageStopped path"
    - "Clicking the retry button on the most-recent assistant message POSTs to /retry, then re-opens the SSE stream and renders the new turn"
    - "Clicking the edit pencil on the most-recent user message swaps the bubble for the edit form; submitting POSTs to /edit-resend and re-opens SSE"
    - "Clicking copy puts the message's raw markdown source on the clipboard and shows a toast/aria-live confirmation"
    - "Clicking share POSTs to /share, copies the resulting permalink to the clipboard, and shows a toast"
    - "All handlers are CSP-strict (delegated listeners, no inline JS, no innerHTML on untrusted strings)"
    - "The original markdown source for each assistant message is captured at stream end into an in-memory map keyed by messageId so copy can read it without round-tripping the server"
  artifacts:
    - path: packages/dashboard/src/static/agent.js
      provides: "Delegated handlers for retryAssistant, copyAssistant, shareAssistant, editUserMessage, submitEditUserMessage, cancelEditUserMessage; markdown source capture; clipboard helpers; toast/aria-live announcer"
      contains: "data-action=\"retryAssistant\""
  key_links:
    - from: packages/dashboard/src/static/agent.js (click delegation block)
      to: packages/dashboard/src/routes/agent.ts (POST endpoints from Plan 03)
      via: "fetch() with x-csrf-token + credentials: same-origin"
      pattern: "fetch\\(.*conversations.*messages.*(retry|edit-resend|share)"
---

<objective>
Wire the per-message actions to working behavior. The visual scaffolding (Plan 02) and the server endpoints (Plan 03) are already in place — this plan connects them in agent.js using CSP-strict delegated listeners and ships the markdown-source capture, clipboard helper, edit-mode toggle, and toast/aria-live announcer.

Purpose: Without this plan, the action buttons are inert. After this plan, all 5 success criteria are end-user-observable on a real device.
Output: Extended agent.js with five new delegated handlers, a markdown-source capture path inside the existing SSE handler, clipboard + toast helpers, and a unit test file using JSDOM that covers the click → fetch → DOM-update flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-streaming-ux-polish/37-CONTEXT.md
@.planning/phases/37-streaming-ux-polish/37-02-PLAN.md
@.planning/phases/37-streaming-ux-polish/37-03-PLAN.md
@.planning/phases/35-agent-conversation-history/35-PATTERNS.md
@packages/dashboard/src/static/agent.js
@packages/dashboard/src/views/partials/agent-msg-actions.hbs
@packages/dashboard/src/views/partials/agent-msg-edit.hbs

<interfaces>
<!-- Delegated-listener contract from Plan 02. agent.js MUST handle each data-action: -->

data-action="retryAssistant"        on assistant-message retry button (data-message-id present)
data-action="copyAssistant"         on assistant-message copy button
data-action="shareAssistant"        on assistant-message share button
data-action="editUserMessage"       on user-message edit pencil
data-action="submitEditUserMessage" on edit form (delegated form submit handler too)
data-action="cancelEditUserMessage" on edit form cancel button

<!-- Server endpoints from Plan 03 (consume these directly, no other discovery needed): -->
POST /agent/conversations/:cid/messages/:mid/retry        → 200 { conversationId, retried: true }
POST /agent/conversations/:cid/messages/:mid/edit-resend  → 200 { conversationId, newUserMessageId }
                                                            body: { content: string }
POST /agent/conversations/:cid/messages/:mid/share        → 201 { shareId, url }

<!-- Existing helpers in agent.js (35-PATTERNS lines 537-552): -->
- csrfToken() → reads <meta name="csrf-token">
- activeStream variable + close() pattern (lines 23, 53, 663-723 of agent.js)
- delegated click listener at agent.js:637-675 — append new handlers here
- replaceMessagesFromHtml() helper for re-rendering the panel
- DOM mutation rule: createTextNode / textContent / DOMParser+importNode; never innerHTML for untrusted strings
- existing SSE on('message') handler accumulates assistant text (locate via grep for 'accumulatedText' or 'data.delta')

<!-- Plan 02's agent-msg-edit.hbs HTML (already in DOM via partials): -->
<form class="agent-msg__edit-form" data-action="submitEditUserMessage" data-message-id="{id}">
  <textarea name="content" ...>...</textarea>
  <button type="submit">Save</button>
  <button type="button" data-action="cancelEditUserMessage">Cancel</button>
</form>

<!-- Plan 02 i18n keys available at runtime via the JSON-script-block fallback (Phase 36-04 pattern): -->
agent.actions.copied, agent.actions.copyFailed, agent.actions.shareCreated, agent.actions.shareFailed, agent.actions.stopped, agent.actions.stopFailed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Markdown source capture + clipboard + toast/aria-live helpers</name>
  <files>packages/dashboard/src/static/agent.js, packages/dashboard/tests/static/agent-actions-handlers.test.ts</files>
  <read_first>
    - packages/dashboard/src/static/agent.js (full file — locate existing SSE handler and the renderedContent path)
    - .planning/phases/35-agent-conversation-history/35-PATTERNS.md "agent.js" section (debounce, IntersectionObserver, fetch+CSRF patterns to mirror)
    - packages/dashboard/tests/static/ (review existing JSDOM test harnesses — agent-panel.test.ts is the closest analog from Phase 36)
  </read_first>
  <behavior>
    Add three primitives to agent.js (top-level closures inside the IIFE):

    1. Markdown source map:
       var markdownSourceById = Object.create(null);
       Inside the existing SSE message handler, when the server emits the per-chunk `delta` events, accumulate raw markdown source text per messageId. Locate the existing `accumulatedText` variable; we already have it. On stream `done` (or on the final message frame that includes a stable `messageId`), assign markdownSourceById[messageId] = accumulatedText. On stream `abort`, also assign whatever's been accumulated. On panel reload (replaceMessagesFromHtml), the map is preserved.
       Server fallback: if the user copies a message rendered from a panel reload (no markdown captured), fetch GET /agent/conversations/:cid/messages/:mid → returns { id, content } and use response.content. Implement the fallback as `getMarkdownSource(messageId, conversationId)` returning a Promise<string>.

    2. Clipboard helper (writeToClipboard):
       async function writeToClipboard(text) {
         if (window.isSecureContext && navigator.clipboard) {
           try { await navigator.clipboard.writeText(text); return true; } catch (_) { /* fall through */ }
         }
         // Fallback per CONTEXT D-04 — hidden textarea + execCommand('copy')
         var ta = document.createElement('textarea');
         ta.value = text; ta.setAttribute('readonly', ''); ta.style.position='fixed'; ta.style.left='-9999px';
         document.body.appendChild(ta); ta.select();
         var ok = false; try { ok = document.execCommand('copy'); } catch (_) {}
         document.body.removeChild(ta);
         return ok;
       }

    3. Toast/aria-live announcer:
       Use a single hidden #agent-aria-live region with role="status" aria-live="polite". If it doesn't exist, append it once in DOMContentLoaded. announce(messageText) clears the region, then sets textContent on next tick (so SR re-announces).

    Tests (agent-actions-handlers.test.ts, ≥6 cases for this task):
      1. writeToClipboard primary path: navigator.clipboard.writeText called with given text in JSDOM (mock secure context)
      2. writeToClipboard fallback path: when navigator.clipboard is missing, hidden textarea + execCommand path runs and returns true
      3. writeToClipboard returns false when both paths fail
      4. announce() updates #agent-aria-live textContent
      5. markdownSourceById accumulates text from simulated SSE deltas
      6. getMarkdownSource falls back to fetch when map miss
  </behavior>
  <action>
    1. RED: Write tests/static/agent-actions-handlers.test.ts using JSDOM. Mirror the harness from tests/static/agent-panel.test.ts (Phase 36-04 added it). Stub navigator.clipboard, document.execCommand, fetch. Import agent.js as a script tag string OR exfiltrate the helpers via a `window.__agentTestExports` shim (only when window.__agentTestMode is true) — match whatever pattern agent-panel.test.ts uses.
    2. GREEN: Add the three primitives to agent.js. Place markdownSourceById near activeStream (top of IIFE). Place writeToClipboard and announce as named function declarations alongside csrfToken().
    3. Modify the existing SSE message handler to store completed assistant text into markdownSourceById on the `done` frame.
    4. Append #agent-aria-live to the agent drawer at DOMContentLoaded if missing (one-line guard).
    5. Verify all 6 tests pass.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/static/agent-actions-handlers.test.ts -t "clipboard\|markdownSource\|announce"</automated>
  </verify>
  <done>
    Three primitives shipped in agent.js. ≥6 tests pass. tsc clean (or tsc-check skipped if static/ is excluded — match existing tsconfig). No regressions in existing agent-panel tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop, retry, copy, share handlers (assistant-side actions)</name>
  <files>packages/dashboard/src/static/agent.js, packages/dashboard/tests/static/agent-actions-handlers.test.ts</files>
  <read_first>
    - packages/dashboard/src/static/agent.js — locate delegated click listener at lines ~637-675 (35-PATTERNS line 514)
    - packages/dashboard/src/static/agent.js — locate #agent-stop existing handler (line 327 references the close pattern)
  </read_first>
  <behavior>
    Stop handler enhancement:
      - Existing #agent-stop button already closes activeStream client-side. CONTEXT decision: this phase formalizes server-side persistence. Plan 03 makes AgentService persist on AbortSignal — but the SSE close from the client must propagate to server's AbortController.
      - Server already detects connection close on existing GET /agent/stream/:cid. Verify this by inspecting agent.ts stream handler. If the server's AbortController is wired to request close, NO additional client work is needed beyond what already exists. Otherwise, the client must POST /agent/conversations/:cid/stop to trigger the abort.
      - Action: extend the existing #agent-stop click handler — after closing activeStream, call announce(t('agent.actions.stopped')). If a server-side stop endpoint is required (verify during execution), add POST /agent/conversations/:cid/stop in routes/agent.ts (small extension of Plan 03 — note in summary).

    Retry handler:
      ```js
      if (e.target.closest('[data-action="retryAssistant"]')) {
        e.preventDefault();
        var btn = e.target.closest('[data-action="retryAssistant"]');
        var mid = btn.getAttribute('data-message-id');
        var cid = getConversationId();
        if (!cid || !mid) return;
        btn.disabled = true;
        fetch('/agent/conversations/' + encodeURIComponent(cid) + '/messages/' + encodeURIComponent(mid) + '/retry', {
          method: 'POST',
          credentials: 'same-origin',
          headers: { 'x-csrf-token': csrfToken(), 'Content-Type': 'application/json' },
          body: '{}',
        })
        .then(function (r) { if (!r.ok) throw new Error('retry_failed'); return r.json(); })
        .then(function () { removeMessageFromDom(mid); openStream(cid); })
        .catch(function () { btn.disabled = false; announce(t('agent.actions.retryFailed')); });
        return;
      }
      ```
      removeMessageFromDom(mid): use document.querySelector('[data-message-id="'+CSS.escape(mid)+'"]').remove(). openStream(cid): re-open the SSE EventSource — reuse whatever existing helper opens GET /agent/stream/:cid (locate via grep for 'EventSource' in agent.js).

    Copy handler:
      ```js
      if (e.target.closest('[data-action="copyAssistant"]')) {
        e.preventDefault();
        var btn = e.target.closest('[data-action="copyAssistant"]');
        var mid = btn.getAttribute('data-message-id');
        var cid = getConversationId();
        getMarkdownSource(mid, cid)
          .then(writeToClipboard)
          .then(function (ok) { announce(ok ? t('agent.actions.copied') : t('agent.actions.copyFailed')); })
          .catch(function () { announce(t('agent.actions.copyFailed')); });
        return;
      }
      ```

    Share handler:
      ```js
      if (e.target.closest('[data-action="shareAssistant"]')) {
        e.preventDefault();
        var btn = e.target.closest('[data-action="shareAssistant"]');
        var mid = btn.getAttribute('data-message-id');
        var cid = getConversationId();
        if (!cid || !mid) return;
        btn.disabled = true;
        fetch('/agent/conversations/' + encodeURIComponent(cid) + '/messages/' + encodeURIComponent(mid) + '/share', {
          method: 'POST', credentials: 'same-origin',
          headers: { 'x-csrf-token': csrfToken(), 'Content-Type': 'application/json' },
          body: '{}',
        })
        .then(function (r) { if (!r.ok) throw new Error('share_failed'); return r.json(); })
        .then(function (payload) {
          var fullUrl = window.location.origin + payload.url;
          return writeToClipboard(fullUrl);
        })
        .then(function (ok) { announce(ok ? t('agent.actions.shareCreated') : t('agent.actions.shareFailed')); })
        .catch(function () { announce(t('agent.actions.shareFailed')); })
        .then(function () { btn.disabled = false; });
        return;
      }
      ```

    Add a t(key) helper that reads from the `agent.actions.*` JSON-script-block (mirror Phase 36-04's `<script type="application/json" id="agent-tools-i18n">` pattern; reuse same loader if it exists).

    Tests (≥9 cases added to agent-actions-handlers.test.ts):
      Retry: success → fetch called with correct URL, message DOM removed, openStream invoked / failure → button re-enabled, announce called with retryFailed
      Copy: success path with map hit, success with map miss + fetch fallback, failure path
      Share: success → POST sent, returned URL written to clipboard, announce success / 4xx → announce shareFailed, button re-enabled
  </behavior>
  <action>
    1. RED: extend tests file with ≥9 new cases. Mock fetch to return the documented response shapes. Mock writeToClipboard, openStream, removeMessageFromDom — or treat them as black-box and observe DOM/window state.
    2. Locate the existing delegated click listener block in agent.js (line ~637-675). Add the four handlers above (stop enhancement, retry, copy, share). Match existing 4-space indentation and the `if (e.target.closest('[data-action="..."]')) { ... return; }` shape.
    3. Verify the server-side stop path: inspect routes/agent.ts GET /agent/stream/:cid to see whether request.raw.on('close') triggers an AbortController abort that AgentService observes. If yes, no extra route is needed and Plan 03 Task 1's AbortSignal observer fires correctly. If no, ADD a small POST /agent/conversations/:cid/stop endpoint in routes/agent.ts that flips an in-memory abort flag on the active turn (one-liner; match the pattern from existing controller maps if any). Document the choice in summary.
    4. Verify all tests pass + manual smoke against running dashboard via the UAT checkpoint at end of plan.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/static/agent-actions-handlers.test.ts</automated>
  </verify>
  <done>
    All 4 assistant-side handlers wired. ≥9 new tests pass. Real-browser smoke passes (deferred to UAT checkpoint). No CSP violations in DevTools console.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Edit-and-resend handler (user-side action)</name>
  <files>packages/dashboard/src/static/agent.js, packages/dashboard/tests/static/agent-actions-handlers.test.ts</files>
  <read_first>
    - packages/dashboard/src/views/partials/agent-msg-edit.hbs (form structure from Plan 02)
    - packages/dashboard/src/static/agent.js (existing form-submit delegation patterns)
  </read_first>
  <behavior>
    Edit handler (open edit mode):
      - On click of [data-action="editUserMessage"]:
        - Find the .agent-msg containing the button.
        - Read the message's text content (from .agent-msg__body — pre-render is HTML; for user messages, body is plain text — capture via .textContent).
        - Replace .agent-msg__body's content with a clone of an off-DOM template of agent-msg-edit (or fetch the partial from the server: GET /agent/conversations/:cid/messages/:mid/edit-form returning the rendered HBS partial). Server-side render is preferable (CSP-strict, no client-side template). Add this small route to Plan 03 OR inline a static template element rendered by the server in the drawer once. Choose: SERVER-RENDERED partial fetched on demand (GET /agent/conversations/:cid/messages/:mid/edit-form → returns agent-msg-edit.hbs HTML).
        - Focus the textarea, place caret at end.

    Submit handler (submit edit):
      - On submit of form with [data-action="submitEditUserMessage"] (delegated form submit listener — add a single addEventListener('submit', ...) at IIFE init):
        - e.preventDefault().
        - Read textarea content. If empty/whitespace, abort + announce error.
        - POST /agent/conversations/:cid/messages/:mid/edit-resend with body { content }.
        - On 200: remove the user message DOM node AND any subsequent assistant message DOM node (the assistant reply that was superseded). Then call replaceMessagesFromHtml(...) by fetching GET /agent/panel?conversationId=:cid OR insert a new user-message DOM node from response data + open SSE for the new assistant reply.
          Simplest correct implementation: re-fetch the panel via existing loadPanel() (35-PATTERNS line 499), then call openStream(cid) for the new assistant turn.
        - On 4xx: re-enable form, announce error.

    Cancel handler:
      - On click of [data-action="cancelEditUserMessage"]: revert .agent-msg__body to the original text (saved in a local-scoped const before the swap).

    Tests (≥6 cases):
      1. Click edit pencil → fetch GET edit-form, body replaced with form, textarea has original content
      2. Submit with valid content → POST /edit-resend with correct body, panel re-fetched, new SSE opened
      3. Submit with empty content → no POST sent, error announced
      4. Submit with 400 response → form stays open, error announced, button re-enabled
      5. Click cancel → body reverts to original text content
      6. Most-recent-only: edit pencil only present on most-recent user message (verified by Plan 02 test, but client-side guard still respected — clicking a stale pencil for a no-longer-most-recent message after retry → server returns 400 → announce error)
  </behavior>
  <action>
    1. RED: extend tests file with ≥6 edit-flow cases.
    2. GREEN: add edit/cancel/submit handlers to agent.js. Add a small server route GET /agent/conversations/:cid/messages/:mid/edit-form to routes/agent.ts that returns the rendered agent-msg-edit partial — this requires re-opening Plan 03's file slightly. Document the cross-plan touch in summary.
    3. Wire submit via a delegated form submit listener (one addEventListener at init guarded by closest('[data-action="submitEditUserMessage"]')).
    4. Verify all tests pass.
  </action>
  <verify>
    <automated>cd packages/dashboard && npx vitest run tests/static/agent-actions-handlers.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Edit flow end-to-end works in JSDOM. ≥6 new tests pass. Total agent-actions-handlers test count is now ≥21 (6 from Task 1 + 9 from Task 2 + 6 from Task 3). tsc clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT: Per-message actions in real browser</name>
  <what-built>
    All 5 AUX requirements wired end-to-end:
    - AUX-01: Stop button cancels SSE; assistant message persists with "Stopped by user" chip
    - AUX-02: Retry button on the most-recent assistant message replaces it with a fresh turn
    - AUX-03: Edit pencil on the most-recent user message swaps to inline editor; Save resubmits and a new assistant reply streams
    - AUX-04: Copy button puts raw markdown on the clipboard with toast confirmation
    - AUX-05: Share button creates a permalink and copies it; the URL renders a read-only conversation snapshot for same-org users only
  </what-built>
  <how-to-verify>
    Pre-flight:
    - Dashboard running locally (npm run dev in packages/dashboard).
    - Logged in as a user with at least one org assignment.
    - Open the agent drawer; ensure DevTools console is visible to catch CSP violations.

    AUX-01 — Stop:
    1. Send a long-prompt message (e.g., "Write a 1000-word essay about WCAG 2.2").
    2. As soon as text starts streaming, click the stop button (#agent-stop).
    3. EXPECTED: streaming halts; the partial text remains visible; a small "Stopped by user" chip appears below the assistant bubble.
    4. Refresh the page. EXPECTED: the conversation reloads with the partial text + chip still there.
    5. Visit /admin/audit. EXPECTED: row with toolName='message_stopped', outcomeDetail='stopped_by_user'.

    AUX-02 — Retry:
    1. On the most-recent assistant message, hover (or tap on mobile). EXPECTED: action row reveals 3 buttons.
    2. Click retry. EXPECTED: the prior assistant bubble disappears; a new assistant turn streams against the same user message.
    3. /admin/audit → toolName='message_retried' row exists.
    4. Click retry on a NON-most-recent assistant message — EXPECTED: button absent OR backend returns 400 and a toast announces an error (the button should not be present per Plan 02).

    AUX-03 — Edit-and-resend:
    1. On the most-recent user message, click the pencil. EXPECTED: bubble swaps to a textarea + Save/Cancel.
    2. Edit text and click Save. EXPECTED: the old user message AND the assistant reply that followed it are removed from the visible thread; a new user message + new assistant reply stream in their place.
    3. /admin/audit → toolName='message_edit_resend' row.
    4. Click Cancel from the editor. EXPECTED: bubble reverts to original text, no POST sent.

    AUX-04 — Copy:
    1. Click copy on an assistant message. EXPECTED: aria-live region announces "Copied to clipboard".
    2. Paste into a plain text editor (e.g., terminal) and confirm: raw markdown source (with **bold**, code fences, etc.), NOT rendered HTML.
    3. Tool-block bubbles and audit metadata are NOT in the clipboard text.

    AUX-05 — Share:
    1. Click share on an assistant message. EXPECTED: aria-live announces "Share link copied to clipboard".
    2. Paste the URL into a new tab while logged in as the same user. EXPECTED: a read-only page renders with the conversation thread, no composer, no action buttons, no edit pencil.
    3. Log out and visit the URL. EXPECTED: redirected to login (auth guard works).
    4. Log in as a user from a DIFFERENT org and visit the URL. EXPECTED: HTTP 403 page.
    5. /admin/audit → toolName='share_created' row.

    CSP / accessibility checks:
    - DevTools console shows ZERO CSP violations across all 5 flows.
    - Tab through the drawer with keyboard only — every button is reachable, focus-visible style is clear.
    - prefers-reduced-motion: enable in OS — action row does NOT fade-transition.
  </how-to-verify>
  <resume-signal>Type "approved" or list any issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → server | All POSTs include x-csrf-token + credentials: same-origin |
| clipboard API | Secure-context only; legacy fallback uses controlled hidden textarea (no untrusted DOM injection) |
| markdownSourceById | In-memory only; cleared on page reload; never sent back to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-16 | Tampering | Edit form HTML rendered via fetch | mitigate | Server-rendered partial (escaped via Handlebars {{content}}); client uses DOMParser+importNode, never innerHTML on string concat |
| T-37-17 | Information disclosure | Markdown source in clipboard | accept | User-initiated action; clipboard write is the entire intended behavior |
| T-37-18 | Repudiation | Client-only stop with no audit | mitigate | Server's AbortSignal observer in Plan 03 Task 1 emits the audit row regardless of how the client triggers close |
| T-37-19 | Elevation of privilege | Forging a most-recent-message claim by sending a stale mid | mitigate | Server validates most-recent-only constraint in Plan 03 routes; client UI is convenience, not security boundary |
</threat_model>

<verification>
- ≥21 JSDOM tests pass.
- Manual UAT checkpoint passes per how-to-verify steps.
- DevTools shows zero CSP violations.
- Keyboard navigation reaches every action button.
</verification>

<success_criteria>
All 5 AUX requirements observable end-to-end in a real browser:
- AUX-01 stop persists + chip + audit
- AUX-02 retry replaces + audit
- AUX-03 edit-resend branches + audit
- AUX-04 copy delivers markdown source
- AUX-05 share creates org-scoped permalink + read-only view + audit
</success_criteria>

<output>
After completion, create `.planning/phases/37-streaming-ux-polish/37-04-SUMMARY.md` listing: handlers added, helpers added, server-side micro-routes added (if any beyond Plan 03), test counts, UAT outcome, any deviations.
</output>
