---
plan_id: 40-05
phase: 40
title: MCP integration guide + agent companion guide + prompt-template authoring guide
wave: 1
depends_on: []
requirements: [DOC-04, DOC-05, DOC-06]
files_modified:
  - docs/guides/mcp-integration.md
  - docs/guides/agent-companion.md
  - docs/guides/prompt-templates.md
  - docs/mcp-client-setup.md
  - docs/mcp-external-client-walkthrough.md
autonomous: true
must_haves:
  truths:
    - "docs/guides/mcp-integration.md covers Claude Desktop, IDE, and custom client setup including OAuth 2.1 + PKCE + DCR walkthrough"
    - "docs/guides/agent-companion.md covers chat usage, tools, history, org switching, multi-step transparency, and speech input from end-user perspective with both `## For end users` and `## For admins` subsections"
    - "docs/guides/prompt-templates.md covers locked sections, fence markers, validator, and override workflow"
    - "Legacy docs/mcp-client-setup.md and docs/mcp-external-client-walkthrough.md are reduced to redirect stubs pointing at docs/guides/mcp-integration.md"
  artifacts:
    - path: "docs/guides/mcp-integration.md"
      provides: "DOC-04 deliverable"
    - path: "docs/guides/agent-companion.md"
      provides: "DOC-05 deliverable"
    - path: "docs/guides/prompt-templates.md"
      provides: "DOC-06 deliverable"
---

<objective>
Publish three guides:
- `docs/guides/mcp-integration.md` (DOC-04) — Claude Desktop, IDE, custom client setup; OAuth 2.1 + PKCE + DCR walkthrough; supersedes legacy `docs/mcp-client-setup.md` and `docs/mcp-external-client-walkthrough.md`
- `docs/guides/agent-companion.md` (DOC-05) — end-user guide for the chat companion
- `docs/guides/prompt-templates.md` (DOC-06) — authoring guide for locked sections, fence markers, validator, override workflow
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-documentation-sweep/40-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/guides/mcp-integration.md (DOC-04)</name>
  <read_first>
    - docs/mcp-client-setup.md (existing — to be folded in)
    - docs/mcp-external-client-walkthrough.md (existing — to be folded in)
    - .planning/phases/30.1-mcp-oauth-scope-gate/ (OAuth scope gate)
    - .planning/phases/31.1-mcp-authorization-spec-upgrade/ (OAuth 2.1 + PKCE + DCR)
    - packages/dashboard/src/mcp/server.ts (current MCP transport behaviour)
    - packages/dashboard/src/mcp/middleware.ts (auth flow)
  </read_first>
  <action>
    Write `docs/guides/mcp-integration.md`. Required structure (with both `## For end users` and `## For admins` per CONTEXT D-11):

    ```markdown
    # MCP Integration Guide

    > Connect Claude Desktop, your IDE, or a custom client to Luqen's MCP server.

    ## What is MCP in Luqen?
    - One-line summary, link to MCP spec
    - Streamable HTTP transport only (per CLAUDE.md gotcha — SSE-only deprecated)
    - Embedded as a Fastify plugin under each service (per CLAUDE.md)

    ## For end users

    ### Claude Desktop setup
    - claude_desktop_config.json snippet with `command`, `args`, `env`
    - Connection URL pattern: `https://<luqen-host>/mcp`
    - First-run OAuth flow walkthrough (browser opens → consent → token returned)

    ### IDE setup
    - VS Code MCP extension config block
    - JetBrains config block
    - Cursor / Windsurf config block (if applicable)

    ### Custom client setup
    - Minimum capabilities to implement
    - JSON-RPC envelope shape (link to OpenAPI snapshot at docs/reference/openapi/mcp.json)

    ## For admins

    ### OAuth 2.1 + PKCE + DCR walkthrough
    - Endpoints: /oauth/authorize, /oauth/token, /oauth/register (DCR)
    - PKCE code_verifier / code_challenge requirements
    - Scope tiers: read / write / admin (link to rbac-matrix.md)
    - Redirect URI registration via DCR
    - Token lifetime + refresh behaviour

    ### Server-side configuration
    - Env vars relevant to MCP (link installer-env-vars.md)
    - Per-org client registration (admin page reference)
    - Audit-log entries per MCP request

    ## Troubleshooting
    - Common errors with fix steps (401 unauthorized, scope denied, transport mismatch)

    ## See also
    - [Agent companion guide](./agent-companion.md)
    - [Multi-step tools guide](./multi-step-tools.md)
    - [Multi-org switching](./multi-org-switching.md)
    - [RBAC matrix](../reference/rbac-matrix.md)
    - [MCP OpenAPI snapshot](../reference/openapi/mcp.json)
    ```

    Lift accurate config snippets from the legacy docs but rewrite for current v3.1.0 surface. Snippets MUST work against current code.
  </action>
  <acceptance_criteria>
    - `test -f docs/guides/mcp-integration.md` → 0
    - `grep -q "^## For end users" docs/guides/mcp-integration.md` → 0
    - `grep -q "^## For admins" docs/guides/mcp-integration.md` → 0
    - `grep -q "Claude Desktop" docs/guides/mcp-integration.md` → 0
    - `grep -q "OAuth 2.1" docs/guides/mcp-integration.md` → 0
    - `grep -q "PKCE" docs/guides/mcp-integration.md` → 0
    - `grep -q "DCR\|Dynamic Client Registration" docs/guides/mcp-integration.md` → 0
    - `grep -q "Streamable HTTP" docs/guides/mcp-integration.md` → 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace legacy MCP docs with redirect stubs</name>
  <read_first>
    - docs/mcp-client-setup.md
    - docs/mcp-external-client-walkthrough.md
    - docs/guides/mcp-integration.md (Task 1 output)
  </read_first>
  <action>
    Replace each legacy file's contents with a 5-line redirect stub:
    ```markdown
    # MCP Client Setup

    > **Moved.** This guide has been folded into [docs/guides/mcp-integration.md](./guides/mcp-integration.md).
    >
    > _Redirect maintained for inbound links. Update bookmarks._
    ```
    (And similar for the walkthrough file.) Update grep'd inbound references in `docs/`, `README.md`, `packages/` to point at the new location.
  </action>
  <acceptance_criteria>
    - `wc -l docs/mcp-client-setup.md` ≤ 10
    - `wc -l docs/mcp-external-client-walkthrough.md` ≤ 10
    - Both files contain the literal string "Moved" and link to `guides/mcp-integration.md`
    - `grep -r "mcp-client-setup.md\|mcp-external-client-walkthrough.md" docs/ packages/ README.md 2>/dev/null` shows zero references EXCEPT the two stubs themselves and the new mcp-integration.md "supersedes" mention if any
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: docs/guides/agent-companion.md (DOC-05)</name>
  <read_first>
    - .planning/phases/32-agent-service-chat-ui/ (chat UI shipping)
    - .planning/phases/35-agent-conversation-history/ (history)
    - .planning/phases/36-multi-step-tool-use/ (tools)
    - .planning/phases/37-streaming-ux-polish/ (streaming + share)
    - .planning/phases/38-multi-org-context-switching/ (org switching)
    - .planning/phases/33-agent-context-hints/ (context hints)
    - packages/dashboard/src/static/agent.js + agent-*.js (current UI surface)
  </read_first>
  <action>
    Write `docs/guides/agent-companion.md` with `## For end users` and `## For admins` subsections.

    For end users — cover the full chat companion surface:
    - Opening the agent
    - Asking a question (input box, send, stop)
    - Streaming responses (token-by-token, scroll behaviour)
    - Tools the agent can use (high-level — link to rbac-matrix for full list)
    - Multi-step tool transparency (link multi-step-tools.md)
    - History panel (link agent-history.md)
    - Share permalinks (link streaming-share-links.md)
    - Org switching (link multi-org-switching.md)
    - Speech input (Web Speech API gating, browser support, permission prompt UX)
    - Context hints (Phase 33) — what they are, when they appear

    For admins:
    - Enabling/disabling the companion per org
    - RBAC permissions controlling access (link rbac-matrix.md)
    - Token cost considerations (per CONTEXT v3.2.0 deferred; note current visibility)
    - Audit log coverage
    - Configuration env vars (link installer-env-vars.md)

    See also: link the four Phase 35–38 guides, mcp-integration.md, prompt-templates.md.
  </action>
  <acceptance_criteria>
    - `test -f docs/guides/agent-companion.md` → 0
    - `grep -q "^## For end users" docs/guides/agent-companion.md` → 0
    - `grep -q "^## For admins" docs/guides/agent-companion.md` → 0
    - `grep -q "speech\|Web Speech" docs/guides/agent-companion.md` → 0
    - `grep -q "agent-history.md\|multi-step-tools.md\|streaming-share-links.md\|multi-org-switching.md" docs/guides/agent-companion.md` matches all 4 (run as 4 separate greps to verify)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: docs/guides/prompt-templates.md (DOC-06)</name>
  <read_first>
    - All prompt-template / prompt-safety related code: `grep -r "fence marker\|locked section\|prompt template" packages/llm/src/ packages/dashboard/src/ --include='*.ts'`
    - Any existing prompt-template docs under `docs/`
    - Validator implementation (likely in packages/llm/src/prompts/ or similar)
    - .planning/phases (search for prompt-safety related context)
  </read_first>
  <action>
    Write `docs/guides/prompt-templates.md`. Template authoring guide — primary audience is admins/power users authoring or overriding prompts. Sections:

    1. **Overview** — what prompt templates are, where they live, when they apply
    2. **Locked sections** — what they are (immutable scaffolding the validator enforces), why they exist (safety/consistency), how they appear in a template file
    3. **Fence markers** — exact syntax for opening/closing locked sections (lift the actual fence string from code; e.g. `<!-- LOCKED: name -->` ... `<!-- /LOCKED -->`)
    4. **The validator** — what it checks, how to run it locally (`npm run validate-prompts` or equivalent — read package.json scripts to confirm exact command), exit codes, common errors
    5. **Override workflow** — how an org admin overrides a default template, where overrides are stored, how they take precedence, how to revert to default
    6. **Examples** — at least 2 worked examples: minimal valid template, override with one locked section preserved
    7. **See also** — link rbac-matrix.md, agent-companion.md

    All command names and fence syntax MUST be lifted from actual code, not invented.
  </action>
  <acceptance_criteria>
    - `test -f docs/guides/prompt-templates.md` → 0
    - `grep -q "locked section" docs/guides/prompt-templates.md` → 0
    - `grep -q "fence marker" docs/guides/prompt-templates.md` → 0
    - `grep -q "validator\|validate" docs/guides/prompt-templates.md` → 0
    - `grep -q "override" docs/guides/prompt-templates.md` → 0
    - `grep -cE "^##? " docs/guides/prompt-templates.md` ≥ 6 (the 6 sections above)
  </acceptance_criteria>
</task>

</tasks>

## Verification Criteria

- 3 new guides exist at canonical paths
- 2 legacy MCP docs reduced to redirect stubs
- mcp-integration.md and agent-companion.md both follow audience-subsection shape (D-11)
- prompt-templates.md content matches actual validator/fence behaviour in code
- All cross-links valid

## must_haves

- DOC-04 satisfied: standalone MCP integration guide with Claude Desktop/IDE/custom + OAuth 2.1 + PKCE + DCR
- DOC-05 satisfied: agent companion guide covers chat/tools/history/org/multi-step/speech
- DOC-06 satisfied: prompt-template guide covers locked sections, fence markers, validator, override
- Legacy MCP docs no longer compete as authority (D-11 single-file-per-surface)
