---
plan_id: 40-03
phase: 40
title: Installer script refresh + installer docs
wave: 1
depends_on: []
requirements: [DOC-03]
files_modified:
  - install.sh
  - install.command
  - install.ps1
  - docs/deployment/installer-env-vars.md
  - docs/deployment/installer-changelog.md
  - docs/getting-started/installation.md
autonomous: true
must_haves:
  truths:
    - "install.sh applies migrations through the head (061) in one pass"
    - "install.sh exports/prompts for every env var introduced since v2.12.0"
    - "install.sh registers systemd units for every service that runs as a daemon at v3.1.0 (compliance, branding, llm, dashboard, mcp-if-separate)"
    - "install.command (macOS) and install.ps1 (Windows) carry the same coverage gaps closed"
    - "docs/deployment/installer-env-vars.md lists every new env var since v2.12.0 with default + purpose"
    - "docs/getting-started/installation.md references current admin pages and RBAC perms introduced since v2.12.0"
  artifacts:
    - path: "install.sh"
      provides: "Patched Linux installer covering v2.12.0→v3.1.0 deltas"
    - path: "install.command"
      provides: "Patched macOS installer"
    - path: "install.ps1"
      provides: "Patched Windows installer"
    - path: "docs/deployment/installer-env-vars.md"
      provides: "Authoritative env var list"
    - path: "docs/deployment/installer-changelog.md"
      provides: "Per-version installer delta log"
---

<objective>
Audit `install.sh` / `install.command` / `install.ps1` against changes since v2.12.0 (env vars, migrations up to head 061, systemd units, admin pages, RBAC permissions). Patch all three scripts to close the gaps. Update installer docs to list every new env var, admin page, and RBAC permission introduced since v2.12.0.

Closes DOC-03 (script + docs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-documentation-sweep/40-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the v2.12.0→v3.1.0 installer-relevant delta inventory</name>
  <read_first>
    - install.sh, install.command, install.ps1 (current state — full files)
    - packages/dashboard/src/db/sqlite/migrations.ts (head migration number)
    - packages/dashboard/src/config/* (env var declarations)
    - packages/compliance/src/config/*, packages/branding/src/config/*, packages/llm/src/config/*
    - .planning/phases/{34..39.1}/*-CONTEXT.md (what was added)
    - docs/deployment/ (existing installer docs if any)
  </read_first>
  <action>
    Produce `.planning/phases/40-documentation-sweep/40-03-DELTA.md` (a working artifact, not a deliverable) listing:
    1. **Migrations**: `git log v2.12.0..HEAD -- packages/dashboard/src/db/sqlite/migrations.ts` and enumerate each migration number added. Confirm head is 061 (per CONTEXT D-03).
    2. **Env vars**: `git diff v2.12.0..HEAD -- packages/*/src/config/` and extract every new env var name + default.
    3. **Services / systemd units**: list every package that ships a long-running server (compliance, branding, llm, dashboard) and confirm whether MCP runs as a separate unit or under dashboard (per CLAUDE.md: embedded as Fastify plugin → no separate unit).
    4. **Admin pages**: `git diff v2.12.0..HEAD -- packages/dashboard/src/routes/admin/` (and pages/) and list new admin URLs.
    5. **RBAC permissions**: grep `requirePerm(` for new permission strings vs v2.12.0 baseline.

    This delta drives Tasks 2-4.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/40-documentation-sweep/40-03-DELTA.md` → 0
    - File contains 5 sections: Migrations, EnvVars, Services, AdminPages, RBACPermissions
    - Migrations section explicitly states "head = 061"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Patch install.sh (Linux installer)</name>
  <read_first>
    - install.sh (full file)
    - .planning/phases/40-documentation-sweep/40-03-DELTA.md
  </read_first>
  <action>
    For every gap in the DELTA file, patch install.sh:
    1. Add prompts/exports for every new env var. Default each to a safe development value, with a comment naming the v3.x feature it enables.
    2. Ensure the migration step runs `npm run migrate` (or equivalent CLI) AFTER package install but BEFORE service start, and that the runner targets head 061 — not a hardcoded older number.
    3. Add/update systemd unit installation for every service that ships a long-running daemon at v3.1.0. Use the existing systemd template pattern in install.sh; do NOT introduce a separate MCP unit (MCP is embedded in dashboard).
    4. Append a "What's new since v2.12.0" echo block at the end of the script that prints the new admin URLs and RBAC perms so operators see them post-install.
    5. Smoke-validate by running `bash -n install.sh` (syntax check) and reading through the diff.

    Add a header comment at the top of install.sh: `# Last reviewed for v3.1.0 (Phase 40 / DOC-03) — head migration 061`.
  </action>
  <acceptance_criteria>
    - `bash -n install.sh` exits 0
    - `grep -q "v3.1.0.*Phase 40.*DOC-03.*head migration 061" install.sh` → 0
    - For each env var listed in 40-03-DELTA.md EnvVars section, `grep -q "<env var name>" install.sh` returns 0
    - `grep -cE "systemctl (enable|start)" install.sh` ≥ 4 (compliance, branding, llm, dashboard)
    - No hardcoded migration number lower than 061: `grep -E "migrate.*05[0-9]|migrate.*06[02-9]" install.sh` returns nothing
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Patch install.command (macOS) and install.ps1 (Windows) for parity</name>
  <read_first>
    - install.command (full file)
    - install.ps1 (full file)
    - The patched install.sh (Task 2 output)
    - 40-03-DELTA.md
  </read_first>
  <action>
    Mirror the Task 2 changes into install.command (bash for macOS) and install.ps1 (PowerShell for Windows). Both must:
    1. Cover every new env var from the DELTA inventory.
    2. Run migrations to head 061.
    3. Register equivalent OS-level service mechanisms — launchd plist on macOS, Windows Service or Scheduled Task on Windows — for compliance, branding, llm, dashboard. If the existing install.command/install.ps1 do not yet support service registration, ADD it (do not skip; CONTEXT D-02 says installer SCRIPT fixes are in scope).
    4. Print the same "What's new since v2.12.0" summary at the end.

    Add the same header comment marker on each file: `# Last reviewed for v3.1.0 (Phase 40 / DOC-03) — head migration 061` (bash) and `# Last reviewed for v3.1.0 (Phase 40 / DOC-03) -- head migration 061` (PowerShell).

    Validate syntax: `bash -n install.command`, `pwsh -NoProfile -Command "Get-Command -Syntax (Get-Content install.ps1 | Out-String)"` (or `pwsh -NoLogo -NoProfile -Command 'try { [scriptblock]::Create((Get-Content install.ps1 -Raw)) | Out-Null; "OK" } catch { $_; exit 1 }'`).
  </action>
  <acceptance_criteria>
    - `bash -n install.command` exits 0
    - `grep -q "v3.1.0.*Phase 40.*DOC-03.*head migration 061" install.command install.ps1` matches both
    - For each env var in 40-03-DELTA.md, both files contain the var name (verify with grep loop)
    - install.ps1 contains a service-registration block (e.g. `New-Service` or `Register-ScheduledTask`) for at least 4 services
    - install.command contains a launchd plist write for at least 4 services
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update installer docs</name>
  <read_first>
    - docs/getting-started/installation.md (current state)
    - docs/deployment/ (current contents)
    - .planning/phases/40-documentation-sweep/40-03-DELTA.md
  </read_first>
  <action>
    1. Create `docs/deployment/installer-env-vars.md` — markdown table: `| Env Var | Default | Required? | Purpose | Introduced In |`. One row per env var read or set by the installer scripts. Sort alphabetically. Source rows from 40-03-DELTA.md and the patched install.sh.
    2. Create `docs/deployment/installer-changelog.md` — sections per release from v2.12.0 → v3.1.0 listing what the installer added each version (env vars, migrations, services, admin pages, RBAC perms).
    3. Update `docs/getting-started/installation.md`:
       - Add a "What's new in v3.1.0" callout at top.
       - Update any version reference older than v3.1.0.
       - Add links to the two new files above.
       - List new admin pages introduced since v2.12.0 with their URL paths.
       - List new RBAC permissions introduced since v2.12.0 with one-line descriptions.

    Do NOT delete unrelated installation content; this is an update, not a rewrite.
  </action>
  <acceptance_criteria>
    - `test -f docs/deployment/installer-env-vars.md` → 0
    - `test -f docs/deployment/installer-changelog.md` → 0
    - `grep -q "Env Var | Default | Required" docs/deployment/installer-env-vars.md` → 0
    - `grep -q "v3.1.0" docs/getting-started/installation.md` → 0
    - `grep -q "installer-env-vars.md\|installer-changelog.md" docs/getting-started/installation.md` → 0
  </acceptance_criteria>
</task>

</tasks>

## Verification Criteria

- All 3 installer scripts pass syntax check
- All 3 installer scripts cover every env var, migration to 061, service unit, admin page, and RBAC permission introduced since v2.12.0
- Two new doc files exist under `docs/deployment/`
- `docs/getting-started/installation.md` references v3.1.0 and links the new files
- Final acceptance — fresh-container dry-run — is owned by Plan 40-07; this plan's job is the static delta closure

## must_haves

- DOC-03 satisfied at script + docs level
- Installer migration baseline = 061 in one pass (CONTEXT D-03)
- Installer SCRIPT fixes landed (CONTEXT D-02)
- Linux/macOS/Windows parity for env vars, migrations, services
