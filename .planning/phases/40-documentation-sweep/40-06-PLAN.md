---
plan_id: 40-06
phase: 40
title: README sweep + cross-link integration
wave: 2
depends_on: [40-01, 40-02, 40-03, 40-04, 40-05]
requirements: [DOC-01]
files_modified:
  - README.md
  - docs/README.md
  - docs/QUICKSTART.md
  - docs/USER-GUIDE.md
autonomous: true
must_haves:
  truths:
    - "Top-level README accurately describes v3.0.0 + v3.1.0 surface (MCP, agent companion, OAuth 2.1, agent history, multi-step tools, streaming UX, multi-org switching)"
    - "Top-level README has no stale instructions (verified by spot-grep for v2.x version markers and removed feature names)"
    - "docs/README.md indexes the new guides created in plans 40-04 and 40-05"
    - "docs/QUICKSTART.md and docs/USER-GUIDE.md cross-link to relevant new guides"
  artifacts:
    - path: "README.md"
      provides: "Updated top-level README"
    - path: "docs/README.md"
      provides: "Updated docs index linking new guides + reference files"
---

<objective>
Sweep the top-level README and `docs/README.md` to reflect v3.0.0 + v3.1.0 surface. Add navigation links to all new guides (40-04, 40-05) and reference files (40-01 OpenAPI snapshots, 40-02 RBAC matrix, 40-03 installer docs). Remove stale instructions.

Closes DOC-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-documentation-sweep/40-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit top-level README.md for staleness</name>
  <read_first>
    - README.md (full file)
    - .planning/PROJECT.md (current value prop)
    - .planning/phases/40-documentation-sweep/40-CONTEXT.md
  </read_first>
  <action>
    Build a working notes file `.planning/phases/40-documentation-sweep/40-06-README-AUDIT.md` flagging:
    - Every version reference older than v3.1.0
    - Every feature mention that is renamed/removed
    - Every link to a doc file that has moved or been retired (e.g. mcp-client-setup.md → guides/mcp-integration.md)
    - Every "Coming soon" / "TODO" / "WIP" marker
    - Every section that lacks coverage of v3.0.0/v3.1.0 surface (MCP, agent companion, OAuth 2.1, agent history, multi-step tools, streaming UX, multi-org switching)

    Decide per CONTEXT D (Claude's discretion): incremental patch (default) vs full restructure. Document the decision at the top of the audit file.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/40-documentation-sweep/40-06-README-AUDIT.md` → 0
    - File contains a "Decision: incremental | restructure" line
    - File enumerates ≥ 1 finding per category (version refs, stale feature mentions, stale links, gaps)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Patch top-level README.md</name>
  <read_first>
    - .planning/phases/40-documentation-sweep/40-06-README-AUDIT.md
    - README.md
  </read_first>
  <action>
    Apply every fix listed in the audit. Required additions:
    - "What's new in v3.1.0" callout near the top with bulleted summary of: agent history, multi-step tools, streaming UX + share permalinks, multi-org switching
    - "Agent companion" section in the feature list, linking docs/guides/agent-companion.md
    - "MCP integration" section in the feature list, linking docs/guides/mcp-integration.md
    - Updated installation section linking docs/getting-started/installation.md and docs/deployment/installer-env-vars.md
    - Updated reference section linking docs/reference/rbac-matrix.md and docs/reference/openapi/{compliance,branding,llm,dashboard,mcp}.json (or pointing at live `/docs` per service)
    - Remove all retired/renamed feature mentions identified in the audit

    Keep the README clean (per project memory: "main README stays clean") — link to docs rather than duplicating content.
  </action>
  <acceptance_criteria>
    - `grep -q "v3.1.0" README.md` → 0
    - `grep -q "agent-companion.md\|agent companion" README.md` → 0
    - `grep -q "mcp-integration.md\|MCP integration" README.md` → 0
    - `grep -q "rbac-matrix.md" README.md` → 0
    - For each stale-feature item flagged in the audit: `grep -q "<flagged term>" README.md` returns 1 (NOT present)
    - `wc -l README.md` ≤ 400 (sanity: stays clean)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update docs/README.md index</name>
  <read_first>
    - docs/README.md (full file)
    - All new files from plans 40-01..05 (guides + reference)
  </read_first>
  <action>
    Update `docs/README.md` to be the canonical docs landing page. Required sections:
    - **Getting started**: link docs/QUICKSTART.md, docs/getting-started/installation.md
    - **Guides** (narrative): list all docs/guides/*.md including the 7 new ones from plans 40-04 + 40-05 (agent-history, multi-step-tools, streaming-share-links, multi-org-switching, mcp-integration, agent-companion, prompt-templates) plus the existing guides
    - **Reference** (matrices/specs): link docs/reference/rbac-matrix.md, docs/reference/api-reference.md, docs/reference/cli-reference.md, docs/reference/openapi/ directory, etc.
    - **Deployment**: link docs/deployment/installer-env-vars.md, docs/deployment/installer-changelog.md, docs/getting-started/installation.md
    - **Domain**: existing dashboard/, compliance/, branding/, plugins/ subsections

    Sort guide links alphabetically within their group. Add a one-line description per link.
  </action>
  <acceptance_criteria>
    - `grep -q "guides/agent-history.md" docs/README.md` → 0
    - `grep -q "guides/multi-step-tools.md" docs/README.md` → 0
    - `grep -q "guides/streaming-share-links.md" docs/README.md` → 0
    - `grep -q "guides/multi-org-switching.md" docs/README.md` → 0
    - `grep -q "guides/mcp-integration.md" docs/README.md` → 0
    - `grep -q "guides/agent-companion.md" docs/README.md` → 0
    - `grep -q "guides/prompt-templates.md" docs/README.md` → 0
    - `grep -q "reference/rbac-matrix.md" docs/README.md` → 0
    - `grep -q "reference/openapi" docs/README.md` → 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update docs/QUICKSTART.md and docs/USER-GUIDE.md cross-links</name>
  <read_first>
    - docs/QUICKSTART.md
    - docs/USER-GUIDE.md
  </read_first>
  <action>
    Add (or update) cross-links in both files to point at relevant new guides:
    - QUICKSTART: add a "Next steps" section linking guides/agent-companion.md, guides/mcp-integration.md, guides/multi-org-switching.md
    - USER-GUIDE: add references to guides/agent-history.md, guides/streaming-share-links.md, guides/multi-step-tools.md where the chat/agent UX is discussed
    - Update any version reference older than v3.1.0
    - Remove any reference to docs/mcp-client-setup.md or docs/mcp-external-client-walkthrough.md — replace with docs/guides/mcp-integration.md
  </action>
  <acceptance_criteria>
    - `grep -q "guides/agent-companion.md\|guides/mcp-integration.md" docs/QUICKSTART.md` → 0
    - `grep -q "guides/agent-history.md\|guides/streaming-share-links.md\|guides/multi-step-tools.md" docs/USER-GUIDE.md` → 0
    - `grep -E "mcp-client-setup\.md|mcp-external-client-walkthrough\.md" docs/QUICKSTART.md docs/USER-GUIDE.md` returns no matches
  </acceptance_criteria>
</task>

</tasks>

## Verification Criteria

- README.md accurately reflects v3.0.0 + v3.1.0 surface
- docs/README.md indexes all new guides and reference files
- QUICKSTART and USER-GUIDE cross-link to new content
- No stale references to retired files anywhere in the audited surface

## must_haves

- DOC-01 satisfied: top-level README current, no stale instructions, surface coverage complete
- Discoverability: every new doc from plans 40-01..05 is reachable from docs/README.md within one click
- Project convention preserved: README stays clean, links to docs/ for detail
