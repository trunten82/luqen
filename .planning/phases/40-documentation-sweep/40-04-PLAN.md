---
plan_id: 40-04
phase: 40
title: New v3.1.0 feature guides — agent history, multi-step tools, streaming/share, multi-org
wave: 1
depends_on: []
requirements: [DOC-01]
files_modified:
  - docs/guides/agent-history.md
  - docs/guides/multi-step-tools.md
  - docs/guides/streaming-share-links.md
  - docs/guides/multi-org-switching.md
autonomous: true
must_haves:
  truths:
    - "Each of the 4 new guide files exists at docs/guides/<file>.md"
    - "Each guide contains BOTH a `## For end users` AND a `## For admins` H2 section"
    - "Each guide describes the feature accurately for v3.1.0 (search/resume/soft-delete for history; parallel dispatch + retry budget + transparency UI for tools; streaming + share permalinks for streaming; org context switching for multi-org)"
    - "Each guide ends with a `## See also` section linking related docs (rbac-matrix.md, agent-companion.md when present)"
  artifacts:
    - path: "docs/guides/agent-history.md"
      provides: "Phase 35 user-facing guide"
    - path: "docs/guides/multi-step-tools.md"
      provides: "Phase 36 user-facing guide"
    - path: "docs/guides/streaming-share-links.md"
      provides: "Phase 37 user-facing guide"
    - path: "docs/guides/multi-org-switching.md"
      provides: "Phase 38 user-facing guide"
---

<objective>
Create the four NEW dedicated guide pages for v3.1.0 surfaces (Phases 35–38). Each guide is ONE file with `## For end users` and `## For admins` subsections (CONTEXT D-11). All four sit under `docs/guides/`.

Closes part of DOC-01 success criterion #9 (every v3.1.0 surface has a doc page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-documentation-sweep/40-CONTEXT.md
@.planning/phases/35-agent-conversation-history/
@.planning/phases/36-multi-step-tool-use/36-CONTEXT.md
@.planning/phases/37-streaming-ux-polish/37-CONTEXT.md
@.planning/phases/38-multi-org-context-switching/38-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/guides/agent-history.md (Phase 35)</name>
  <read_first>
    - .planning/phases/35-agent-conversation-history/35-CONTEXT.md
    - All 35-XX-SUMMARY.md files (final shipped behaviour)
    - .planning/phases/35-agent-conversation-history/35-VERIFICATION.md
    - packages/dashboard/src/static/agent-history.js (the shipped UI module)
  </read_first>
  <action>
    Write `docs/guides/agent-history.md`. Required structure:

    ```markdown
    # Agent Conversation History

    > Search, resume, and manage your past chat sessions with the Luqen agent companion.

    ## For end users
    - What it is (rolling 20-turn window, persisted history panel)
    - How to open the history panel (UI control reference)
    - How to search past conversations (search box + match behaviour)
    - How to resume a past conversation (click → loads context)
    - How to soft-delete a conversation (delete button → restoration window if any)
    - Retention behaviour and what gets persisted

    ## For admins
    - Where conversations are stored (DB table, retention)
    - RBAC permissions controlling who sees whose history (link to rbac-matrix.md row)
    - How to bulk-purge conversations (admin page or CLI command, if any)
    - Audit-log integration if relevant
    - Storage growth considerations

    ## See also
    - [Agent Companion guide](./agent-companion.md)
    - [RBAC matrix](../reference/rbac-matrix.md)
    ```

    Fill each section with accurate details lifted from the source artifacts. End-user voice is task-oriented (use "you"); admin voice is reference-oriented.
  </action>
  <acceptance_criteria>
    - `test -f docs/guides/agent-history.md` → 0
    - `grep -q "^## For end users" docs/guides/agent-history.md` → 0
    - `grep -q "^## For admins" docs/guides/agent-history.md` → 0
    - `grep -q "^## See also" docs/guides/agent-history.md` → 0
    - `grep -q "soft-delete\|soft delete" docs/guides/agent-history.md` → 0
    - `grep -q "rbac-matrix.md" docs/guides/agent-history.md` → 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: docs/guides/multi-step-tools.md (Phase 36)</name>
  <read_first>
    - .planning/phases/36-multi-step-tool-use/36-CONTEXT.md
    - All 36-XX-SUMMARY.md files
    - .planning/phases/36-multi-step-tool-use/36-VERIFICATION.md
    - packages/dashboard/src/static/agent-tools.js (chip strip / tool SSE handlers)
  </read_first>
  <action>
    Write `docs/guides/multi-step-tools.md` with the same `## For end users` / `## For admins` / `## See also` structure. Must cover:

    For end users:
    - What multi-step tool use means (agent runs multiple tools sequentially or in parallel to answer one question)
    - Parallel dispatch (multiple tools fire simultaneously when independent)
    - Transparency UI (chip strip showing each tool call, status, duration)
    - Retry budget visible to user (what happens when a tool fails repeatedly)
    - How to interpret the chip strip states (running / done / error / retried)

    For admins:
    - Configuration knobs for retry budget (env var, default value, recommended ranges)
    - Per-tool RBAC enforcement still applies on every step
    - Token / cost implications of multi-step runs
    - Logs/audit-trail entries produced per step
    - Link to rbac-matrix.md for the MCP tool surface

    For See also: link agent-companion.md, mcp-integration.md, rbac-matrix.md.
  </action>
  <acceptance_criteria>
    - `test -f docs/guides/multi-step-tools.md` → 0
    - `grep -q "^## For end users" docs/guides/multi-step-tools.md` → 0
    - `grep -q "^## For admins" docs/guides/multi-step-tools.md` → 0
    - `grep -q "parallel dispatch\|parallel-dispatch" docs/guides/multi-step-tools.md` → 0
    - `grep -q "retry budget" docs/guides/multi-step-tools.md` → 0
    - `grep -q "chip strip\|chip-strip" docs/guides/multi-step-tools.md` → 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: docs/guides/streaming-share-links.md (Phase 37)</name>
  <read_first>
    - .planning/phases/37-streaming-ux-polish/37-CONTEXT.md
    - All 37-XX-SUMMARY.md files
    - .planning/phases/37-streaming-ux-polish/37-VERIFICATION.md
    - packages/dashboard/db/migrations relevant to share links (e.g. 059, 060)
    - packages/dashboard/src/static/agent.js (streaming UX)
  </read_first>
  <action>
    Write `docs/guides/streaming-share-links.md` covering:

    For end users:
    - What streaming UX polish delivers (token-by-token rendering, smooth scroll, stop button)
    - How to copy/share a conversation via permalink (share button → URL copied to clipboard)
    - Permalink expiry behaviour (expires_at — see Phase 38 migration 060)
    - What recipients see when they open a share link (read-only view, scope of visibility)
    - Privacy considerations: who can see a share link

    For admins:
    - Share link storage table (`agent_share_links` from migration 059, `expires_at` from 060)
    - Default expiry duration + how to change it
    - RBAC permission required to create a share link (link to rbac-matrix.md)
    - Bulk revocation (admin page or CLI), if any
    - Streaming transport notes (Streamable HTTP per CLAUDE.md), failure-mode UX

    For See also: link agent-companion.md, agent-history.md, rbac-matrix.md.
  </action>
  <acceptance_criteria>
    - `test -f docs/guides/streaming-share-links.md` → 0
    - `grep -q "^## For end users" docs/guides/streaming-share-links.md` → 0
    - `grep -q "^## For admins" docs/guides/streaming-share-links.md` → 0
    - `grep -q "permalink\|share link" docs/guides/streaming-share-links.md` → 0
    - `grep -q "expires_at\|expiry" docs/guides/streaming-share-links.md` → 0
    - `grep -q "agent_share_links" docs/guides/streaming-share-links.md` → 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: docs/guides/multi-org-switching.md (Phase 38)</name>
  <read_first>
    - .planning/phases/38-multi-org-context-switching/38-CONTEXT.md
    - All 38-XX-SUMMARY.md files
    - .planning/phases/38-multi-org-context-switching/38-VERIFICATION.md
    - packages/dashboard/src/static/agent-org.js (org switcher module)
  </read_first>
  <action>
    Write `docs/guides/multi-org-switching.md`:

    For end users:
    - When users belong to multiple orgs (memberships overview)
    - Where the org switcher lives in the UI
    - What changes when switching orgs (dashboard scope, agent context, MCP tool scope)
    - How agent conversations relate to org context (per CLAUDE.md: org sourced from JWT preHandler, never from tool args)
    - Common gotchas: switching mid-conversation behaviour

    For admins:
    - How memberships are managed (admin page reference + RBAC permission per rbac-matrix.md)
    - JWT claim shape carrying org context
    - Why MCP tool schemas never include orgId (CLAUDE.md gotcha) — implications for custom-client integrators
    - Audit log entries on org switch
    - Per-org plugin configs (project memory: project_rbac_design)

    For See also: link agent-companion.md, mcp-integration.md, rbac-matrix.md.
  </action>
  <acceptance_criteria>
    - `test -f docs/guides/multi-org-switching.md` → 0
    - `grep -q "^## For end users" docs/guides/multi-org-switching.md` → 0
    - `grep -q "^## For admins" docs/guides/multi-org-switching.md` → 0
    - `grep -q "org switcher\|org-switcher\|organization switcher" docs/guides/multi-org-switching.md` → 0
    - `grep -q "JWT" docs/guides/multi-org-switching.md` → 0
    - `grep -q "orgId\|org_id" docs/guides/multi-org-switching.md` → 0
  </acceptance_criteria>
</task>

</tasks>

## Verification Criteria

- All 4 guide files exist
- All 4 conform to the audience-subsection shape (D-11): one file with `## For end users` AND `## For admins`
- All 4 link to rbac-matrix.md (so cross-linking is consistent for Plan 40-06's README sweep)
- Content is accurate to Phase 35–38 SUMMARY/VERIFICATION evidence

## must_haves

- DOC-01 success criterion #9 satisfied for the 4 v3.1.0 surfaces
- Single-file-per-surface shape with audience subsections (D-11)
- All 4 sit under `docs/guides/` (D-10 — narrative docs)
