---
phase: 05-ui-review-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/static/style.css
  - packages/dashboard/src/views/admin/llm.hbs
  - packages/dashboard/src/views/admin/branding-guideline-detail.hbs
autonomous: true
requirements:
  - UIR-01
  - UIR-02
  - UIR-03
must_haves:
  truths:
    - "All LLM admin page tabs (Providers, Models, Capabilities, Prompts) render correctly on a 375px mobile viewport without horizontal overflow"
    - "The capability fallback chain list and priority input form display in a readable stacked layout on mobile"
    - "The brand discovery form on the guideline detail page uses design system spacing tokens (no raw px values)"
    - "All i18n text in LLM admin page tab labels uses {{t}} keys (no hardcoded English strings)"
  artifacts:
    - path: "packages/dashboard/src/static/style.css"
      provides: "Mobile responsive rules for LLM admin page and fix hint panels using design system tokens"
      contains: ".brd-discover"
    - path: "packages/dashboard/src/views/admin/llm.hbs"
      provides: "LLM admin page with i18n tab labels and mobile-friendly layout"
    - path: "packages/dashboard/src/views/admin/branding-guideline-detail.hbs"
      provides: "Brand discovery section using design system spacing tokens"
  key_links:
    - from: "packages/dashboard/src/static/style.css"
      to: "packages/dashboard/src/views/admin/llm.hbs"
      via: ".tab, .card, .form-grid, .fallback-chain CSS classes"
      pattern: "fallback-chain|form-grid|tab-content"
---

<objective>
Full UI audit and structural fix pass across all dashboard pages modified in Phases 1-3.

Scope:
1. Audit all four pages for Emerald design system adherence (spacing tokens, typography scale, color variables)
2. Fix layout inconsistencies: raw px values, hardcoded colors, inconsistent spacing in new Phase 1-3 sections
3. Improve mobile responsiveness on the LLM admin page — all four tabs must stack correctly on narrow viewports
4. Replace hardcoded English strings in llm.hbs tab labels and empty states with {{t}} i18n keys

Purpose: The LLM admin page and new AI feature sections were built rapidly across phases — this pass ensures they match the quality and consistency of the existing Emerald UI.

Output:
- Updated style.css with mobile fixes and token-aligned CSS
- Updated llm.hbs with i18n keys and mobile layout improvements
- Minor spacing/consistency fixes in branding-guideline-detail.hbs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/dashboard/src/static/style.css
@packages/dashboard/src/views/admin/llm.hbs
@packages/dashboard/src/views/admin/branding-guideline-detail.hbs
@packages/dashboard/src/i18n/locales/en.json

<interfaces>
<!-- Emerald design system tokens (from style.css :root) -->
<!-- Use these everywhere — do NOT use raw px/rem/color values in new/modified CSS -->

Spacing tokens:
  --space-xs: 0.25rem
  --space-sm: 0.5rem
  --space-md: 1rem
  --space-lg: 1.5rem
  --space-xl: 2rem

Typography tokens:
  --font-size-xs: 0.75rem
  --font-size-sm: 0.875rem
  --font-size-base: 1rem
  --font-mono: (monospace stack)

Color tokens:
  --text-primary, --text-secondary, --text-muted
  --bg-primary, --bg-secondary, --bg-tertiary
  --border-color
  --accent, --accent-light
  --status-success, --status-success-bg
  --status-warning, --status-warning-bg
  --status-error, --status-error-bg

Existing mobile breakpoint: @media (max-width: 640px) — used by .rpt-tabs section

Design system classes already in use:
  .card, .card__header, .card__title, .card__body
  .form-grid (2-column grid, collapses on narrow)
  .form-group, .form-hint, .form-actions
  .btn, .btn--primary, .btn--secondary, .btn--sm, .btn--ghost, .btn--danger, .btn--xs
  .badge, .badge--neutral, .badge--info, .badge--success
  .alert, .alert--warning, .alert--error, .alert--info
  .tab, .tab--active, .tab-content
  .table, .table-responsive
  .input (for form controls)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LLM admin page — i18n keys and mobile layout audit</name>
  <files>
    packages/dashboard/src/views/admin/llm.hbs,
    packages/dashboard/src/i18n/locales/en.json,
    packages/dashboard/src/static/style.css
  </files>
  <read_first>
    - packages/dashboard/src/views/admin/llm.hbs — full file (421 lines), identify all hardcoded English strings and mobile layout issues
    - packages/dashboard/src/i18n/locales/en.json — check existing admin.llm section for any existing keys (search for "llm" in the admin section)
    - packages/dashboard/src/static/style.css lines 2690-2703 — existing @media (max-width: 640px) block at end of report section, extend it
    - packages/dashboard/src/static/style.css lines 3009-3102 — fix-hint and AI summary CSS for reference patterns
  </read_first>
  <action>
**Step 1 — Audit hardcoded strings in llm.hbs and add i18n keys:**

Scan llm.hbs for all hardcoded English strings. These are the known ones — confirm and fix all of them:
- Tab labels: "Providers", "Models", "Capabilities", "Prompts"
- Empty states: "No providers configured. Add one below.", "No models registered for this provider.", "No capabilities found...", etc.
- Form labels: "Add Provider", "Register Model", "Add to Chain", "Save Override", "Reset to Default"
- Section copy: "Models are tried in priority order...", "Fallback chain (tried in order):", etc.

Add i18n keys to `en.json` under an `"admin": { "llm": { ... } }` section (check if it exists first, extend if so). Use flat descriptive keys:
```json
"admin": {
  "llm": {
    "tabProviders": "Providers",
    "tabModels": "Models",
    "tabCapabilities": "Capabilities",
    "tabPrompts": "Prompts",
    "addProvider": "Add Provider",
    "registerModel": "Register Model",
    "addToChain": "Add to Chain",
    "saveOverride": "Save Override",
    "resetToDefault": "Reset to Default",
    "noProviders": "No providers configured. Add one below.",
    "noModelsForProvider": "No models registered for this provider.",
    "noCapabilities": "No capabilities found. Ensure the LLM service is seeded.",
    "noPromptsAvailable": "Prompt management is not available when the LLM service is disconnected.",
    "capabilityFallbackIntro": "Models are tried in priority order (lowest number first). If one fails, the next is tried automatically.",
    "capabilityFallbackLabel": "Fallback chain (tried in order):",
    "capabilityFallbackFooter": "If the primary model fails or times out, the system automatically tries the next one.",
    "capabilityNoModels": "No models assigned. This capability is not available until a model is assigned.",
    "modelsNoProviders": "No providers configured. Add a provider first.",
    "promptNoTemplate": "This capability does not have a prompt template yet. It will be available in a future update."
  }
}
```

Replace all hardcoded strings in llm.hbs with the corresponding `{{t "admin.llm.xxx"}}` keys.

**Step 2 — Mobile layout fixes in style.css:**

The LLM admin page has these mobile issues to fix in the CSS:

1. **Fallback chain list** (`.fallback-chain li`): The inline-flex with badge + priority input + delete button wraps awkwardly on mobile. Add to existing mobile block:
```css
/* Inside existing @media (max-width: 640px) block or add a new one after the report section */
.fallback-chain li {
  flex-direction: column;
  align-items: flex-start;
  gap: var(--space-xs);
}
.fallback-chain li .form-group { width: 100%; }
```

2. **Capability cards grid** (`.cards` used in capabilities tab): The `.cards` class renders as a grid. On mobile, ensure single column:
```css
@media (max-width: 640px) {
  .tab-content .cards { grid-template-columns: 1fr; }
}
```

3. **Brand discovery form** (`.brd-discover__form`): Already uses `flex-wrap: wrap` which is good, but the `min-width: 240px` on `.brd-discover__input-group` may not collapse to single column on very narrow screens. Update:
```css
@media (max-width: 640px) {
  .brd-discover__form { flex-direction: column; }
  .brd-discover__input-group { min-width: unset; width: 100%; }
}
```

4. **Prompt textarea** in the prompts tab: The textarea rows="8" is fine, but ensure the form actions button wraps on mobile (likely already works via existing `.form-actions` styles — verify, add if needed).

5. **LLM warning banner** spacing: The `LLM service not reachable` alert text with `<code>` inline — ensure code wraps on mobile:
```css
@media (max-width: 640px) {
  .alert code { word-break: break-all; }
}
```

Place all new mobile rules in a clearly commented block at the end of the existing Phase additions (after the `.brd-discover` block around line 3392), before the final EOF:
```css
/* ── LLM admin + brand discovery — mobile responsive ─────────────────── */
@media (max-width: 640px) {
  /* ... all rules here ... */
}
```

Do NOT change any existing CSS rules — only ADD new rules.
  </action>
  <verify>
    <automated>grep -c "{{t \"admin.llm\." packages/dashboard/src/views/admin/llm.hbs</automated>
  </verify>
  <acceptance_criteria>
    - All tab labels (Providers, Models, Capabilities, Prompts) use {{t "admin.llm.tabXxx"}} keys
    - All empty state messages use {{t}} keys — no bare English strings for UI copy remain
    - All form submit button labels use {{t}} keys
    - en.json contains the admin.llm section with all new keys
    - style.css has a new @media (max-width: 640px) block with fallback-chain, brd-discover, cards, and alert code rules
    - `grep -c 'hardcoded text that should be translated' llm.hbs` is 0 (no remaining bare English UI copy)
    - `node -e "JSON.parse(require('fs').readFileSync('packages/dashboard/src/i18n/locales/en.json','utf8'))"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>All LLM admin strings use i18n keys, mobile layout rules added to style.css for fallback-chain, cards, and brand discover form</done>
</task>

<task type="auto">
  <name>Task 2: Design system token audit — fix raw values in Phase 1-3 CSS sections</name>
  <files>
    packages/dashboard/src/static/style.css
  </files>
  <read_first>
    - packages/dashboard/src/static/style.css lines 3008-3155 — the three Phase addition blocks: Fix suggestion hints (Phase 1), AI Summary Panel (Phase 2), Brand Discovery (Phase 3)
    - packages/dashboard/src/static/style.css lines 1-60 — :root CSS variables block to confirm available tokens
  </read_first>
  <action>
Audit the three Phase addition CSS sections (lines ~3008-3392) for design system violations. Replace raw values with tokens where possible.

**Known issues to fix:**

1. **`.rpt-fix-hint` section** (~line 3009-3101):
   - `margin-top: 0.5rem` → `margin-top: var(--space-sm)`
   - `padding: 0.4rem 0.7rem` (toggle) — no direct token match, leave as-is (sub-token sizing)
   - `font-size: 0.8rem` (toggle) — no token, leave as-is (keep custom)
   - `gap: 0.4rem` (toggle) — no token, leave as-is
   - `font-size: 0.82rem` (desc) — use `var(--font-size-sm)` (0.875rem is close enough, or leave if team uses 0.82rem intentionally)
   - `padding: 0.5rem 0.7rem 0.25rem` (desc) — leave (mixed)
   - `margin: 0.25rem 0.7rem 0.7rem` (code) — partial token: `margin: var(--space-xs) var(--space-sm) var(--space-sm)` — apply
   - `padding: 0.5rem 0.6rem` (code) — leave (sub-token)
   - `font-size: 0.75rem` (code) — use `var(--font-size-xs)`
   - `.rpt-fix-hint__source` padding `2px var(--space-xs)` — already uses token, good
   - `.rpt-fix-hint__actions` padding `var(--space-xs) var(--space-md) var(--space-sm)` — already uses tokens, good

2. **`.rpt-ai-summary` section** (~line 3117-3152):
   - `padding: var(--space-md)` — already good
   - `margin-bottom: var(--space-lg)` — already good
   - `font-size: var(--font-size-base)` — already good
   - `font-weight: 600` — acceptable raw value (design system has no font-weight token)
   - `margin-bottom: var(--space-sm)` — good
   - `color: var(--text-primary, inherit)` — good (has fallback)
   - `font-size: var(--font-size-sm)` — good
   - `line-height: var(--leading-relaxed, 1.6)` — good (has fallback)
   - `padding-left: var(--space-lg)` — good
   - `.rpt-ai-summary__footer` font-size `var(--font-size-xs)` — good
   - `color: var(--text-muted, #64748b)` — good
   - `margin-top: var(--space-lg)` — good
   - `padding-top: var(--space-sm)` — good
   - `border-top: 1px solid var(--border-color, #e5e7eb)` — good
   - `padding: var(--space-md)` (loading) — good

3. **`.brd-discover` section** (~line 3377-3392):
   - `margin-top: var(--spacing-6, var(--space-lg))` — the `--spacing-6` is not a real Emerald token; simplify to `margin-top: var(--space-lg)`
   - `gap: var(--spacing-3, var(--space-sm))` — same issue; simplify to `gap: var(--space-sm)`
   - `min-width: 240px` — acceptable raw value for min-width constraints

4. **`.rpt-fix-effort` section** (~line 3050-3071):
   - `padding: 0.1rem 0.45rem` — sub-token, acceptable for pill badge
   - `font-size: 0.68rem` — sub-token for tiny badge, acceptable
   - `font-weight: 600` — acceptable
   - `letter-spacing: 0.03em` — acceptable
   - `margin-left: 0.25rem` — use `var(--space-xs)`
   - Background/color values already use design system status tokens — good

**Apply these specific changes:**
- `.rpt-fix-hint` → `margin-top: var(--space-sm)`
- `.rpt-fix-hint__code` → `margin: var(--space-xs) var(--space-sm) var(--space-sm)` and `font-size: var(--font-size-xs)`
- `.rpt-fix-effort` → `margin-left: var(--space-xs)`
- `.brd-discover` → remove `var(--spacing-6,` wrapper, use `margin-top: var(--space-lg)` directly
- `.brd-discover__form` → remove `var(--spacing-3,` wrapper, use `gap: var(--space-sm)` directly

After editing, verify the file still has valid CSS (no syntax errors) by checking the token replacements are balanced.
  </action>
  <verify>
    <automated>grep -c "var(--spacing-" packages/dashboard/src/static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `var(--spacing-` in style.css (all `--spacing-N` fallback wrappers replaced with direct Emerald tokens)
    - `.rpt-fix-hint` uses `var(--space-sm)` for margin-top
    - `.rpt-fix-hint__code` uses `var(--font-size-xs)` for font-size
    - `.rpt-fix-effort` uses `var(--space-xs)` for margin-left
    - `.brd-discover` uses `var(--space-lg)` directly (no double-fallback)
    - The CSS file parses without errors (balanced braces — count opening vs closing `{` should match)
  </acceptance_criteria>
  <done>All Phase 1-3 CSS additions use Emerald design system tokens — no raw px fallback wrappers remain</done>
</task>

</tasks>

<verification>
```bash
# Verify i18n JSON is valid
node -e "JSON.parse(require('fs').readFileSync('packages/dashboard/src/i18n/locales/en.json','utf8'))" && echo "JSON valid"

# Verify all tab labels are i18n keys
grep -c 'Providers\|Models\|Capabilities\|Prompts' packages/dashboard/src/views/admin/llm.hbs

# Verify --spacing- tokens replaced
grep -c "var(--spacing-" packages/dashboard/src/static/style.css

# Verify mobile rules added
grep "fallback-chain\|brd-discover__form\|tab-content .cards" packages/dashboard/src/static/style.css

# Verify dashboard builds
cd packages/dashboard && npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
- en.json valid JSON with admin.llm section containing all new keys
- llm.hbs tab labels and empty states use {{t}} keys — no bare English UI copy
- style.css has mobile @media rules for fallback-chain, brd-discover, cards on narrow viewports
- style.css Phase 1-3 sections use Emerald tokens throughout — zero --spacing-N wrappers
- Dashboard package builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-review-polish/05-01-SUMMARY.md`
</output>
