---
phase: 32.1-agent-chat-fixes
verified: 2026-04-25T00:00:00Z
status: passed
score: gap-closure phase (no formal SC list in v3.0.0-ROADMAP — verified against 32-UAT.md gaps + 32.1-PLAN.md plans)
overrides_applied: 0
requirements_coverage: []  # 32.1 inherits AGENT-01..03 + APER-02 from Phase 32; no new requirement IDs
---

# Phase 32.1: Agent Chat Fixes — Verification Report (Backfill)

**Phase Goal (from v3.0.0-ROADMAP.md "Inserted Phases" + 32.1-PLAN.md):** Close the 10 UAT gaps found during Phase 32 live UAT on lxc-luqen — 3 blockers (global-admin chat, tool dispatcher empty, no new-conversation control), 3 major UX issues, 3 cosmetic, plus CI green.

**Verified:** 2026-04-25 (lightweight backfill per Phase 39 / VER-01)
**Status:** passed (every UAT gap closed end-to-end; CI green on master since `e0e533f`)
**Re-verification:** No — backfill of a phase that shipped without VERIFICATION.md

## Goal Achievement

Phase 32.1 has no separate v3.0.0-ROADMAP.md success-criteria block — it is recorded as an INSERTED phase whose intent is "in-session fixes" against `32-UAT.md`. The Observable Truths table below maps each of the 9 plans (01-09) to its UAT gap and resolution evidence.

### Observable Truths (UAT Gaps Closed)

| # | Plan | Gap | Status | Evidence |
|---|------|-----|--------|----------|
| 1 | 32.1-09 | CI red on master for weeks (8 stale failures) | PASS | `32.1-SUMMARY.md` §Plans Delivered; CI green on master since commit `e0e533f` |
| 2 | 32.1-01 | Global admin POST /agent/message → 400 `no_org_context` | PASS | 32.1-SUMMARY §Plan 01; synthetic `__admin__:<userId>` namespace in /agent/* routes (5 routes); `routes/agent.ts` + `agent-service.ts` modified |
| 3 | 32.1-02 | Tool dispatcher had `tools:[]` — every call returned `unknown_tool` | PASS | 32.1-SUMMARY §Plan 02; new `packages/dashboard/src/agent/mcp-bridge.ts`; `_registeredTools` extracted into ToolDispatcher manifest + AgentService toolCatalog; `runInToolContext` ALS re-entry pattern (Phase 32-UAT.md Gap #2 + Gap #3 closed) |
| 4 | 32.1-03 | No way to reset a conversation | PASS | 32.1-SUMMARY §Plan 03; "New chat" button + localStorage clear in `agent-drawer.hbs` + `agent.js`; 6 locale JSON keys |
| 5 | 32.1-04 | Display-name change needed hard refresh | PASS | 32.1-SUMMARY §Plan 04; HX-Trigger + agent.js listener updates `#agent-display-name` in place via `routes/admin/organizations.ts` |
| 6 | 32.1-05 | Badges overflowed on capabilities tab | PASS | 32.1-SUMMARY §Plan 05; CSS `flex-wrap: wrap` + `overflow-wrap: anywhere` in `style.css` |
| 7 | 32.1-06 | Markdown rendered as literal `**bold**` / tables collapsed | PASS | 32.1-SUMMARY §Plan 06; vendored marked + DOMPurify + mermaid (UMD); `.agent-msg__body` table CSS overrides mobile `.data-table` card-stack rule; multimodal output added |
| 8 | 32.1-07 | Org settings page not in nav | PASS | 32.1-SUMMARY §Plan 07; "Agent settings" link added on org members page |
| 9 | 32.1-08 | Assistant tool_calls row synthesised from tool rows (lossy) | PASS | 32.1-SUMMARY §Plan 08; persisted `role='assistant'` row with `toolCallJson` BEFORE dispatch in `dispatchAndPersist`; matches Ollama/OpenAI/Anthropic message order |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/dashboard/src/agent/mcp-bridge.ts` (created) | Bridges MCP `_registeredTools` into ToolDispatcher | VERIFIED | 32.1-SUMMARY key-files §created |
| `packages/dashboard/src/routes/agent.ts` (modified) | Synthetic `__admin__:<userId>` namespace on 5 routes | VERIFIED | 32.1-SUMMARY §Plan 01 |
| `packages/dashboard/src/agent/agent-service.ts` (modified) | toolCatalog + global-admin orgId fallback + persisted assistant(tool_calls) row | VERIFIED | 32.1-SUMMARY §Plans 01, 02, 08 |
| `packages/dashboard/src/server.ts` (modified) | MCP server boot + `_registeredTools` extraction | VERIFIED | 32.1-SUMMARY §Plan 02 |
| `packages/dashboard/src/static/agent.js` (modified) | New-chat button + display-name listener + markdown rendering | VERIFIED | 32.1-SUMMARY §Plans 03, 04, 06 |
| `packages/dashboard/src/static/style.css` (modified) | Badge wrap + table CSS override | VERIFIED | 32.1-SUMMARY §Plans 05, 06 |
| `packages/dashboard/src/views/partials/agent-drawer.hbs` (modified) | New-chat button + display-name span | VERIFIED | 32.1-SUMMARY §Plans 03, 04 |
| `packages/core/src/mcp/http-plugin.ts` + `index.ts` (modified) | `runInToolContext` helper for in-process ALS re-entry | VERIFIED | 32.1-SUMMARY §Plan 02 patterns-established |
| `packages/llm/src/providers/ollama.ts` (modified) | Tool schema fixes (properties, description) | VERIFIED | 32.1-SUMMARY §Plan 02 (rolled in via 32-UAT investigations) |
| 6× locale JSON files | New i18n keys for new-chat, proposals, nav | VERIFIED | 32.1-SUMMARY key-files |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `routes/agent.ts` (5 routes) | global-admin namespace | `user.currentOrgId === undefined && user.role === 'admin'` → `__admin__:${user.id}` | WIRED (Plan 01) |
| `server.ts` MCP boot | `ToolDispatcher.tools` + `AgentService.toolCatalog` | `_registeredTools` extraction via mcp-bridge | WIRED (Plan 02) |
| `agent-drawer.hbs` "New chat" button | `agent.js` newChat handler | `data-action="newChat"` + delegated click | WIRED (Plan 03) |
| `routes/admin/organizations.ts` POST /settings | drawer `#agent-display-name` span | `HX-Trigger` header → agent.js listener | WIRED (Plan 04) |
| `agent-service.ts` dispatchAndPersist | `agent_messages` row | `role='assistant'` + `toolCallJson` persisted BEFORE tool exec | WIRED (Plan 08) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Dashboard suite | full vitest | 3008 passing + 40 skipped + 3 todo (zero failures after Plan 09) per 32.1-SUMMARY | PASS |
| Compliance suite | full vitest | 571 passing per 32.1-SUMMARY | PASS |
| LLM suite | full vitest | 327 passing per 32.1-SUMMARY | PASS |
| Core suite | full vitest | 251 passing per 32.1-SUMMARY | PASS |
| CI on master | `gh run list --branch master` | green since commit `e0e533f` per 32.1-SUMMARY | PASS |
| Live UAT on lxc-luqen | manual smoke (drawer, global-admin, org-admin, tool calls, markdown, mermaid, reset/reload) | "drawer works for global admin AND org admin, tool calls return real data, markdown + mermaid render correctly, reset/reload flows all honour conversationId persistence" per 32.1-SUMMARY §Verification Results | PASS |

### Requirements Coverage

Phase 32.1 ships under no new requirement IDs — it patches AGENT-01/02/03 and APER-02 (owned by Phase 32) and was inserted to bring those requirements over the live-UAT line. Phase 32 verification (this same backfill set) is the formal coverage record for those IDs; Phase 32.1's role is gap-closure, not requirement claim.

### Deferred / Out of Scope (per 32.1-SUMMARY)

- Multiple-conversation listing/switching UI — future phase (Phase 35 delivered this).
- tiktoken-accurate token counting — Phase 33-03 uses char/4 heuristic; Phase 34 delivered the precise tokenizer.
- Larger agent model assignment — config change, not code.

### Gaps Summary

None. Every UAT gap from `32-UAT.md` was assigned a plan (01-09) and every plan landed per 32.1-SUMMARY. CI is green; live UAT pass recorded; no `deferred-items.md` exists for Phase 32.1. The deferred items the SUMMARY lists are scope-deferrals, not unresolved gaps — all three were subsequently delivered (Phase 34 tokenizer; Phase 35 conversation history).

---

_Verified: 2026-04-25 (backfill)_
_Verifier: Claude (gsd-planner backfill per Phase 39 / VER-01)_
