---
phase: 38
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/src/db/sqlite/migrations.ts
  - packages/dashboard/src/db/interfaces/user-repository.ts
  - packages/dashboard/src/db/sqlite/repositories/user-repository.ts
  - packages/dashboard/tests/db/user-repository.test.ts
autonomous: true
requirements: [AORG-03]
task_count: 2
tags: [aorg-03, persistence, migration, sqlite]
must_haves:
  truths:
    - "dashboard_users has an active_org_id column that can store an org id or NULL"
    - "Repository can read a user's active_org_id and write a new active_org_id transactionally"
    - "Existing user rows continue to work unchanged (column defaults NULL)"
  artifacts:
    - path: "packages/dashboard/src/db/sqlite/migrations.ts"
      provides: "Migration 061 — add active_org_id TEXT NULL on dashboard_users"
      contains: "id: '061'"
    - path: "packages/dashboard/src/db/interfaces/user-repository.ts"
      provides: "DashboardUser.activeOrgId field + setActiveOrgId(userId, orgId|null) method"
      exports: ["DashboardUser", "UserRepository"]
    - path: "packages/dashboard/src/db/sqlite/repositories/user-repository.ts"
      provides: "SQLite implementation of setActiveOrgId + activeOrgId surfaced from row mapper"
    - path: "packages/dashboard/tests/db/user-repository.test.ts"
      provides: "Tests covering migration column presence, default NULL, set/clear active_org_id"
  key_links:
    - from: "user-repository.ts"
      to: "dashboard_users.active_org_id"
      via: "single UPDATE in setActiveOrgId"
      pattern: "UPDATE dashboard_users SET active_org_id"
---

<objective>
Add per-user `active_org_id` persistence on `dashboard_users` so global admins can choose an active org that survives logout. This is the bedrock under D-Persistence and AORG-03 — every other plan in the phase reads or writes this column.

Purpose: Without this storage primitive there is nothing to bind tool dispatch to.
Output: Migration 061, extended UserRepository interface + SQLite implementation, repository tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-multi-org-context-switching/38-CONTEXT.md
@packages/dashboard/src/db/sqlite/migrations.ts
@packages/dashboard/src/db/interfaces/user-repository.ts
@packages/dashboard/src/db/sqlite/repositories/user-repository.ts

<interfaces>
<!-- Existing migration pattern (latest is 060 share-link expires_at). -->
<!-- Existing repo pattern uses { id, currentOrgId, displayName } DashboardUser shape. -->
<!-- Pattern reference: migration 056 (agent-conversations-soft-delete) ALTER TABLE + repo extension. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add migration 061 (active_org_id column on dashboard_users)</name>
  <files>packages/dashboard/src/db/sqlite/migrations.ts, packages/dashboard/tests/db/user-repository.test.ts</files>
  <behavior>
    - After running migrations, dashboard_users PRAGMA shows an `active_org_id` column of type TEXT, NULL allowed, default NULL.
    - Re-running migrations is idempotent (id `061` already in schema_migrations → no re-apply).
    - Existing rows are not rewritten; their active_org_id is NULL.
  </behavior>
  <action>
    Append a new migration after id `060`:
    - id: `'061'`
    - name: `'agent-active-org'`
    - sql: `ALTER TABLE dashboard_users ADD COLUMN active_org_id TEXT;`
    No index — small cardinality (D-Discretion).
    Add a test in tests/db/user-repository.test.ts asserting `PRAGMA table_info(dashboard_users)` lists `active_org_id` with type `TEXT` and `dflt_value` null. Use the same SQLite fixture pattern as `tests/db/migration-055-agent-display-name.test.ts` if present, otherwise the in-memory adapter pattern from `tests/db/conversation-repository.test.ts`.
  </action>
  <verify>
    <automated>cd packages/dashboard &amp;&amp; npx vitest run tests/db/user-repository.test.ts</automated>
  </verify>
  <done>
    - Migration 061 entry exists with `id: '061'` and `ALTER TABLE dashboard_users ADD COLUMN active_org_id`.
    - PRAGMA test passes.
    - tsc --noEmit clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend UserRepository — activeOrgId field + setActiveOrgId method</name>
  <files>packages/dashboard/src/db/interfaces/user-repository.ts, packages/dashboard/src/db/sqlite/repositories/user-repository.ts, packages/dashboard/tests/db/user-repository.test.ts</files>
  <behavior>
    - DashboardUser type carries `activeOrgId: string | null`.
    - `findById(userId)` returns `activeOrgId` populated from the new column (null when unset).
    - `setActiveOrgId(userId, orgId)` writes the value; passing `null` clears it; returns `true` if a row was updated, `false` if user not found.
    - Method is idempotent — calling twice with the same value yields the same final state.
  </behavior>
  <action>
    1. Edit `db/interfaces/user-repository.ts`:
       - Add `activeOrgId: string | null` to `DashboardUser`.
       - Add `setActiveOrgId(userId: string, orgId: string | null): Promise&lt;boolean&gt;` to `UserRepository`.
    2. Edit `db/sqlite/repositories/user-repository.ts`:
       - Update the row mapper to read `active_org_id` and surface as `activeOrgId`.
       - Update SELECT lists in any existing finders to include `active_org_id`.
       - Implement `setActiveOrgId`: single `UPDATE dashboard_users SET active_org_id = @orgId WHERE id = @userId`. Use `info.changes &gt; 0` for the boolean return.
       - Do NOT validate org existence here — that is the route's job (D-Default + D-Switcher UI).
    3. Add tests:
       - findById returns activeOrgId=null for a fresh user.
       - setActiveOrgId('orgA') then findById returns activeOrgId='orgA'.
       - setActiveOrgId(null) clears it back to null.
       - setActiveOrgId on unknown user returns false; no row inserted.
       - Idempotency: two consecutive sets to the same value both return true (or doc the actual semantics in the test).
  </action>
  <verify>
    <automated>cd packages/dashboard &amp;&amp; npx vitest run tests/db/user-repository.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - Interface + SQLite implementation expose `activeOrgId` and `setActiveOrgId`.
    - All new tests pass.
    - Existing user-repository tests still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → repository | Route layer (Plan 38-03) supplies userId + orgId; repo trusts these as already-authenticated values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | setActiveOrgId | mitigate | Single parameterised UPDATE, no string concat. WHERE clause keyed on `id = @userId` so a user can only mutate their own row (route enforces caller identity). |
| T-38-02 | Information disclosure | active_org_id column | accept | Column stores a public org id only; no PII added. |
| T-38-03 | Elevation of privilege | setActiveOrgId | mitigate | Repo does not enforce admin.system — that is the route's job (Plan 38-03). Repo is intentionally a thin write primitive; documenting this contract here so future callers don't bypass the route. |
</threat_model>

<verification>
- Migration 061 idempotent: run migrations twice → second run is a no-op.
- All new tests in tests/db/user-repository.test.ts pass.
- `cd packages/dashboard && pnpm test` regression — no existing tests broken.
- `cd packages/dashboard && npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Migration 061 present and applied.
- DashboardUser exposes `activeOrgId`.
- UserRepository.setActiveOrgId works (set/clear/unknown-user).
- Zero regressions across the dashboard test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/38-multi-org-context-switching/38-01-SUMMARY.md`
</output>
