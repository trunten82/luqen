---
phase: 80-mcp-fix-tools-for-coding-agents
plan: 03
type: execute
wave: 3
depends_on: ["80-02"]
files_modified:
  - packages/dashboard/src/mcp/metadata.ts
  - packages/dashboard/src/mcp/server.ts
  - packages/dashboard/src/routes/api/mcp.ts
  - packages/dashboard/tests/mcp/scan-fix-tools.test.ts
  - packages/dashboard/tests/mcp/tool-metadata-drift.test.ts
autonomous: true
requirements: [MCPFIX-01, MCPFIX-02, MCPFIX-03, MCPFIX-04, MCPFIX-05]
must_haves:
  truths:
    - "dashboard_scan_page and dashboard_generate_fix are registered on the single dashboard MCP front door (D-01)"
    - "Both tools carry requiredPermission (scans.create / issues.fix) and the drift test stays green"
    - "Access composes mcp.use with per-tool permission (issues.fix / scans.create) (D-08)"
    - "tools/list hides the tools from a caller lacking the permission; tools/call rejects without it (D-08)"
    - "Connection still gated by mcp.use (Phase 31.2) — unchanged (D-08)"
    - "End-to-end: an authorized agent scans and generates an enriched, disclaimed fix draft"
    - "Neither tool can apply changes — no destructive flag, no write path end-to-end (D-09)"
    - "The end-to-end fix payload carries the conservative disclaimer (D-10)"
  artifacts:
    - path: "packages/dashboard/src/mcp/metadata.ts"
      provides: "DASHBOARD_AGENT_TOOL_METADATA spread into DASHBOARD_TOOL_METADATA"
      contains: "DASHBOARD_AGENT_TOOL_METADATA"
    - path: "packages/dashboard/src/mcp/server.ts"
      provides: "registerScanTools + registerFixTools wired; AGENT tool names in toolNames; llmAccess option"
      contains: "registerFixTools"
    - path: "packages/dashboard/src/routes/api/mcp.ts"
      provides: "scanner + llmAccess wired from config/ServiceClientRegistry into createDashboardMcpServer"
      contains: "llmAccess"
  key_links:
    - from: "packages/dashboard/src/mcp/server.ts"
      to: "DASHBOARD_AGENT_TOOL_METADATA"
      via: "toolNames + metadata count parity (drift test)"
      pattern: "AGENT_TOOL_NAMES"
    - from: "packages/dashboard/src/routes/api/mcp.ts"
      to: "ServiceClientRegistry llm token manager / config.llmUrl"
      via: "llmAccess callback wiring"
      pattern: "llm"
    - from: "core/mcp http-plugin per-tool runtime guard"
      to: "dashboard_scan_page / dashboard_generate_fix"
      via: "requiredPermission enforcement on tools/call"
      pattern: "requiredPermission"
---

<objective>
Wire the two new tool modules from Plan 02 into the dashboard MCP server so they are
discoverable and callable under the existing OAuth2 JWT + RBAC + mcp.use plumbing, and
prove the full auth/RBAC/never-apply/conservative-framing guarantees end-to-end
(MCPFIX-05). Registration is atomic: metadata entries and server registration land
together so tool-metadata-drift.test.ts count-parity never sees a partial state.

Add DASHBOARD_AGENT_TOOL_METADATA (dashboard_scan_page to scans.create,
dashboard_generate_fix to issues.fix — both existing permissions, NO new permission,
NO edit to permissions.ts), spread it into DASHBOARD_TOOL_METADATA, register both tool
modules in server.ts (fix guarded like registerComplianceTools), add their names to
toolNames, add an llmAccess option mirroring complianceAccess, and wire scanner +
llmAccess in routes/api/mcp.ts.

Purpose: deliver MCPFIX-05 (auth/RBAC/org-scope + never-apply) and surface MCPFIX-01..04
through the live MCP endpoint.
Output: wired server, drift test green, and an end-to-end integration suite proving
tools/list filtering, tools/call rejection, and a full authorized scan+fix flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-mcp-fix-tools-for-coding-agents/80-CONTEXT.md
@.planning/phases/80-mcp-fix-tools-for-coding-agents/80-PATTERNS.md
@.planning/phases/80-mcp-fix-tools-for-coding-agents/80-02-SUMMARY.md

@packages/dashboard/src/mcp/server.ts
@packages/dashboard/src/mcp/metadata.ts

<interfaces>
Authoritative wiring contracts.

metadata.ts (CURRENT): DASHBOARD_TOOL_METADATA = [...DATA, ...COMPLIANCE, ...ADMIN, ...FLEET] (Phase 80 adds ...AGENT). ToolMetadata entries: { name, requiredPermission, destructive?, confirmationTemplate? }.

server.ts (CURRENT): DashboardMcpServerOptions { storage, scanService, serviceConnections, complianceAccess? } (add llmAccess?, scanner). createDashboardMcpServer composes registerDataTools / registerAdminTools / registerFleetTools / registerComplianceTools(guarded). toolNames = complianceAccess ? [...DATA,...COMPLIANCE,...ADMIN,...FLEET] : [...DATA,...ADMIN,...FLEET].

permissions.ts — ALREADY PRESENT, no edit: 'scans.create' (line 9), 'issues.fix' (line 18) both in ALL_PERMISSION_IDS.

Per-tool runtime RBAC guard (core/mcp/http-plugin.ts lines 202-247): tools/call rejects when meta.requiredPermission is set and ctx.permissions lacks it. tools/list filter (core/mcp/tool-filter.ts): filterToolsByPermissions hides tools whose requiredPermission is absent from effective perms.

Drift test (tests/mcp/tool-metadata-drift.test.ts) invariant 5: DASHBOARD_TOOL_METADATA count MUST equal the registered tool count from createDashboardMcpServer.

llm access wiring source (services/service-client-registry.ts): config.llmUrl / llmClientId / llmClientSecret; resolves an llm token manager — mirror getComplianceTokenManager wiring in server.ts around line 341.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DASHBOARD_AGENT_TOOL_METADATA and register tools in server.ts</name>
  <read_first>
    - packages/dashboard/src/mcp/metadata.ts (DASHBOARD_COMPLIANCE_TOOL_METADATA analog, lines 27-74)
    - packages/dashboard/src/mcp/server.ts (registerComplianceTools guard + toolNames assembly, lines 74-104)
    - packages/dashboard/src/mcp/tools/scan.ts and fix.ts (SCAN_TOOL_NAMES/FIX_TOOL_NAMES, registerScanTools/registerFixTools from Plan 02)
    - 80-PATTERNS.md "metadata.ts (extend in-place)" lines 206-237 and "server.ts (extend in-place)" lines 240-267
  </read_first>
  <action>
    In metadata.ts: add DASHBOARD_AGENT_TOOL_METADATA with exactly two entries — dashboard_scan_page requiredPermission scans.create, and dashboard_generate_fix requiredPermission issues.fix — neither with a destructive flag (D-09; 80-PATTERNS.md lines 222-233). Spread DASHBOARD_AGENT_TOOL_METADATA into DASHBOARD_TOOL_METADATA. Do NOT edit permissions.ts — both ids already exist (80-PATTERNS.md lines 312-318).
    In server.ts: import registerScanTools/SCAN_TOOL_NAMES from './tools/scan.js' and registerFixTools/FIX_TOOL_NAMES plus type LlmAccess from './tools/fix.js'. Add optional llmAccess of type LlmAccess to DashboardMcpServerOptions, and provide the scanner accessor the scan tool needs (match Plan 02's RegisterScanToolsOptions — reuse the existing scanService scanner or instantiate a DirectScanner). Register the scan tool unconditionally and the fix tool only when llmAccess is defined (guard mirroring registerComplianceTools). Add SCAN_TOOL_NAMES (always) and FIX_TOOL_NAMES (only when llmAccess present) to toolNames so count parity matches what is registered, keeping the drift invariant green in both branches.
  </action>
  <verify>
    <automated>cd /root/luqen && npx tsc -p packages/dashboard/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: DASHBOARD_AGENT_TOOL_METADATA has exactly two entries with requiredPermission scans.create and issues.fix; neither has destructive:true (D-09).
    - source: server.ts registers scan tool always and fix tool guarded by llmAccess; toolNames includes SCAN_TOOL_NAMES always and FIX_TOOL_NAMES iff llmAccess defined.
    - typecheck: dashboard tsc project clean.
    - permissions.ts unchanged: git diff --quiet packages/dashboard/src/permissions.ts exits 0.
  </acceptance_criteria>
  <done>Both agent tools registered + annotated with existing permissions; typecheck clean; no permission added.</done>
</task>

<task type="auto">
  <name>Task 2: Wire scanner + llmAccess into the MCP route (routes/api/mcp.ts)</name>
  <read_first>
    - packages/dashboard/src/routes/api/mcp.ts (registerMcpRoutes — where createDashboardMcpServer is constructed and complianceAccess is wired)
    - packages/dashboard/src/server.ts lines 324-350 (ServiceClientRegistry.create + getComplianceTokenManager wiring to mirror for llm)
    - packages/dashboard/src/services/service-client-registry.ts lines 76-135 (llm client/token resolution)
  </read_first>
  <action>
    In registerMcpRoutes (routes/api/mcp.ts): build the llmAccess callback the same way complianceAccess is built — resolve baseUrl + token from config.llmUrl plus the ServiceClientRegistry llm token manager per call (so secret rotation is picked up without restart); return null when the llm connection is not configured. Pass llmAccess into createDashboardMcpServer options, and pass the scanner accessor the scan tool needs (reuse the existing scanService scanner if exposed, otherwise instantiate a DirectScanner). Do not change the existing complianceAccess wiring, and do not touch the mcp.use connection gate or JWT verification path — MCPFIX-05 reuses Phase 31.2. Update the route options type / call site so the new fields type-check.
  </action>
  <verify>
    <automated>cd /root/luqen && npx tsc -p packages/dashboard/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - source: registerMcpRoutes builds an llmAccess callback returning baseUrl+token (or null when llm not configured) and passes it + the scanner into createDashboardMcpServer.
    - source: the mcp.use connection gate and JWT verification path are unchanged (no edits to authorize.ts / oauth-consent or the verifyToken wiring).
    - typecheck: dashboard tsc project clean.
  </acceptance_criteria>
  <done>The MCP route wires scanner + llmAccess; tools are live under the existing auth/connection gate; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end auth/RBAC/never-apply integration tests + keep drift green</name>
  <read_first>
    - packages/dashboard/tests/mcp/compliance-tools.test.ts (buildApp via registerMcpRoutes, tools/list permission filtering, callTool)
    - packages/dashboard/tests/mcp/tool-metadata-drift.test.ts (stub options for createDashboardMcpServer — add llmAccess stub so both new tools count)
    - packages/dashboard/tests/mcp/scan-fix-tools.test.ts (the module-level suite from Plan 02 — append the integration describe block here)
    - 80-PATTERNS.md "scan-fix-tools.test.ts" lines 322-393 (buildApp, parseSseOrJson, callTool)
  </read_first>
  <action>
    Extend the drift test's stub createDashboardMcpServer options to pass an llmAccess stub (and the scanner accessor) so the registered tool count includes dashboard_scan_page + dashboard_generate_fix and invariant 5 (count parity) stays green.
    Append an integration describe block to scan-fix-tools.test.ts that boots the full route via the buildApp(registerMcpRoutes) helper from compliance-tools.test.ts (with llmAccess + scanner + complianceAccess stubs), and asserts:
    1. tools/list for a caller WITHOUT scans.create hides dashboard_scan_page; WITHOUT issues.fix hides dashboard_generate_fix (RBAC tools/list filtering — MCPFIX-05).
    2. tools/call dashboard_generate_fix WITHOUT issues.fix returns the per-tool permission rejection from http-plugin (MCPFIX-05 runtime guard).
    3. Authorized happy path: a caller WITH both permissions calls dashboard_scan_page (structured findings) then dashboard_generate_fix and receives wcagCriterion + diff + fixedHtml + explanation + effort + legalContext + disclaimer (MCPFIX-01..04 + D-10).
    4. legalContext degrades to null (not isError) when the compliance stub is null (D-06 graceful).
    5. Never-apply (D-09): neither tool's metadata has destructive:true, and a source grep over scan.ts/fix.ts shows no write/apply/initiateScan path.
    Do NOT re-test JWT verification internals — assert only that an unauthenticated request is rejected by the existing plugin (reuse compliance-tools.test.ts's pattern) to confirm the mcp.use/auth gate still fronts the new tools.
  </action>
  <verify>
    <automated>cd /root/luqen && npx vitest run packages/dashboard/tests/mcp/scan-fix-tools.test.ts packages/dashboard/tests/mcp/tool-metadata-drift.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - drift: tool-metadata-drift.test.ts green with the new tools counted (llmAccess stub added to its createDashboardMcpServer options).
    - RBAC list (MCPFIX-05): tools/list omits dashboard_scan_page without scans.create and dashboard_generate_fix without issues.fix.
    - RBAC call (MCPFIX-05): tools/call dashboard_generate_fix without issues.fix returns the requiredPermission rejection (not a result).
    - happy path (MCPFIX-01..04 + D-10): authorized caller gets findings, then a fix payload containing wcagCriterion, diff, fixedHtml, explanation, effort, legalContext, and disclaimer === DRAFT_DISCLAIMER.
    - degrade (D-06): compliance null -> legalContext:null, isError NOT set.
    - never-apply (D-09): assertion that neither metadata entry is destructive; grep -nE "destructive|writeFile|initiateScan|applyFix|\.write\(" over scan.ts/fix.ts (filtered with grep -v '^#') shows no destructive/apply/write path.
    - test-command: the two suites above pass.
  </acceptance_criteria>
  <done>End-to-end auth/RBAC enforcement, enriched authorized fix flow, graceful legalContext degrade, and never-apply guarantee are all proven; drift test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coding agent -> dashboard MCP endpoint | OAuth2 JWT + mcp.use connection gate + per-tool RBAC |
| dashboard MCP server -> llm service (HTTP) | llmAccess token forwarded |
| dashboard MCP server -> compliance service (HTTP) | complianceAccess token for legalContext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-10 | Elevation of Privilege | tools/call without permission | mitigate | requiredPermission (scans.create / issues.fix) on both tools; http-plugin runtime guard rejects (Task 3 case 2) |
| T-80-11 | Information Disclosure | tools/list exposing tools to unauthorized callers | mitigate | filterToolsByPermissions hides both tools without the permission (Task 3 case 1) |
| T-80-12 | Spoofing | unauthenticated / non-mcp.use connection | mitigate | Reuse Phase 31.2 JWT + mcp.use consent gate unchanged; Task 3 asserts unauthenticated rejection |
| T-80-13 | Integrity/Repudiation | asserting "compliant" output | mitigate | D-10 disclaimer asserted in the authorized happy-path test (Task 3 case 3) |
| T-80-14 | Tampering | drift between metadata and registered tools | mitigate | tool-metadata-drift invariant 5 kept green by counting both new tools (Task 3) |
| T-80-15 | Tampering (never-apply) | a write/apply path slipping in | mitigate | No destructive flag; source grep gate in Task 3 (D-09) |
| T-80-SC | Tampering | npm installs | mitigate | No new dependency expected; any install requires RESEARCH.md Package Legitimacy Audit + blocking checkpoint |
</threat_model>

<verification>
- tool-metadata-drift.test.ts green (both new tools counted; no permission typo).
- scan-fix-tools.test.ts integration block green: tools/list RBAC filtering, tools/call rejection, authorized scan+fix happy path with enriched disclaimed payload, legalContext degrade.
- npx tsc -p packages/dashboard/tsconfig.json --noEmit clean.
- git diff --quiet packages/dashboard/src/permissions.ts (no new permission).
- grep over scan.ts/fix.ts shows no destructive/apply/write path (D-09).
</verification>

<success_criteria>
- Both agent tools live on the dashboard MCP endpoint under OAuth2 JWT + mcp.use + per-tool RBAC (MCPFIX-05).
- An authorized agent scans (MCPFIX-01) and generates a source-level fix with diff + WCAG criterion (MCPFIX-02), legal context (MCPFIX-03), WP-block support via platform (MCPFIX-04).
- Tools never apply changes (D-09) and never assert compliance (D-10).
- drift + integration + typecheck all green.
</success_criteria>

<output>
Create `.planning/phases/80-mcp-fix-tools-for-coding-agents/80-03-SUMMARY.md` when done.
</output>
