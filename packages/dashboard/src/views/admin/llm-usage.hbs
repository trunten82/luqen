{{!-- Phase 72-03 — Admin LLM usage page.
     The full body (KPIs, filter form, row table) is rendered server-side
     by routes/admin/llm-usage.ts so this template stays a thin shell. --}}
{{{bodyHtml}}}
