<div class="modal-overlay" role="dialog" aria-modal="true" aria-labelledby="modal-title">
  <div class="modal">
    <h2 id="modal-title">{{#if isNew}}Add Jurisdiction{{else}}Edit Jurisdiction{{/if}}</h2>

    <form {{#if isNew}}
          hx-post="/admin/jurisdictions"
          hx-target="#jurisdictions-table-body"
          hx-swap="beforeend"
          {{else}}
          hx-patch="/admin/jurisdictions/{{jurisdiction.id}}"
          hx-target="#jurisdiction-{{jurisdiction.id}}"
          hx-swap="outerHTML"
          {{/if}}
          hx-on::after-request="if(event.detail.successful) document.getElementById('modal-container').replaceChildren()"
          novalidate>

      <div class="form-group">
        <label for="jur-id" class="form-label">ID <span aria-hidden="true">*</span></label>
        <input id="jur-id"
               name="id"
               type="text"
               class="input"
               value="{{jurisdiction.id}}"
               {{#unless isNew}}readonly{{/unless}}
               required
               aria-required="true"
               aria-describedby="jur-id-hint">
        <span id="jur-id-hint" class="form-hint">Short identifier, e.g. EU, US-CA</span>
      </div>

      <div class="form-group">
        <label for="jur-name" class="form-label">Name <span aria-hidden="true">*</span></label>
        <input id="jur-name"
               name="name"
               type="text"
               class="input"
               value="{{jurisdiction.name}}"
               required
               aria-required="true">
      </div>

      <div class="form-group">
        <label for="jur-type" class="form-label">Type <span aria-hidden="true">*</span></label>
        <input id="jur-type"
               name="type"
               type="text"
               class="input"
               value="{{jurisdiction.type}}"
               required
               aria-required="true"
               aria-describedby="jur-type-hint">
        <span id="jur-type-hint" class="form-hint">e.g. country, region, state</span>
      </div>

      <div class="form-group">
        <label for="jur-parent" class="form-label">Parent ID</label>
        <input id="jur-parent"
               name="parentId"
               type="text"
               class="input"
               value="{{jurisdiction.parentId}}">
      </div>

      <div class="form-actions">
        <button type="submit" class="btn btn--primary">
          {{#if isNew}}Create{{else}}Save{{/if}}
        </button>
        <button type="button"
                class="btn btn--ghost close-modal-btn"
                aria-label="Cancel and close dialog">Cancel</button>
      </div>
    </form>
  </div>
</div>
